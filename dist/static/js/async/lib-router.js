"use strict";
(self["chunk_mfe_shell_app"] = self["chunk_mfe_shell_app"] || []).push([["lib-router"], {
"./node_modules/react-router/dist/development/chunk-JZWAC4HX.mjs"(module, __webpack_exports__, __webpack_require__) {
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
  Action: () => (Action),
  Await: () => (Await),
  AwaitContextProvider: () => (AwaitContextProvider),
  BrowserRouter: () => (BrowserRouter),
  CRITICAL_CSS_DATA_ATTRIBUTE: () => (CRITICAL_CSS_DATA_ATTRIBUTE),
  DataRouterContext: () => (DataRouterContext),
  DataRouterStateContext: () => (DataRouterStateContext),
  ENABLE_DEV_WARNINGS: () => (ENABLE_DEV_WARNINGS),
  ErrorResponseImpl: () => (ErrorResponseImpl),
  FetchersContext: () => (FetchersContext),
  Form: () => (Form),
  FrameworkContext: () => (FrameworkContext),
  HashRouter: () => (HashRouter),
  HistoryRouter: () => (HistoryRouter),
  IDLE_BLOCKER: () => (IDLE_BLOCKER),
  IDLE_FETCHER: () => (IDLE_FETCHER),
  IDLE_NAVIGATION: () => (IDLE_NAVIGATION),
  Link: () => (Link),
  Links: () => (Links),
  LocationContext: () => (LocationContext),
  MemoryRouter: () => (MemoryRouter),
  Meta: () => (Meta),
  NO_BODY_STATUS_CODES: () => (NO_BODY_STATUS_CODES),
  NavLink: () => (NavLink),
  Navigate: () => (Navigate),
  NavigationContext: () => (NavigationContext),
  Outlet: () => (Outlet),
  PrefetchPageLinks: () => (PrefetchPageLinks),
  RSCRouterContext: () => (RSCRouterContext),
  RemixErrorBoundary: () => (RemixErrorBoundary),
  Route: () => (Route),
  RouteContext: () => (RouteContext),
  Router: () => (Router),
  RouterContextProvider: () => (RouterContextProvider),
  RouterProvider: () => (RouterProvider),
  Routes: () => (Routes),
  SINGLE_FETCH_REDIRECT_STATUS: () => (SINGLE_FETCH_REDIRECT_STATUS),
  Scripts: () => (Scripts),
  ScrollRestoration: () => (ScrollRestoration),
  SingleFetchRedirectSymbol: () => (SingleFetchRedirectSymbol),
  StaticRouter: () => (StaticRouter),
  StaticRouterProvider: () => (StaticRouterProvider),
  StreamTransfer: () => (StreamTransfer),
  ViewTransitionContext: () => (ViewTransitionContext),
  WithComponentProps: () => (WithComponentProps),
  WithErrorBoundaryProps: () => (WithErrorBoundaryProps),
  WithHydrateFallbackProps: () => (WithHydrateFallbackProps),
  convertRoutesToDataRoutes: () => (convertRoutesToDataRoutes),
  createBrowserHistory: () => (createBrowserHistory),
  createBrowserRouter: () => (createBrowserRouter),
  createClientRoutes: () => (createClientRoutes),
  createClientRoutesWithHMRRevalidationOptOut: () => (createClientRoutesWithHMRRevalidationOptOut),
  createContext: () => (createContext),
  createHashHistory: () => (createHashHistory),
  createHashRouter: () => (createHashRouter),
  createMemoryHistory: () => (createMemoryHistory),
  createMemoryRouter: () => (createMemoryRouter),
  createPath: () => (createPath),
  createRequestInit: () => (createRequestInit),
  createRouter: () => (createRouter),
  createRoutesFromChildren: () => (createRoutesFromChildren),
  createRoutesFromElements: () => (createRoutesFromElements),
  createSearchParams: () => (createSearchParams),
  createServerRoutes: () => (createServerRoutes),
  createStaticHandler: () => (createStaticHandler),
  createStaticHandler2: () => (createStaticHandler2),
  createStaticRouter: () => (createStaticRouter),
  data: () => (data),
  decodeRedirectErrorDigest: () => (decodeRedirectErrorDigest),
  decodeRouteErrorResponseDigest: () => (decodeRouteErrorResponseDigest),
  decodeViaTurboStream: () => (decodeViaTurboStream),
  encode: () => (encode),
  escapeHtml: () => (escapeHtml),
  generatePath: () => (generatePath),
  getManifestPath: () => (getManifestPath),
  getPatchRoutesOnNavigationFunction: () => (getPatchRoutesOnNavigationFunction),
  getSingleFetchDataStrategyImpl: () => (getSingleFetchDataStrategyImpl),
  getStaticContextFromError: () => (getStaticContextFromError),
  getTurboStreamSingleFetchDataStrategy: () => (getTurboStreamSingleFetchDataStrategy),
  hydrationRouteProperties: () => (hydrationRouteProperties),
  instrumentHandler: () => (instrumentHandler),
  invariant: () => (invariant),
  isDataWithResponseInit: () => (isDataWithResponseInit),
  isMutationMethod: () => (isMutationMethod),
  isRedirectResponse: () => (isRedirectResponse),
  isRedirectStatusCode: () => (isRedirectStatusCode),
  isResponse: () => (isResponse),
  isRouteErrorResponse: () => (isRouteErrorResponse),
  mapRouteProperties: () => (mapRouteProperties),
  matchPath: () => (matchPath),
  matchRoutes: () => (matchRoutes),
  noActionDefinedError: () => (noActionDefinedError),
  parsePath: () => (parsePath),
  redirect: () => (redirect),
  redirectDocument: () => (redirectDocument),
  renderMatches: () => (renderMatches),
  replace: () => (replace),
  resolvePath: () => (resolvePath),
  setIsHydrated: () => (setIsHydrated),
  shouldHydrateRouteLoader: () => (shouldHydrateRouteLoader),
  singleFetchUrl: () => (singleFetchUrl),
  stripBasename: () => (stripBasename),
  stripIndexParam: () => (stripIndexParam),
  useActionData: () => (useActionData),
  useAsyncError: () => (useAsyncError),
  useAsyncValue: () => (useAsyncValue),
  useBeforeUnload: () => (useBeforeUnload),
  useBlocker: () => (useBlocker),
  useFetcher: () => (useFetcher),
  useFetchers: () => (useFetchers),
  useFogOFWarDiscovery: () => (useFogOFWarDiscovery),
  useFormAction: () => (useFormAction),
  useHref: () => (useHref),
  useInRouterContext: () => (useInRouterContext),
  useLinkClickHandler: () => (useLinkClickHandler),
  useLoaderData: () => (useLoaderData),
  useLocation: () => (useLocation),
  useMatch: () => (useMatch),
  useMatches: () => (useMatches),
  useNavigate: () => (useNavigate),
  useNavigation: () => (useNavigation),
  useNavigationType: () => (useNavigationType),
  useOutlet: () => (useOutlet),
  useOutletContext: () => (useOutletContext),
  useParams: () => (useParams),
  usePrompt: () => (usePrompt),
  useResolvedPath: () => (useResolvedPath),
  useRevalidator: () => (useRevalidator),
  useRoute: () => (useRoute),
  useRouteError: () => (useRouteError),
  useRouteLoaderData: () => (useRouteLoaderData),
  useRoutes: () => (useRoutes),
  useScrollRestoration: () => (useScrollRestoration),
  useSearchParams: () => (useSearchParams),
  useSubmit: () => (useSubmit),
  useViewTransitionState: () => (useViewTransitionState),
  warnOnce: () => (warnOnce),
  withComponentProps: () => (withComponentProps),
  withErrorBoundaryProps: () => (withErrorBoundaryProps),
  withHydrateFallbackProps: () => (withHydrateFallbackProps)
});
/* import */ var _swc_helpers_async_to_generator__rspack_import_3 = __webpack_require__("./node_modules/@swc/helpers/esm/_async_to_generator.js");
/* import */ var _swc_helpers_object_spread__rspack_import_2 = __webpack_require__("./node_modules/@swc/helpers/esm/_object_spread.js");
/* import */ var _swc_helpers_object_spread_props__rspack_import_1 = __webpack_require__("./node_modules/@swc/helpers/esm/_object_spread_props.js");
/* import */ var _swc_helpers_object_without_properties__rspack_import_5 = __webpack_require__("./node_modules/@swc/helpers/esm/_object_without_properties.js");
/* import */ var _swc_helpers_type_of__rspack_import_4 = __webpack_require__("./node_modules/@swc/helpers/esm/_type_of.js");
/* import */ var react__rspack_import_0 = __webpack_require__("webpack/sharing/consume/default/react/react?33b2");
/* import */ var react__rspack_import_0_default = /*#__PURE__*/__webpack_require__.n(react__rspack_import_0);
/* provided dependency */ var $ReactRefreshRuntime$ = __webpack_require__("./node_modules/@rspack/plugin-react-refresh/client/reactRefresh.js");





var _s = $RefreshSig$(), _s1 = $RefreshSig$(), _s2 = $RefreshSig$(), _s3 = $RefreshSig$(), _s4 = $RefreshSig$(), _s5 = $RefreshSig$(), _s6 = $RefreshSig$(), _s7 = $RefreshSig$(), _s8 = $RefreshSig$(), _s9 = $RefreshSig$(), _s10 = $RefreshSig$(), _s11 = $RefreshSig$(), _s12 = $RefreshSig$(), _s13 = $RefreshSig$(), _s14 = $RefreshSig$(), _s15 = $RefreshSig$(), _s16 = $RefreshSig$(), _s17 = $RefreshSig$(), _s18 = $RefreshSig$(), _s19 = $RefreshSig$(), _s20 = $RefreshSig$(), _s21 = $RefreshSig$(), _s22 = $RefreshSig$(), _s23 = $RefreshSig$(), _s24 = $RefreshSig$(), _s25 = $RefreshSig$(), _s26 = $RefreshSig$(), _s27 = $RefreshSig$(), _s28 = $RefreshSig$(), _s29 = $RefreshSig$(), _s30 = $RefreshSig$(), _s31 = $RefreshSig$(), _s32 = $RefreshSig$(), _s33 = $RefreshSig$(), _s34 = $RefreshSig$(), _s35 = $RefreshSig$(), _s36 = $RefreshSig$(), _s37 = $RefreshSig$(), _s38 = $RefreshSig$(), _s39 = $RefreshSig$(), _s40 = $RefreshSig$(), _s41 = $RefreshSig$(), _s42 = $RefreshSig$(), _s43 = $RefreshSig$(), _s44 = $RefreshSig$(), _s45 = $RefreshSig$(), _s46 = $RefreshSig$(), _s47 = $RefreshSig$(), _s48 = $RefreshSig$(), _s49 = $RefreshSig$(), _s50 = $RefreshSig$(), _s51 = $RefreshSig$(), _s52 = $RefreshSig$(), _s53 = $RefreshSig$(), _s54 = $RefreshSig$(), _s55 = $RefreshSig$(), _s56 = $RefreshSig$(), _s57 = $RefreshSig$(), _s58 = $RefreshSig$(), _s59 = $RefreshSig$(), _s60 = $RefreshSig$(), _s61 = $RefreshSig$(), _s62 = $RefreshSig$(), _s63 = $RefreshSig$(), _s64 = $RefreshSig$(), _s65 = $RefreshSig$(), _s66 = $RefreshSig$(), _s67 = $RefreshSig$(), _s68 = $RefreshSig$(), _s69 = $RefreshSig$(), _s70 = $RefreshSig$(), _s71 = $RefreshSig$(), _s72 = $RefreshSig$(), _s73 = $RefreshSig$(), _s74 = $RefreshSig$(), _s75 = $RefreshSig$(), _s76 = $RefreshSig$(), _s77 = $RefreshSig$(), _s78 = $RefreshSig$(), _s79 = $RefreshSig$(), _s80 = $RefreshSig$(), _s81 = $RefreshSig$(), _s82 = $RefreshSig$();
/**
 * react-router v7.13.0
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */ var __typeError = (msg)=>{
    throw TypeError(msg);
};
var __accessCheck = (obj, member, msg)=>member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter)=>(__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value)=>member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
// lib/router/history.ts
var Action = /* @__PURE__ */ ((Action2)=>{
    Action2["Pop"] = "POP";
    Action2["Push"] = "PUSH";
    Action2["Replace"] = "REPLACE";
    return Action2;
})(Action || {});
var PopStateEventType = "popstate";
function createMemoryHistory() {
    var options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    var { initialEntries = [
        "/"
    ], initialIndex, v5Compat = false } = options;
    var entries;
    entries = initialEntries.map((entry, index2)=>createMemoryLocation(entry, typeof entry === "string" ? null : entry.state, index2 === 0 ? "default" : void 0));
    var index = clampIndex(initialIndex == null ? entries.length - 1 : initialIndex);
    var action = "POP" /* Pop */ ;
    var listener = null;
    function clampIndex(n) {
        return Math.min(Math.max(n, 0), entries.length - 1);
    }
    function getCurrentLocation() {
        return entries[index];
    }
    function createMemoryLocation(to) {
        var state = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null, key = arguments.length > 2 ? arguments[2] : void 0;
        var location = createLocation(entries ? getCurrentLocation().pathname : "/", to, state, key);
        warning(location.pathname.charAt(0) === "/", "relative pathnames are not supported in memory history: ".concat(JSON.stringify(to)));
        return location;
    }
    function createHref2(to) {
        return typeof to === "string" ? to : createPath(to);
    }
    var history = {
        get index () {
            return index;
        },
        get action () {
            return action;
        },
        get location () {
            return getCurrentLocation();
        },
        createHref: createHref2,
        createURL (to) {
            return new URL(createHref2(to), "http://localhost");
        },
        encodeLocation (to) {
            var path = typeof to === "string" ? parsePath(to) : to;
            return {
                pathname: path.pathname || "",
                search: path.search || "",
                hash: path.hash || ""
            };
        },
        push (to, state) {
            action = "PUSH" /* Push */ ;
            var nextLocation = createMemoryLocation(to, state);
            index += 1;
            entries.splice(index, entries.length, nextLocation);
            if (v5Compat && listener) {
                listener({
                    action,
                    location: nextLocation,
                    delta: 1
                });
            }
        },
        replace (to, state) {
            action = "REPLACE" /* Replace */ ;
            var nextLocation = createMemoryLocation(to, state);
            entries[index] = nextLocation;
            if (v5Compat && listener) {
                listener({
                    action,
                    location: nextLocation,
                    delta: 0
                });
            }
        },
        go (delta) {
            action = "POP" /* Pop */ ;
            var nextIndex = clampIndex(index + delta);
            var nextLocation = entries[nextIndex];
            index = nextIndex;
            if (listener) {
                listener({
                    action,
                    location: nextLocation,
                    delta
                });
            }
        },
        listen (fn) {
            listener = fn;
            return ()=>{
                listener = null;
            };
        }
    };
    return history;
}
function createBrowserHistory() {
    var options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    function createBrowserLocation(window2, globalHistory) {
        var { pathname, search, hash } = window2.location;
        return createLocation("", {
            pathname,
            search,
            hash
        }, // state defaults to `null` because `window.history.state` does
        globalHistory.state && globalHistory.state.usr || null, globalHistory.state && globalHistory.state.key || "default");
    }
    function createBrowserHref(window2, to) {
        return typeof to === "string" ? to : createPath(to);
    }
    return getUrlBasedHistory(createBrowserLocation, createBrowserHref, null, options);
}
function createHashHistory() {
    var options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    function createHashLocation(window2, globalHistory) {
        var { pathname = "/", search = "", hash = "" } = parsePath(window2.location.hash.substring(1));
        if (!pathname.startsWith("/") && !pathname.startsWith(".")) {
            pathname = "/" + pathname;
        }
        return createLocation("", {
            pathname,
            search,
            hash
        }, // state defaults to `null` because `window.history.state` does
        globalHistory.state && globalHistory.state.usr || null, globalHistory.state && globalHistory.state.key || "default");
    }
    function createHashHref(window2, to) {
        var base = window2.document.querySelector("base");
        var href = "";
        if (base && base.getAttribute("href")) {
            var url = window2.location.href;
            var hashIndex = url.indexOf("#");
            href = hashIndex === -1 ? url : url.slice(0, hashIndex);
        }
        return href + "#" + (typeof to === "string" ? to : createPath(to));
    }
    function validateHashLocation(location, to) {
        warning(location.pathname.charAt(0) === "/", "relative pathnames are not supported in hash history.push(".concat(JSON.stringify(to), ")"));
    }
    return getUrlBasedHistory(createHashLocation, createHashHref, validateHashLocation, options);
}
function invariant(value, message) {
    if (value === false || value === null || typeof value === "undefined") {
        throw new Error(message);
    }
}
function warning(cond, message) {
    if (!cond) {
        if (typeof console !== "undefined") console.warn(message);
        try {
            throw new Error(message);
        } catch (e) {}
    }
}
function createKey() {
    return Math.random().toString(36).substring(2, 10);
}
function getHistoryState(location, index) {
    return {
        usr: location.state,
        key: location.key,
        idx: index
    };
}
function createLocation(current, to) {
    var state = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null, key = arguments.length > 3 ? arguments[3] : void 0;
    var location = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({
        pathname: typeof current === "string" ? current : current.pathname,
        search: "",
        hash: ""
    }, typeof to === "string" ? parsePath(to) : to), {
        state,
        // TODO: This could be cleaned up.  push/replace should probably just take
        // full Locations now and avoid the need to run through this flow at all
        // But that's a pretty big refactor to the current test suite so going to
        // keep as is for the time being and just let any incoming keys take precedence
        key: to && to.key || key || createKey()
    });
    return location;
}
function createPath(param) {
    var { pathname = "/", search = "", hash = "" } = param;
    if (search && search !== "?") pathname += search.charAt(0) === "?" ? search : "?" + search;
    if (hash && hash !== "#") pathname += hash.charAt(0) === "#" ? hash : "#" + hash;
    return pathname;
}
function parsePath(path) {
    var parsedPath = {};
    if (path) {
        var hashIndex = path.indexOf("#");
        if (hashIndex >= 0) {
            parsedPath.hash = path.substring(hashIndex);
            path = path.substring(0, hashIndex);
        }
        var searchIndex = path.indexOf("?");
        if (searchIndex >= 0) {
            parsedPath.search = path.substring(searchIndex);
            path = path.substring(0, searchIndex);
        }
        if (path) {
            parsedPath.pathname = path;
        }
    }
    return parsedPath;
}
function getUrlBasedHistory(getLocation, createHref2, validateLocation) {
    var options = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {};
    var { window: window2 = document.defaultView, v5Compat = false } = options;
    var globalHistory = window2.history;
    var action = "POP" /* Pop */ ;
    var listener = null;
    var index = getIndex();
    if (index == null) {
        index = 0;
        globalHistory.replaceState((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, globalHistory.state), {
            idx: index
        }), "");
    }
    function getIndex() {
        var state = globalHistory.state || {
            idx: null
        };
        return state.idx;
    }
    function handlePop() {
        action = "POP" /* Pop */ ;
        var nextIndex = getIndex();
        var delta = nextIndex == null ? null : nextIndex - index;
        index = nextIndex;
        if (listener) {
            listener({
                action,
                location: history.location,
                delta
            });
        }
    }
    function push(to, state) {
        action = "PUSH" /* Push */ ;
        var location = createLocation(history.location, to, state);
        if (validateLocation) validateLocation(location, to);
        index = getIndex() + 1;
        var historyState = getHistoryState(location, index);
        var url = history.createHref(location);
        try {
            globalHistory.pushState(historyState, "", url);
        } catch (error) {
            if (error instanceof DOMException && error.name === "DataCloneError") {
                throw error;
            }
            window2.location.assign(url);
        }
        if (v5Compat && listener) {
            listener({
                action,
                location: history.location,
                delta: 1
            });
        }
    }
    function replace2(to, state) {
        action = "REPLACE" /* Replace */ ;
        var location = createLocation(history.location, to, state);
        if (validateLocation) validateLocation(location, to);
        index = getIndex();
        var historyState = getHistoryState(location, index);
        var url = history.createHref(location);
        globalHistory.replaceState(historyState, "", url);
        if (v5Compat && listener) {
            listener({
                action,
                location: history.location,
                delta: 0
            });
        }
    }
    function createURL(to) {
        return createBrowserURLImpl(to);
    }
    var history = {
        get action () {
            return action;
        },
        get location () {
            return getLocation(window2, globalHistory);
        },
        listen (fn) {
            if (listener) {
                throw new Error("A history only accepts one active listener");
            }
            window2.addEventListener(PopStateEventType, handlePop);
            listener = fn;
            return ()=>{
                window2.removeEventListener(PopStateEventType, handlePop);
                listener = null;
            };
        },
        createHref (to) {
            return createHref2(window2, to);
        },
        createURL,
        encodeLocation (to) {
            var url = createURL(to);
            return {
                pathname: url.pathname,
                search: url.search,
                hash: url.hash
            };
        },
        push,
        replace: replace2,
        go (n) {
            return globalHistory.go(n);
        }
    };
    return history;
}
function createBrowserURLImpl(to) {
    var isAbsolute = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false;
    var base = "http://localhost";
    if (typeof window !== "undefined") {
        base = window.location.origin !== "null" ? window.location.origin : window.location.href;
    }
    invariant(base, "No window.location.(origin|href) available to create URL");
    var href = typeof to === "string" ? to : createPath(to);
    href = href.replace(/ $/, "%20");
    if (!isAbsolute && href.startsWith("//")) {
        href = base + href;
    }
    return new URL(href, base);
}
// lib/router/utils.ts
function createContext(defaultValue) {
    return {
        defaultValue
    };
}
var _map;
var RouterContextProvider = class RouterContextProvider {
    /**
   * Access a value from the context. If no value has been set for the context,
   * it will return the context's `defaultValue` if provided, or throw an error
   * if no `defaultValue` was set.
   * @param context The context to get the value for
   * @returns The value for the context, or the context's `defaultValue` if no
   * value was set
   */ get(context) {
        if (__privateGet(this, _map).has(context)) {
            return __privateGet(this, _map).get(context);
        }
        if (context.defaultValue !== void 0) {
            return context.defaultValue;
        }
        throw new Error("No value found for context");
    }
    /**
   * Set a value for the context. If the context already has a value set, this
   * will overwrite it.
   *
   * @param context The context to set the value for
   * @param value The value to set for the context
   * @returns {void}
   */ set(context, value) {
        __privateGet(this, _map).set(context, value);
    }
    /**
   * Create a new `RouterContextProvider` instance
   * @param init An optional initial context map to populate the provider with
   */ constructor(init){
        __privateAdd(this, _map, /* @__PURE__ */ new Map());
        if (init) {
            for (var [context, value] of init){
                this.set(context, value);
            }
        }
    }
};
_map = new WeakMap();
var unsupportedLazyRouteObjectKeys = /* @__PURE__ */ new Set([
    "lazy",
    "caseSensitive",
    "path",
    "id",
    "index",
    "children"
]);
function isUnsupportedLazyRouteObjectKey(key) {
    return unsupportedLazyRouteObjectKeys.has(key);
}
var unsupportedLazyRouteFunctionKeys = /* @__PURE__ */ new Set([
    "lazy",
    "caseSensitive",
    "path",
    "id",
    "index",
    "middleware",
    "children"
]);
function isUnsupportedLazyRouteFunctionKey(key) {
    return unsupportedLazyRouteFunctionKeys.has(key);
}
function isIndexRoute(route) {
    return route.index === true;
}
function convertRoutesToDataRoutes(routes, mapRouteProperties2) {
    var parentPath = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : [], manifest = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {}, allowInPlaceMutations = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
    return routes.map((route, index)=>{
        var treePath = [
            ...parentPath,
            String(index)
        ];
        var id = typeof route.id === "string" ? route.id : treePath.join("-");
        invariant(route.index !== true || !route.children, "Cannot specify children on an index route");
        invariant(allowInPlaceMutations || !manifest[id], 'Found a route id collision on id "'.concat(id, "\".  Route id's must be globally unique within Data Router usages"));
        if (isIndexRoute(route)) {
            var indexRoute = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, route), {
                id
            });
            manifest[id] = mergeRouteUpdates(indexRoute, mapRouteProperties2(indexRoute));
            return indexRoute;
        } else {
            var pathOrLayoutRoute = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, route), {
                id,
                children: void 0
            });
            manifest[id] = mergeRouteUpdates(pathOrLayoutRoute, mapRouteProperties2(pathOrLayoutRoute));
            if (route.children) {
                pathOrLayoutRoute.children = convertRoutesToDataRoutes(route.children, mapRouteProperties2, treePath, manifest, allowInPlaceMutations);
            }
            return pathOrLayoutRoute;
        }
    });
}
function mergeRouteUpdates(route, updates) {
    return Object.assign(route, (0,_swc_helpers_object_spread__rspack_import_2._)({}, updates, typeof updates.lazy === "object" && updates.lazy != null ? {
        lazy: (0,_swc_helpers_object_spread__rspack_import_2._)({}, route.lazy, updates.lazy)
    } : {}));
}
function matchRoutes(routes, locationArg) {
    var basename = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "/";
    return matchRoutesImpl(routes, locationArg, basename, false);
}
function matchRoutesImpl(routes, locationArg, basename, allowPartial) {
    var location = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
    var pathname = stripBasename(location.pathname || "/", basename);
    if (pathname == null) {
        return null;
    }
    var branches = flattenRoutes(routes);
    rankRouteBranches(branches);
    var matches = null;
    for(var i = 0; matches == null && i < branches.length; ++i){
        var decoded = decodePath(pathname);
        matches = matchRouteBranch(branches[i], decoded, allowPartial);
    }
    return matches;
}
function convertRouteMatchToUiMatch(match, loaderData) {
    var { route, pathname, params } = match;
    return {
        id: route.id,
        pathname,
        params,
        data: loaderData[route.id],
        loaderData: loaderData[route.id],
        handle: route.handle
    };
}
function flattenRoutes(routes) {
    var branches = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [], parentsMeta = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : [], parentPath = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "", _hasParentOptionalSegments = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
    var flattenRoute = function(route, index) {
        var hasParentOptionalSegments = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : _hasParentOptionalSegments, relativePath = arguments.length > 3 ? arguments[3] : void 0;
        var meta = {
            relativePath: relativePath === void 0 ? route.path || "" : relativePath,
            caseSensitive: route.caseSensitive === true,
            childrenIndex: index,
            route
        };
        if (meta.relativePath.startsWith("/")) {
            if (!meta.relativePath.startsWith(parentPath) && hasParentOptionalSegments) {
                return;
            }
            invariant(meta.relativePath.startsWith(parentPath), 'Absolute route path "'.concat(meta.relativePath, '" nested under path "').concat(parentPath, '" is not valid. An absolute child route path must start with the combined path of all its parent routes.'));
            meta.relativePath = meta.relativePath.slice(parentPath.length);
        }
        var path = joinPaths([
            parentPath,
            meta.relativePath
        ]);
        var routesMeta = parentsMeta.concat(meta);
        if (route.children && route.children.length > 0) {
            invariant(// Our types know better, but runtime JS may not!
            // @ts-expect-error
            route.index !== true, 'Index routes must not have child routes. Please remove all child routes from route path "'.concat(path, '".'));
            flattenRoutes(route.children, branches, routesMeta, path, hasParentOptionalSegments);
        }
        if (route.path == null && !route.index) {
            return;
        }
        branches.push({
            path,
            score: computeScore(path, route.index),
            routesMeta
        });
    };
    routes.forEach((route, index)=>{
        var _route_path;
        if (route.path === "" || !((_route_path = route.path) === null || _route_path === void 0 ? void 0 : _route_path.includes("?"))) {
            flattenRoute(route, index);
        } else {
            for (var exploded of explodeOptionalSegments(route.path)){
                flattenRoute(route, index, true, exploded);
            }
        }
    });
    return branches;
}
function explodeOptionalSegments(path) {
    var segments = path.split("/");
    if (segments.length === 0) return [];
    var [first, ...rest] = segments;
    var isOptional = first.endsWith("?");
    var required = first.replace(/\?$/, "");
    if (rest.length === 0) {
        return isOptional ? [
            required,
            ""
        ] : [
            required
        ];
    }
    var restExploded = explodeOptionalSegments(rest.join("/"));
    var result = [];
    result.push(...restExploded.map((subpath)=>subpath === "" ? required : [
            required,
            subpath
        ].join("/")));
    if (isOptional) {
        result.push(...restExploded);
    }
    return result.map((exploded)=>path.startsWith("/") && exploded === "" ? "/" : exploded);
}
function rankRouteBranches(branches) {
    branches.sort((a, b)=>a.score !== b.score ? b.score - a.score : compareIndexes(a.routesMeta.map((meta)=>meta.childrenIndex), b.routesMeta.map((meta)=>meta.childrenIndex)));
}
var paramRe = /^:[\w-]+$/;
var dynamicSegmentValue = 3;
var indexRouteValue = 2;
var emptySegmentValue = 1;
var staticSegmentValue = 10;
var splatPenalty = -2;
var isSplat = (s)=>s === "*";
function computeScore(path, index) {
    var segments = path.split("/");
    var initialScore = segments.length;
    if (segments.some(isSplat)) {
        initialScore += splatPenalty;
    }
    if (index) {
        initialScore += indexRouteValue;
    }
    return segments.filter((s)=>!isSplat(s)).reduce((score, segment)=>score + (paramRe.test(segment) ? dynamicSegmentValue : segment === "" ? emptySegmentValue : staticSegmentValue), initialScore);
}
function compareIndexes(a, b) {
    var siblings = a.length === b.length && a.slice(0, -1).every((n, i)=>n === b[i]);
    return siblings ? // If two routes are siblings, we should try to match the earlier sibling
    // first. This allows people to have fine-grained control over the matching
    // behavior by simply putting routes with identical paths in the order they
    // want them tried.
    a[a.length - 1] - b[b.length - 1] : // Otherwise, it doesn't really make sense to rank non-siblings by index,
    // so they sort equally.
    0;
}
function matchRouteBranch(branch, pathname) {
    var allowPartial = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
    var { routesMeta } = branch;
    var matchedParams = {};
    var matchedPathname = "/";
    var matches = [];
    for(var i = 0; i < routesMeta.length; ++i){
        var meta = routesMeta[i];
        var end = i === routesMeta.length - 1;
        var remainingPathname = matchedPathname === "/" ? pathname : pathname.slice(matchedPathname.length) || "/";
        var match = matchPath({
            path: meta.relativePath,
            caseSensitive: meta.caseSensitive,
            end
        }, remainingPathname);
        var route = meta.route;
        if (!match && end && allowPartial && !routesMeta[routesMeta.length - 1].route.index) {
            match = matchPath({
                path: meta.relativePath,
                caseSensitive: meta.caseSensitive,
                end: false
            }, remainingPathname);
        }
        if (!match) {
            return null;
        }
        Object.assign(matchedParams, match.params);
        matches.push({
            // TODO: Can this as be avoided?
            params: matchedParams,
            pathname: joinPaths([
                matchedPathname,
                match.pathname
            ]),
            pathnameBase: normalizePathname(joinPaths([
                matchedPathname,
                match.pathnameBase
            ])),
            route
        });
        if (match.pathnameBase !== "/") {
            matchedPathname = joinPaths([
                matchedPathname,
                match.pathnameBase
            ]);
        }
    }
    return matches;
}
function generatePath(originalPath) {
    var params = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var path = originalPath;
    if (path.endsWith("*") && path !== "*" && !path.endsWith("/*")) {
        warning(false, 'Route path "'.concat(path, '" will be treated as if it were "').concat(path.replace(/\*$/, "/*"), '" because the `*` character must always follow a `/` in the pattern. To get rid of this warning, please change the route path to "').concat(path.replace(/\*$/, "/*"), '".'));
        path = path.replace(/\*$/, "/*");
    }
    var prefix = path.startsWith("/") ? "/" : "";
    var stringify2 = (p)=>p == null ? "" : typeof p === "string" ? p : String(p);
    var segments = path.split(/\/+/).map((segment, index, array)=>{
        var isLastSegment = index === array.length - 1;
        if (isLastSegment && segment === "*") {
            var star = "*";
            return stringify2(params[star]);
        }
        var keyMatch = segment.match(/^:([\w-]+)(\??)(.*)/);
        if (keyMatch) {
            var [, key, optional, suffix] = keyMatch;
            var param = params[key];
            invariant(optional === "?" || param != null, 'Missing ":'.concat(key, '" param'));
            return encodeURIComponent(stringify2(param)) + suffix;
        }
        return segment.replace(/\?$/g, "");
    }).filter((segment)=>!!segment);
    return prefix + segments.join("/");
}
function matchPath(pattern, pathname) {
    if (typeof pattern === "string") {
        pattern = {
            path: pattern,
            caseSensitive: false,
            end: true
        };
    }
    var [matcher, compiledParams] = compilePath(pattern.path, pattern.caseSensitive, pattern.end);
    var match = pathname.match(matcher);
    if (!match) return null;
    var matchedPathname = match[0];
    var pathnameBase = matchedPathname.replace(/(.)\/+$/, "$1");
    var captureGroups = match.slice(1);
    var params = compiledParams.reduce((memo2, param, index)=>{
        var { paramName, isOptional } = param;
        if (paramName === "*") {
            var splatValue = captureGroups[index] || "";
            pathnameBase = matchedPathname.slice(0, matchedPathname.length - splatValue.length).replace(/(.)\/+$/, "$1");
        }
        var value = captureGroups[index];
        if (isOptional && !value) {
            memo2[paramName] = void 0;
        } else {
            memo2[paramName] = (value || "").replace(/%2F/g, "/");
        }
        return memo2;
    }, {});
    return {
        params,
        pathname: matchedPathname,
        pathnameBase,
        pattern
    };
}
function compilePath(path) {
    var caseSensitive = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false, end = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
    warning(path === "*" || !path.endsWith("*") || path.endsWith("/*"), 'Route path "'.concat(path, '" will be treated as if it were "').concat(path.replace(/\*$/, "/*"), '" because the `*` character must always follow a `/` in the pattern. To get rid of this warning, please change the route path to "').concat(path.replace(/\*$/, "/*"), '".'));
    var params = [];
    var regexpSource = "^" + path.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(/\/:([\w-]+)(\?)?/g, (_, paramName, isOptional)=>{
        params.push({
            paramName,
            isOptional: isOptional != null
        });
        return isOptional ? "/?([^\\/]+)?" : "/([^\\/]+)";
    }).replace(/\/([\w-]+)\?(\/|$)/g, "(/$1)?$2");
    if (path.endsWith("*")) {
        params.push({
            paramName: "*"
        });
        regexpSource += path === "*" || path === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$";
    } else if (end) {
        regexpSource += "\\/*$";
    } else if (path !== "" && path !== "/") {
        regexpSource += "(?:(?=\\/|$))";
    } else {}
    var matcher = new RegExp(regexpSource, caseSensitive ? void 0 : "i");
    return [
        matcher,
        params
    ];
}
function decodePath(value) {
    try {
        return value.split("/").map((v)=>decodeURIComponent(v).replace(/\//g, "%2F")).join("/");
    } catch (error) {
        warning(false, 'The URL path "'.concat(value, '" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (').concat(error, ")."));
        return value;
    }
}
function stripBasename(pathname, basename) {
    if (basename === "/") return pathname;
    if (!pathname.toLowerCase().startsWith(basename.toLowerCase())) {
        return null;
    }
    var startIndex = basename.endsWith("/") ? basename.length - 1 : basename.length;
    var nextChar = pathname.charAt(startIndex);
    if (nextChar && nextChar !== "/") {
        return null;
    }
    return pathname.slice(startIndex) || "/";
}
function prependBasename(param) {
    var { basename, pathname } = param;
    return pathname === "/" ? basename : joinPaths([
        basename,
        pathname
    ]);
}
var ABSOLUTE_URL_REGEX = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
var isAbsoluteUrl = (url)=>ABSOLUTE_URL_REGEX.test(url);
function resolvePath(to) {
    var fromPathname = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "/";
    var { pathname: toPathname, search = "", hash = "" } = typeof to === "string" ? parsePath(to) : to;
    var pathname;
    if (toPathname) {
        toPathname = toPathname.replace(/\/\/+/g, "/");
        if (toPathname.startsWith("/")) {
            pathname = resolvePathname(toPathname.substring(1), "/");
        } else {
            pathname = resolvePathname(toPathname, fromPathname);
        }
    } else {
        pathname = fromPathname;
    }
    return {
        pathname,
        search: normalizeSearch(search),
        hash: normalizeHash(hash)
    };
}
function resolvePathname(relativePath, fromPathname) {
    var segments = fromPathname.replace(/\/+$/, "").split("/");
    var relativeSegments = relativePath.split("/");
    relativeSegments.forEach((segment)=>{
        if (segment === "..") {
            if (segments.length > 1) segments.pop();
        } else if (segment !== ".") {
            segments.push(segment);
        }
    });
    return segments.length > 1 ? segments.join("/") : "/";
}
function getInvalidPathError(char, field, dest, path) {
    return "Cannot include a '".concat(char, "' character in a manually specified `to.").concat(field, "` field [").concat(JSON.stringify(path), "].  Please separate it out to the `to.").concat(dest, '` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.');
}
function getPathContributingMatches(matches) {
    return matches.filter((match, index)=>index === 0 || match.route.path && match.route.path.length > 0);
}
function getResolveToMatches(matches) {
    var pathMatches = getPathContributingMatches(matches);
    return pathMatches.map((match, idx)=>idx === pathMatches.length - 1 ? match.pathname : match.pathnameBase);
}
function resolveTo(toArg, routePathnames, locationPathname) {
    var isPathRelative = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
    var to;
    if (typeof toArg === "string") {
        to = parsePath(toArg);
    } else {
        to = (0,_swc_helpers_object_spread__rspack_import_2._)({}, toArg);
        invariant(!to.pathname || !to.pathname.includes("?"), getInvalidPathError("?", "pathname", "search", to));
        invariant(!to.pathname || !to.pathname.includes("#"), getInvalidPathError("#", "pathname", "hash", to));
        invariant(!to.search || !to.search.includes("#"), getInvalidPathError("#", "search", "hash", to));
    }
    var isEmptyPath = toArg === "" || to.pathname === "";
    var toPathname = isEmptyPath ? "/" : to.pathname;
    var from;
    if (toPathname == null) {
        from = locationPathname;
    } else {
        var routePathnameIndex = routePathnames.length - 1;
        if (!isPathRelative && toPathname.startsWith("..")) {
            var toSegments = toPathname.split("/");
            while(toSegments[0] === ".."){
                toSegments.shift();
                routePathnameIndex -= 1;
            }
            to.pathname = toSegments.join("/");
        }
        from = routePathnameIndex >= 0 ? routePathnames[routePathnameIndex] : "/";
    }
    var path = resolvePath(to, from);
    var hasExplicitTrailingSlash = toPathname && toPathname !== "/" && toPathname.endsWith("/");
    var hasCurrentTrailingSlash = (isEmptyPath || toPathname === ".") && locationPathname.endsWith("/");
    if (!path.pathname.endsWith("/") && (hasExplicitTrailingSlash || hasCurrentTrailingSlash)) {
        path.pathname += "/";
    }
    return path;
}
var joinPaths = (paths)=>paths.join("/").replace(/\/\/+/g, "/");
var normalizePathname = (pathname)=>pathname.replace(/\/+$/, "").replace(/^\/*/, "/");
var normalizeSearch = (search)=>!search || search === "?" ? "" : search.startsWith("?") ? search : "?" + search;
var normalizeHash = (hash)=>!hash || hash === "#" ? "" : hash.startsWith("#") ? hash : "#" + hash;
var DataWithResponseInit = class DataWithResponseInit {
    constructor(data2, init){
        this.type = "DataWithResponseInit";
        this.data = data2;
        this.init = init || null;
    }
};
function data(data2, init) {
    return new DataWithResponseInit(data2, typeof init === "number" ? {
        status: init
    } : init);
}
var redirect = function(url) {
    var init = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 302;
    var responseInit = init;
    if (typeof responseInit === "number") {
        responseInit = {
            status: responseInit
        };
    } else if (typeof responseInit.status === "undefined") {
        responseInit.status = 302;
    }
    var headers = new Headers(responseInit.headers);
    headers.set("Location", url);
    return new Response(null, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, responseInit), {
        headers
    }));
};
var redirectDocument = (url, init)=>{
    var response = redirect(url, init);
    response.headers.set("X-Remix-Reload-Document", "true");
    return response;
};
var replace = (url, init)=>{
    var response = redirect(url, init);
    response.headers.set("X-Remix-Replace", "true");
    return response;
};
var ErrorResponseImpl = class ErrorResponseImpl {
    constructor(status, statusText, data2, internal = false){
        this.status = status;
        this.statusText = statusText || "";
        this.internal = internal;
        if (data2 instanceof Error) {
            this.data = data2.toString();
            this.error = data2;
        } else {
            this.data = data2;
        }
    }
};
function isRouteErrorResponse(error) {
    return error != null && typeof error.status === "number" && typeof error.statusText === "string" && typeof error.internal === "boolean" && "data" in error;
}
function getRoutePattern(matches) {
    return matches.map((m)=>m.route.path).filter(Boolean).join("/").replace(/\/\/*/g, "/") || "/";
}
var isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
function parseToInfo(_to, basename) {
    var to = _to;
    if (typeof to !== "string" || !ABSOLUTE_URL_REGEX.test(to)) {
        return {
            absoluteURL: void 0,
            isExternal: false,
            to
        };
    }
    var absoluteURL = to;
    var isExternal = false;
    if (isBrowser) {
        try {
            var currentUrl = new URL(window.location.href);
            var targetUrl = to.startsWith("//") ? new URL(currentUrl.protocol + to) : new URL(to);
            var path = stripBasename(targetUrl.pathname, basename);
            if (targetUrl.origin === currentUrl.origin && path != null) {
                to = path + targetUrl.search + targetUrl.hash;
            } else {
                isExternal = true;
            }
        } catch (e) {
            warning(false, '<Link to="'.concat(to, '"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.'));
        }
    }
    return {
        absoluteURL,
        isExternal,
        to
    };
}
// lib/router/instrumentation.ts
var UninstrumentedSymbol = Symbol("Uninstrumented");
function getRouteInstrumentationUpdates(fns, route) {
    var aggregated = {
        lazy: [],
        "lazy.loader": [],
        "lazy.action": [],
        "lazy.middleware": [],
        middleware: [],
        loader: [],
        action: []
    };
    fns.forEach((fn)=>fn({
            id: route.id,
            index: route.index,
            path: route.path,
            instrument (i) {
                var keys = Object.keys(aggregated);
                for (var key of keys){
                    if (i[key]) {
                        aggregated[key].push(i[key]);
                    }
                }
            }
        }));
    var updates = {};
    if (typeof route.lazy === "function" && aggregated.lazy.length > 0) {
        var instrumented = wrapImpl(aggregated.lazy, route.lazy, ()=>void 0);
        if (instrumented) {
            updates.lazy = instrumented;
        }
    }
    if (typeof route.lazy === "object") {
        var lazyObject = route.lazy;
        [
            "middleware",
            "loader",
            "action"
        ].forEach((key)=>{
            var lazyFn = lazyObject[key];
            var instrumentations = aggregated["lazy.".concat(key)];
            if (typeof lazyFn === "function" && instrumentations.length > 0) {
                var instrumented = wrapImpl(instrumentations, lazyFn, ()=>void 0);
                if (instrumented) {
                    updates.lazy = Object.assign(updates.lazy || {}, {
                        [key]: instrumented
                    });
                }
            }
        });
    }
    [
        "loader",
        "action"
    ].forEach((key)=>{
        var handler = route[key];
        if (typeof handler === "function" && aggregated[key].length > 0) {
            var _handler_UninstrumentedSymbol;
            var original = (_handler_UninstrumentedSymbol = handler[UninstrumentedSymbol]) !== null && _handler_UninstrumentedSymbol !== void 0 ? _handler_UninstrumentedSymbol : handler;
            var instrumented = wrapImpl(aggregated[key], original, function() {
                for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                    args[_key] = arguments[_key];
                }
                return getHandlerInfo(args[0]);
            });
            if (instrumented) {
                if (key === "loader" && original.hydrate === true) {
                    instrumented.hydrate = true;
                }
                instrumented[UninstrumentedSymbol] = original;
                updates[key] = instrumented;
            }
        }
    });
    if (route.middleware && route.middleware.length > 0 && aggregated.middleware.length > 0) {
        updates.middleware = route.middleware.map((middleware)=>{
            var _middleware_UninstrumentedSymbol;
            var original = (_middleware_UninstrumentedSymbol = middleware[UninstrumentedSymbol]) !== null && _middleware_UninstrumentedSymbol !== void 0 ? _middleware_UninstrumentedSymbol : middleware;
            var instrumented = wrapImpl(aggregated.middleware, original, function() {
                for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                    args[_key] = arguments[_key];
                }
                return getHandlerInfo(args[0]);
            });
            if (instrumented) {
                instrumented[UninstrumentedSymbol] = original;
                return instrumented;
            }
            return middleware;
        });
    }
    return updates;
}
function instrumentClientSideRouter(router, fns) {
    var aggregated = {
        navigate: [],
        fetch: []
    };
    fns.forEach((fn)=>fn({
            instrument (i) {
                var keys = Object.keys(i);
                for (var key of keys){
                    if (i[key]) {
                        aggregated[key].push(i[key]);
                    }
                }
            }
        }));
    if (aggregated.navigate.length > 0) {
        var _router_navigate_UninstrumentedSymbol;
        var navigate = (_router_navigate_UninstrumentedSymbol = router.navigate[UninstrumentedSymbol]) !== null && _router_navigate_UninstrumentedSymbol !== void 0 ? _router_navigate_UninstrumentedSymbol : router.navigate;
        var instrumentedNavigate = wrapImpl(aggregated.navigate, navigate, function() {
            for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                args[_key] = arguments[_key];
            }
            var [to, opts] = args;
            return (0,_swc_helpers_object_spread__rspack_import_2._)({
                to: typeof to === "number" || typeof to === "string" ? to : to ? createPath(to) : "."
            }, getRouterInfo(router, opts !== null && opts !== void 0 ? opts : {}));
        });
        if (instrumentedNavigate) {
            instrumentedNavigate[UninstrumentedSymbol] = navigate;
            router.navigate = instrumentedNavigate;
        }
    }
    if (aggregated.fetch.length > 0) {
        var _router_fetch_UninstrumentedSymbol;
        var fetch2 = (_router_fetch_UninstrumentedSymbol = router.fetch[UninstrumentedSymbol]) !== null && _router_fetch_UninstrumentedSymbol !== void 0 ? _router_fetch_UninstrumentedSymbol : router.fetch;
        var instrumentedFetch = wrapImpl(aggregated.fetch, fetch2, function() {
            for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                args[_key] = arguments[_key];
            }
            var [key, , href, opts] = args;
            return (0,_swc_helpers_object_spread__rspack_import_2._)({
                href: href !== null && href !== void 0 ? href : ".",
                fetcherKey: key
            }, getRouterInfo(router, opts !== null && opts !== void 0 ? opts : {}));
        });
        if (instrumentedFetch) {
            instrumentedFetch[UninstrumentedSymbol] = fetch2;
            router.fetch = instrumentedFetch;
        }
    }
    return router;
}
function instrumentHandler(handler, fns) {
    var aggregated = {
        request: []
    };
    fns.forEach((fn)=>fn({
            instrument (i) {
                var keys = Object.keys(i);
                for (var key of keys){
                    if (i[key]) {
                        aggregated[key].push(i[key]);
                    }
                }
            }
        }));
    var instrumentedHandler = handler;
    if (aggregated.request.length > 0) {
        instrumentedHandler = wrapImpl(aggregated.request, handler, function() {
            for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                args[_key] = arguments[_key];
            }
            var [request, context] = args;
            return {
                request: getReadonlyRequest(request),
                context: context != null ? getReadonlyContext(context) : context
            };
        });
    }
    return instrumentedHandler;
}
function wrapImpl(impls, handler, getInfo) {
    if (impls.length === 0) {
        return null;
    }
    return function() {
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
            args[_key] = arguments[_key];
        }
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var result = yield recurseRight(impls, getInfo(...args), ()=>handler(...args), impls.length - 1);
            if (result.type === "error") {
                throw result.value;
            }
            return result.value;
        })();
    };
}
function recurseRight(impls, info, handler, index) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var impl = impls[index];
        var result;
        if (!impl) {
            try {
                var value = yield handler();
                result = {
                    type: "success",
                    value
                };
            } catch (e) {
                result = {
                    type: "error",
                    value: e
                };
            }
        } else {
            var handlerPromise = void 0;
            var callHandler = ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                    if (handlerPromise) {
                        console.error("You cannot call instrumented handlers more than once");
                    } else {
                        handlerPromise = recurseRight(impls, info, handler, index - 1);
                    }
                    result = yield handlerPromise;
                    invariant(result, "Expected a result");
                    if (result.type === "error" && result.value instanceof Error) {
                        return {
                            status: "error",
                            error: result.value
                        };
                    }
                    return {
                        status: "success",
                        error: void 0
                    };
                })();
            try {
                yield impl(callHandler, info);
            } catch (e) {
                console.error("An instrumentation function threw an error:", e);
            }
            if (!handlerPromise) {
                yield callHandler();
            }
            yield handlerPromise;
        }
        if (result) {
            return result;
        }
        return {
            type: "error",
            value: new Error("No result assigned in instrumentation chain.")
        };
    })();
}
function getHandlerInfo(args) {
    var { request, context, params, unstable_pattern } = args;
    return {
        request: getReadonlyRequest(request),
        params: (0,_swc_helpers_object_spread__rspack_import_2._)({}, params),
        unstable_pattern,
        context: getReadonlyContext(context)
    };
}
function getRouterInfo(router, opts) {
    return (0,_swc_helpers_object_spread__rspack_import_2._)({
        currentUrl: createPath(router.state.location)
    }, "formMethod" in opts ? {
        formMethod: opts.formMethod
    } : {}, "formEncType" in opts ? {
        formEncType: opts.formEncType
    } : {}, "formData" in opts ? {
        formData: opts.formData
    } : {}, "body" in opts ? {
        body: opts.body
    } : {});
}
function getReadonlyRequest(request) {
    return {
        method: request.method,
        url: request.url,
        headers: {
            get: function() {
                for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                    args[_key] = arguments[_key];
                }
                return request.headers.get(...args);
            }
        }
    };
}
function getReadonlyContext(context) {
    if (isPlainObject(context)) {
        var frozen = (0,_swc_helpers_object_spread__rspack_import_2._)({}, context);
        Object.freeze(frozen);
        return frozen;
    } else {
        return {
            get: (ctx)=>context.get(ctx)
        };
    }
}
var objectProtoNames = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
function isPlainObject(thing) {
    if (thing === null || typeof thing !== "object") {
        return false;
    }
    var proto = Object.getPrototypeOf(thing);
    return proto === Object.prototype || proto === null || Object.getOwnPropertyNames(proto).sort().join("\0") === objectProtoNames;
}
// lib/router/router.ts
var validMutationMethodsArr = [
    "POST",
    "PUT",
    "PATCH",
    "DELETE"
];
var validMutationMethods = new Set(validMutationMethodsArr);
var validRequestMethodsArr = [
    "GET",
    ...validMutationMethodsArr
];
var validRequestMethods = new Set(validRequestMethodsArr);
var redirectStatusCodes = /* @__PURE__ */ new Set([
    301,
    302,
    303,
    307,
    308
]);
var redirectPreserveMethodStatusCodes = /* @__PURE__ */ new Set([
    307,
    308
]);
var IDLE_NAVIGATION = {
    state: "idle",
    location: void 0,
    formMethod: void 0,
    formAction: void 0,
    formEncType: void 0,
    formData: void 0,
    json: void 0,
    text: void 0
};
var IDLE_FETCHER = {
    state: "idle",
    data: void 0,
    formMethod: void 0,
    formAction: void 0,
    formEncType: void 0,
    formData: void 0,
    json: void 0,
    text: void 0
};
var IDLE_BLOCKER = {
    state: "unblocked",
    proceed: void 0,
    reset: void 0,
    location: void 0
};
var defaultMapRouteProperties = (route)=>({
        hasErrorBoundary: Boolean(route.hasErrorBoundary)
    });
var TRANSITIONS_STORAGE_KEY = "remix-router-transitions";
var ResetLoaderDataSymbol = Symbol("ResetLoaderData");
function createRouter(init) {
    var routerWindow = init.window ? init.window : typeof window !== "undefined" ? window : void 0;
    var isBrowser3 = typeof routerWindow !== "undefined" && typeof routerWindow.document !== "undefined" && typeof routerWindow.document.createElement !== "undefined";
    invariant(init.routes.length > 0, "You must provide a non-empty routes array to createRouter");
    var hydrationRouteProperties2 = init.hydrationRouteProperties || [];
    var _mapRouteProperties = init.mapRouteProperties || defaultMapRouteProperties;
    var mapRouteProperties2 = _mapRouteProperties;
    if (init.unstable_instrumentations) {
        var instrumentations = init.unstable_instrumentations;
        mapRouteProperties2 = (route)=>{
            return (0,_swc_helpers_object_spread__rspack_import_2._)({}, _mapRouteProperties(route), getRouteInstrumentationUpdates(instrumentations.map((i)=>i.route).filter(Boolean), route));
        };
    }
    var manifest = {};
    var dataRoutes = convertRoutesToDataRoutes(init.routes, mapRouteProperties2, void 0, manifest);
    var inFlightDataRoutes;
    var basename = init.basename || "/";
    if (!basename.startsWith("/")) {
        basename = "/".concat(basename);
    }
    var dataStrategyImpl = init.dataStrategy || defaultDataStrategyWithMiddleware;
    var future = (0,_swc_helpers_object_spread__rspack_import_2._)({}, init.future);
    var unlistenHistory = null;
    var subscribers = /* @__PURE__ */ new Set();
    var savedScrollPositions2 = null;
    var getScrollRestorationKey2 = null;
    var getScrollPosition = null;
    var initialScrollRestored = init.hydrationData != null;
    var initialMatches = matchRoutes(dataRoutes, init.history.location, basename);
    var initialMatchesIsFOW = false;
    var initialErrors = null;
    var initialized;
    if (initialMatches == null && !init.patchRoutesOnNavigation) {
        var error = getInternalRouterError(404, {
            pathname: init.history.location.pathname
        });
        var { matches, route } = getShortCircuitMatches(dataRoutes);
        initialized = true;
        initialMatches = matches;
        initialErrors = {
            [route.id]: error
        };
    } else {
        if (initialMatches && !init.hydrationData) {
            var fogOfWar = checkFogOfWar(initialMatches, dataRoutes, init.history.location.pathname);
            if (fogOfWar.active) {
                initialMatches = null;
            }
        }
        if (!initialMatches) {
            initialized = false;
            initialMatches = [];
            var fogOfWar1 = checkFogOfWar(null, dataRoutes, init.history.location.pathname);
            if (fogOfWar1.active && fogOfWar1.matches) {
                initialMatchesIsFOW = true;
                initialMatches = fogOfWar1.matches;
            }
        } else if (initialMatches.some((m)=>m.route.lazy)) {
            initialized = false;
        } else if (!initialMatches.some((m)=>routeHasLoaderOrMiddleware(m.route))) {
            initialized = true;
        } else {
            var loaderData = init.hydrationData ? init.hydrationData.loaderData : null;
            var errors = init.hydrationData ? init.hydrationData.errors : null;
            if (errors) {
                var idx = initialMatches.findIndex((m)=>errors[m.route.id] !== void 0);
                initialized = initialMatches.slice(0, idx + 1).every((m)=>!shouldLoadRouteOnHydration(m.route, loaderData, errors));
            } else {
                initialized = initialMatches.every((m)=>!shouldLoadRouteOnHydration(m.route, loaderData, errors));
            }
        }
    }
    var router;
    var state = {
        historyAction: init.history.action,
        location: init.history.location,
        matches: initialMatches,
        initialized,
        navigation: IDLE_NAVIGATION,
        // Don't restore on initial updateState() if we were SSR'd
        restoreScrollPosition: init.hydrationData != null ? false : null,
        preventScrollReset: false,
        revalidation: "idle",
        loaderData: init.hydrationData && init.hydrationData.loaderData || {},
        actionData: init.hydrationData && init.hydrationData.actionData || null,
        errors: init.hydrationData && init.hydrationData.errors || initialErrors,
        fetchers: /* @__PURE__ */ new Map(),
        blockers: /* @__PURE__ */ new Map()
    };
    var pendingAction = "POP" /* Pop */ ;
    var pendingPopstateNavigationDfd = null;
    var pendingPreventScrollReset = false;
    var pendingNavigationController;
    var pendingViewTransitionEnabled = false;
    var appliedViewTransitions = /* @__PURE__ */ new Map();
    var removePageHideEventListener = null;
    var isUninterruptedRevalidation = false;
    var isRevalidationRequired = false;
    var cancelledFetcherLoads = /* @__PURE__ */ new Set();
    var fetchControllers = /* @__PURE__ */ new Map();
    var incrementingLoadId = 0;
    var pendingNavigationLoadId = -1;
    var fetchReloadIds = /* @__PURE__ */ new Map();
    var fetchRedirectIds = /* @__PURE__ */ new Set();
    var fetchLoadMatches = /* @__PURE__ */ new Map();
    var activeFetchers = /* @__PURE__ */ new Map();
    var fetchersQueuedForDeletion = /* @__PURE__ */ new Set();
    var blockerFunctions = /* @__PURE__ */ new Map();
    var unblockBlockerHistoryUpdate = void 0;
    var pendingRevalidationDfd = null;
    function initialize() {
        unlistenHistory = init.history.listen((param)=>{
            var { action: historyAction, location, delta } = param;
            if (unblockBlockerHistoryUpdate) {
                unblockBlockerHistoryUpdate();
                unblockBlockerHistoryUpdate = void 0;
                return;
            }
            warning(blockerFunctions.size === 0 || delta != null, "You are trying to use a blocker on a POP navigation to a location that was not created by @remix-run/router. This will fail silently in production. This can happen if you are navigating outside the router via `window.history.pushState`/`window.location.hash` instead of using router navigation APIs.  This can also happen if you are using createHashRouter and the user manually changes the URL.");
            var blockerKey = shouldBlockNavigation({
                currentLocation: state.location,
                nextLocation: location,
                historyAction
            });
            if (blockerKey && delta != null) {
                var nextHistoryUpdatePromise = new Promise((resolve)=>{
                    unblockBlockerHistoryUpdate = resolve;
                });
                init.history.go(delta * -1);
                updateBlocker(blockerKey, {
                    state: "blocked",
                    location,
                    proceed () {
                        updateBlocker(blockerKey, {
                            state: "proceeding",
                            proceed: void 0,
                            reset: void 0,
                            location
                        });
                        nextHistoryUpdatePromise.then(()=>init.history.go(delta));
                    },
                    reset () {
                        var blockers = new Map(state.blockers);
                        blockers.set(blockerKey, IDLE_BLOCKER);
                        updateState({
                            blockers
                        });
                    }
                });
                pendingPopstateNavigationDfd === null || pendingPopstateNavigationDfd === void 0 ? void 0 : pendingPopstateNavigationDfd.resolve();
                pendingPopstateNavigationDfd = null;
                return;
            }
            return startNavigation(historyAction, location);
        });
        if (isBrowser3) {
            restoreAppliedTransitions(routerWindow, appliedViewTransitions);
            var _saveAppliedTransitions = ()=>persistAppliedTransitions(routerWindow, appliedViewTransitions);
            routerWindow.addEventListener("pagehide", _saveAppliedTransitions);
            removePageHideEventListener = ()=>routerWindow.removeEventListener("pagehide", _saveAppliedTransitions);
        }
        if (!state.initialized) {
            startNavigation("POP" /* Pop */ , state.location, {
                initialHydration: true
            });
        }
        return router;
    }
    function dispose() {
        if (unlistenHistory) {
            unlistenHistory();
        }
        if (removePageHideEventListener) {
            removePageHideEventListener();
        }
        subscribers.clear();
        pendingNavigationController && pendingNavigationController.abort();
        state.fetchers.forEach((_, key)=>deleteFetcher(key));
        state.blockers.forEach((_, key)=>deleteBlocker(key));
    }
    function subscribe(fn) {
        subscribers.add(fn);
        return ()=>subscribers.delete(fn);
    }
    function updateState(newState) {
        var opts = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        if (newState.matches) {
            newState.matches = newState.matches.map((m)=>{
                var route = manifest[m.route.id];
                var matchRoute = m.route;
                if (matchRoute.element !== route.element || matchRoute.errorElement !== route.errorElement || matchRoute.hydrateFallbackElement !== route.hydrateFallbackElement) {
                    return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, m), {
                        route
                    });
                }
                return m;
            });
        }
        state = (0,_swc_helpers_object_spread__rspack_import_2._)({}, state, newState);
        var unmountedFetchers = [];
        var mountedFetchers = [];
        state.fetchers.forEach((fetcher, key)=>{
            if (fetcher.state === "idle") {
                if (fetchersQueuedForDeletion.has(key)) {
                    unmountedFetchers.push(key);
                } else {
                    mountedFetchers.push(key);
                }
            }
        });
        fetchersQueuedForDeletion.forEach((key)=>{
            if (!state.fetchers.has(key) && !fetchControllers.has(key)) {
                unmountedFetchers.push(key);
            }
        });
        [
            ...subscribers
        ].forEach((subscriber)=>{
            var _newState_errors;
            return subscriber(state, {
                deletedFetchers: unmountedFetchers,
                newErrors: (_newState_errors = newState.errors) !== null && _newState_errors !== void 0 ? _newState_errors : null,
                viewTransitionOpts: opts.viewTransitionOpts,
                flushSync: opts.flushSync === true
            });
        });
        unmountedFetchers.forEach((key)=>deleteFetcher(key));
        mountedFetchers.forEach((key)=>state.fetchers.delete(key));
    }
    function completeNavigation(location, newState) {
        var { flushSync } = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
        var _location_state, _location_state1;
        var isActionReload = state.actionData != null && state.navigation.formMethod != null && isMutationMethod(state.navigation.formMethod) && state.navigation.state === "loading" && ((_location_state = location.state) === null || _location_state === void 0 ? void 0 : _location_state._isRedirect) !== true;
        var actionData;
        if (newState.actionData) {
            if (Object.keys(newState.actionData).length > 0) {
                actionData = newState.actionData;
            } else {
                actionData = null;
            }
        } else if (isActionReload) {
            actionData = state.actionData;
        } else {
            actionData = null;
        }
        var loaderData = newState.loaderData ? mergeLoaderData(state.loaderData, newState.loaderData, newState.matches || [], newState.errors) : state.loaderData;
        var blockers = state.blockers;
        if (blockers.size > 0) {
            blockers = new Map(blockers);
            blockers.forEach((_, k)=>blockers.set(k, IDLE_BLOCKER));
        }
        var restoreScrollPosition = isUninterruptedRevalidation ? false : getSavedScrollPosition(location, newState.matches || state.matches);
        var preventScrollReset = pendingPreventScrollReset === true || state.navigation.formMethod != null && isMutationMethod(state.navigation.formMethod) && ((_location_state1 = location.state) === null || _location_state1 === void 0 ? void 0 : _location_state1._isRedirect) !== true;
        if (inFlightDataRoutes) {
            dataRoutes = inFlightDataRoutes;
            inFlightDataRoutes = void 0;
        }
        if (isUninterruptedRevalidation) {} else if (pendingAction === "POP" /* Pop */ ) {} else if (pendingAction === "PUSH" /* Push */ ) {
            init.history.push(location, location.state);
        } else if (pendingAction === "REPLACE" /* Replace */ ) {
            init.history.replace(location, location.state);
        }
        var viewTransitionOpts;
        if (pendingAction === "POP" /* Pop */ ) {
            var priorPaths = appliedViewTransitions.get(state.location.pathname);
            if (priorPaths && priorPaths.has(location.pathname)) {
                viewTransitionOpts = {
                    currentLocation: state.location,
                    nextLocation: location
                };
            } else if (appliedViewTransitions.has(location.pathname)) {
                viewTransitionOpts = {
                    currentLocation: location,
                    nextLocation: state.location
                };
            }
        } else if (pendingViewTransitionEnabled) {
            var toPaths = appliedViewTransitions.get(state.location.pathname);
            if (toPaths) {
                toPaths.add(location.pathname);
            } else {
                toPaths = /* @__PURE__ */ new Set([
                    location.pathname
                ]);
                appliedViewTransitions.set(state.location.pathname, toPaths);
            }
            viewTransitionOpts = {
                currentLocation: state.location,
                nextLocation: location
            };
        }
        updateState((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, newState), {
            // matches, errors, fetchers go through as-is
            actionData,
            loaderData,
            historyAction: pendingAction,
            location,
            initialized: true,
            navigation: IDLE_NAVIGATION,
            revalidation: "idle",
            restoreScrollPosition,
            preventScrollReset,
            blockers
        }), {
            viewTransitionOpts,
            flushSync: flushSync === true
        });
        pendingAction = "POP" /* Pop */ ;
        pendingPreventScrollReset = false;
        pendingViewTransitionEnabled = false;
        isUninterruptedRevalidation = false;
        isRevalidationRequired = false;
        pendingPopstateNavigationDfd === null || pendingPopstateNavigationDfd === void 0 ? void 0 : pendingPopstateNavigationDfd.resolve();
        pendingPopstateNavigationDfd = null;
        pendingRevalidationDfd === null || pendingRevalidationDfd === void 0 ? void 0 : pendingRevalidationDfd.resolve();
        pendingRevalidationDfd = null;
    }
    function navigate(to, opts) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            pendingPopstateNavigationDfd === null || pendingPopstateNavigationDfd === void 0 ? void 0 : pendingPopstateNavigationDfd.resolve();
            pendingPopstateNavigationDfd = null;
            if (typeof to === "number") {
                if (!pendingPopstateNavigationDfd) {
                    pendingPopstateNavigationDfd = createDeferred();
                }
                var promise = pendingPopstateNavigationDfd.promise;
                init.history.go(to);
                return promise;
            }
            var normalizedPath = normalizeTo(state.location, state.matches, basename, to, opts === null || opts === void 0 ? void 0 : opts.fromRouteId, opts === null || opts === void 0 ? void 0 : opts.relative);
            var { path, submission, error } = normalizeNavigateOptions(false, normalizedPath, opts);
            var currentLocation = state.location;
            var nextLocation = createLocation(state.location, path, opts && opts.state);
            nextLocation = (0,_swc_helpers_object_spread__rspack_import_2._)({}, nextLocation, init.history.encodeLocation(nextLocation));
            var userReplace = opts && opts.replace != null ? opts.replace : void 0;
            var historyAction = "PUSH" /* Push */ ;
            if (userReplace === true) {
                historyAction = "REPLACE" /* Replace */ ;
            } else if (userReplace === false) {} else if (submission != null && isMutationMethod(submission.formMethod) && submission.formAction === state.location.pathname + state.location.search) {
                historyAction = "REPLACE" /* Replace */ ;
            }
            var preventScrollReset = opts && "preventScrollReset" in opts ? opts.preventScrollReset === true : void 0;
            var flushSync = (opts && opts.flushSync) === true;
            var blockerKey = shouldBlockNavigation({
                currentLocation,
                nextLocation,
                historyAction
            });
            if (blockerKey) {
                updateBlocker(blockerKey, {
                    state: "blocked",
                    location: nextLocation,
                    proceed () {
                        updateBlocker(blockerKey, {
                            state: "proceeding",
                            proceed: void 0,
                            reset: void 0,
                            location: nextLocation
                        });
                        navigate(to, opts);
                    },
                    reset () {
                        var blockers = new Map(state.blockers);
                        blockers.set(blockerKey, IDLE_BLOCKER);
                        updateState({
                            blockers
                        });
                    }
                });
                return;
            }
            yield startNavigation(historyAction, nextLocation, {
                submission,
                // Send through the formData serialization error if we have one so we can
                // render at the right error boundary after we match routes
                pendingError: error,
                preventScrollReset,
                replace: opts && opts.replace,
                enableViewTransition: opts && opts.viewTransition,
                flushSync,
                callSiteDefaultShouldRevalidate: opts && opts.unstable_defaultShouldRevalidate
            });
        })();
    }
    function revalidate() {
        if (!pendingRevalidationDfd) {
            pendingRevalidationDfd = createDeferred();
        }
        interruptActiveLoads();
        updateState({
            revalidation: "loading"
        });
        var promise = pendingRevalidationDfd.promise;
        if (state.navigation.state === "submitting") {
            return promise;
        }
        if (state.navigation.state === "idle") {
            startNavigation(state.historyAction, state.location, {
                startUninterruptedRevalidation: true
            });
            return promise;
        }
        startNavigation(pendingAction || state.historyAction, state.navigation.location, {
            overrideNavigation: state.navigation,
            // Proxy through any rending view transition
            enableViewTransition: pendingViewTransitionEnabled === true
        });
        return promise;
    }
    function startNavigation(historyAction, location, opts) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            pendingNavigationController && pendingNavigationController.abort();
            pendingNavigationController = null;
            pendingAction = historyAction;
            isUninterruptedRevalidation = (opts && opts.startUninterruptedRevalidation) === true;
            saveScrollPosition(state.location, state.matches);
            pendingPreventScrollReset = (opts && opts.preventScrollReset) === true;
            pendingViewTransitionEnabled = (opts && opts.enableViewTransition) === true;
            var routesToUse = inFlightDataRoutes || dataRoutes;
            var loadingNavigation = opts && opts.overrideNavigation;
            var matches = (opts === null || opts === void 0 ? void 0 : opts.initialHydration) && state.matches && state.matches.length > 0 && !initialMatchesIsFOW ? // `matchRoutes()` has already been called if we're in here via `router.initialize()`
            state.matches : matchRoutes(routesToUse, location, basename);
            var flushSync = (opts && opts.flushSync) === true;
            if (matches && state.initialized && !isRevalidationRequired && isHashChangeOnly(state.location, location) && !(opts && opts.submission && isMutationMethod(opts.submission.formMethod))) {
                completeNavigation(location, {
                    matches
                }, {
                    flushSync
                });
                return;
            }
            var fogOfWar = checkFogOfWar(matches, routesToUse, location.pathname);
            if (fogOfWar.active && fogOfWar.matches) {
                matches = fogOfWar.matches;
            }
            if (!matches) {
                var { error, notFoundMatches, route } = handleNavigational404(location.pathname);
                completeNavigation(location, {
                    matches: notFoundMatches,
                    loaderData: {},
                    errors: {
                        [route.id]: error
                    }
                }, {
                    flushSync
                });
                return;
            }
            pendingNavigationController = new AbortController();
            var request = createClientSideRequest(init.history, location, pendingNavigationController.signal, opts && opts.submission);
            var scopedContext = init.getContext ? yield init.getContext() : new RouterContextProvider();
            var pendingActionResult;
            if (opts && opts.pendingError) {
                pendingActionResult = [
                    findNearestBoundary(matches).route.id,
                    {
                        type: "error" /* error */ ,
                        error: opts.pendingError
                    }
                ];
            } else if (opts && opts.submission && isMutationMethod(opts.submission.formMethod)) {
                var actionResult = yield handleAction(request, location, opts.submission, matches, scopedContext, fogOfWar.active, opts && opts.initialHydration === true, {
                    replace: opts.replace,
                    flushSync
                });
                if (actionResult.shortCircuited) {
                    return;
                }
                if (actionResult.pendingActionResult) {
                    var [routeId, result] = actionResult.pendingActionResult;
                    if (isErrorResult(result) && isRouteErrorResponse(result.error) && result.error.status === 404) {
                        pendingNavigationController = null;
                        completeNavigation(location, {
                            matches: actionResult.matches,
                            loaderData: {},
                            errors: {
                                [routeId]: result.error
                            }
                        });
                        return;
                    }
                }
                matches = actionResult.matches || matches;
                pendingActionResult = actionResult.pendingActionResult;
                loadingNavigation = getLoadingNavigation(location, opts.submission);
                flushSync = false;
                fogOfWar.active = false;
                request = createClientSideRequest(init.history, request.url, request.signal);
            }
            var { shortCircuited, matches: updatedMatches, loaderData, errors } = yield handleLoaders(request, location, matches, scopedContext, fogOfWar.active, loadingNavigation, opts && opts.submission, opts && opts.fetcherSubmission, opts && opts.replace, opts && opts.initialHydration === true, flushSync, pendingActionResult, opts && opts.callSiteDefaultShouldRevalidate);
            if (shortCircuited) {
                return;
            }
            pendingNavigationController = null;
            completeNavigation(location, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({
                matches: updatedMatches || matches
            }, getActionDataForCommit(pendingActionResult)), {
                loaderData,
                errors
            }));
        })();
    }
    function handleAction(_0, _1, _2, _3, _4, _5, _6) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*(request, location, submission, matches, scopedContext, isFogOfWar, initialHydration) {
            var opts = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : {};
            interruptActiveLoads();
            var navigation = getSubmittingNavigation(location, submission);
            updateState({
                navigation
            }, {
                flushSync: opts.flushSync === true
            });
            if (isFogOfWar) {
                var discoverResult = yield discoverRoutes(matches, location.pathname, request.signal);
                if (discoverResult.type === "aborted") {
                    return {
                        shortCircuited: true
                    };
                } else if (discoverResult.type === "error") {
                    if (discoverResult.partialMatches.length === 0) {
                        var { matches: matches2, route } = getShortCircuitMatches(dataRoutes);
                        return {
                            matches: matches2,
                            pendingActionResult: [
                                route.id,
                                {
                                    type: "error" /* error */ ,
                                    error: discoverResult.error
                                }
                            ]
                        };
                    }
                    var boundaryId = findNearestBoundary(discoverResult.partialMatches).route.id;
                    return {
                        matches: discoverResult.partialMatches,
                        pendingActionResult: [
                            boundaryId,
                            {
                                type: "error" /* error */ ,
                                error: discoverResult.error
                            }
                        ]
                    };
                } else if (!discoverResult.matches) {
                    var { notFoundMatches, error, route: route1 } = handleNavigational404(location.pathname);
                    return {
                        matches: notFoundMatches,
                        pendingActionResult: [
                            route1.id,
                            {
                                type: "error" /* error */ ,
                                error
                            }
                        ]
                    };
                } else {
                    matches = discoverResult.matches;
                }
            }
            var result;
            var actionMatch = getTargetMatch(matches, location);
            if (!actionMatch.route.action && !actionMatch.route.lazy) {
                result = {
                    type: "error" /* error */ ,
                    error: getInternalRouterError(405, {
                        method: request.method,
                        pathname: location.pathname,
                        routeId: actionMatch.route.id
                    })
                };
            } else {
                var dsMatches = getTargetedDataStrategyMatches(mapRouteProperties2, manifest, request, matches, actionMatch, initialHydration ? [] : hydrationRouteProperties2, scopedContext);
                var results = yield callDataStrategy(request, dsMatches, scopedContext, null);
                result = results[actionMatch.route.id];
                if (!result) {
                    for (var match of matches){
                        if (results[match.route.id]) {
                            result = results[match.route.id];
                            break;
                        }
                    }
                }
                if (request.signal.aborted) {
                    return {
                        shortCircuited: true
                    };
                }
            }
            if (isRedirectResult(result)) {
                var replace2;
                if (opts && opts.replace != null) {
                    replace2 = opts.replace;
                } else {
                    var location2 = normalizeRedirectLocation(result.response.headers.get("Location"), new URL(request.url), basename, init.history);
                    replace2 = location2 === state.location.pathname + state.location.search;
                }
                yield startRedirectNavigation(request, result, true, {
                    submission,
                    replace: replace2
                });
                return {
                    shortCircuited: true
                };
            }
            if (isErrorResult(result)) {
                var boundaryMatch = findNearestBoundary(matches, actionMatch.route.id);
                if ((opts && opts.replace) !== true) {
                    pendingAction = "PUSH" /* Push */ ;
                }
                return {
                    matches,
                    pendingActionResult: [
                        boundaryMatch.route.id,
                        result,
                        actionMatch.route.id
                    ]
                };
            }
            return {
                matches,
                pendingActionResult: [
                    actionMatch.route.id,
                    result
                ]
            };
        }).apply(this, arguments);
    }
    function handleLoaders(request, location, matches, scopedContext, isFogOfWar, overrideNavigation, submission, fetcherSubmission, replace2, initialHydration, flushSync, pendingActionResult, callSiteDefaultShouldRevalidate) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var loadingNavigation = overrideNavigation || getLoadingNavigation(location, submission);
            var activeSubmission = submission || fetcherSubmission || getSubmissionFromNavigation(loadingNavigation);
            var shouldUpdateNavigationState = !isUninterruptedRevalidation && !initialHydration;
            if (isFogOfWar) {
                if (shouldUpdateNavigationState) {
                    var actionData = getUpdatedActionData(pendingActionResult);
                    updateState((0,_swc_helpers_object_spread__rspack_import_2._)({
                        navigation: loadingNavigation
                    }, actionData !== void 0 ? {
                        actionData
                    } : {}), {
                        flushSync
                    });
                }
                var discoverResult = yield discoverRoutes(matches, location.pathname, request.signal);
                if (discoverResult.type === "aborted") {
                    return {
                        shortCircuited: true
                    };
                } else if (discoverResult.type === "error") {
                    if (discoverResult.partialMatches.length === 0) {
                        var { matches: matches2, route } = getShortCircuitMatches(dataRoutes);
                        return {
                            matches: matches2,
                            loaderData: {},
                            errors: {
                                [route.id]: discoverResult.error
                            }
                        };
                    }
                    var boundaryId = findNearestBoundary(discoverResult.partialMatches).route.id;
                    return {
                        matches: discoverResult.partialMatches,
                        loaderData: {},
                        errors: {
                            [boundaryId]: discoverResult.error
                        }
                    };
                } else if (!discoverResult.matches) {
                    var { error, notFoundMatches, route: route1 } = handleNavigational404(location.pathname);
                    return {
                        matches: notFoundMatches,
                        loaderData: {},
                        errors: {
                            [route1.id]: error
                        }
                    };
                } else {
                    matches = discoverResult.matches;
                }
            }
            var routesToUse = inFlightDataRoutes || dataRoutes;
            var { dsMatches, revalidatingFetchers } = getMatchesToLoad(request, scopedContext, mapRouteProperties2, manifest, init.history, state, matches, activeSubmission, location, initialHydration ? [] : hydrationRouteProperties2, initialHydration === true, isRevalidationRequired, cancelledFetcherLoads, fetchersQueuedForDeletion, fetchLoadMatches, fetchRedirectIds, routesToUse, basename, init.patchRoutesOnNavigation != null, pendingActionResult, callSiteDefaultShouldRevalidate);
            pendingNavigationLoadId = ++incrementingLoadId;
            if (!init.dataStrategy && !dsMatches.some((m)=>m.shouldLoad) && !dsMatches.some((m)=>m.route.middleware && m.route.middleware.length > 0) && revalidatingFetchers.length === 0) {
                var updatedFetchers2 = markFetchRedirectsDone();
                completeNavigation(location, (0,_swc_helpers_object_spread__rspack_import_2._)({
                    matches,
                    loaderData: {},
                    // Commit pending error if we're short circuiting
                    errors: pendingActionResult && isErrorResult(pendingActionResult[1]) ? {
                        [pendingActionResult[0]]: pendingActionResult[1].error
                    } : null
                }, getActionDataForCommit(pendingActionResult), updatedFetchers2 ? {
                    fetchers: new Map(state.fetchers)
                } : {}), {
                    flushSync
                });
                return {
                    shortCircuited: true
                };
            }
            if (shouldUpdateNavigationState) {
                var updates = {};
                if (!isFogOfWar) {
                    updates.navigation = loadingNavigation;
                    var actionData1 = getUpdatedActionData(pendingActionResult);
                    if (actionData1 !== void 0) {
                        updates.actionData = actionData1;
                    }
                }
                if (revalidatingFetchers.length > 0) {
                    updates.fetchers = getUpdatedRevalidatingFetchers(revalidatingFetchers);
                }
                updateState(updates, {
                    flushSync
                });
            }
            revalidatingFetchers.forEach((rf)=>{
                abortFetcher(rf.key);
                if (rf.controller) {
                    fetchControllers.set(rf.key, rf.controller);
                }
            });
            var abortPendingFetchRevalidations = ()=>revalidatingFetchers.forEach((f)=>abortFetcher(f.key));
            if (pendingNavigationController) {
                pendingNavigationController.signal.addEventListener("abort", abortPendingFetchRevalidations);
            }
            var { loaderResults, fetcherResults } = yield callLoadersAndMaybeResolveData(dsMatches, revalidatingFetchers, request, scopedContext);
            if (request.signal.aborted) {
                return {
                    shortCircuited: true
                };
            }
            if (pendingNavigationController) {
                pendingNavigationController.signal.removeEventListener("abort", abortPendingFetchRevalidations);
            }
            revalidatingFetchers.forEach((rf)=>fetchControllers.delete(rf.key));
            var redirect2 = findRedirect(loaderResults);
            if (redirect2) {
                yield startRedirectNavigation(request, redirect2.result, true, {
                    replace: replace2
                });
                return {
                    shortCircuited: true
                };
            }
            redirect2 = findRedirect(fetcherResults);
            if (redirect2) {
                fetchRedirectIds.add(redirect2.key);
                yield startRedirectNavigation(request, redirect2.result, true, {
                    replace: replace2
                });
                return {
                    shortCircuited: true
                };
            }
            var { loaderData, errors } = processLoaderData(state, matches, loaderResults, pendingActionResult, revalidatingFetchers, fetcherResults);
            if (initialHydration && state.errors) {
                errors = (0,_swc_helpers_object_spread__rspack_import_2._)({}, state.errors, errors);
            }
            var updatedFetchers = markFetchRedirectsDone();
            var didAbortFetchLoads = abortStaleFetchLoads(pendingNavigationLoadId);
            var shouldUpdateFetchers = updatedFetchers || didAbortFetchLoads || revalidatingFetchers.length > 0;
            return (0,_swc_helpers_object_spread__rspack_import_2._)({
                matches,
                loaderData,
                errors
            }, shouldUpdateFetchers ? {
                fetchers: new Map(state.fetchers)
            } : {});
        })();
    }
    function getUpdatedActionData(pendingActionResult) {
        if (pendingActionResult && !isErrorResult(pendingActionResult[1])) {
            return {
                [pendingActionResult[0]]: pendingActionResult[1].data
            };
        } else if (state.actionData) {
            if (Object.keys(state.actionData).length === 0) {
                return null;
            } else {
                return state.actionData;
            }
        }
    }
    function getUpdatedRevalidatingFetchers(revalidatingFetchers) {
        revalidatingFetchers.forEach((rf)=>{
            var fetcher = state.fetchers.get(rf.key);
            var revalidatingFetcher = getLoadingFetcher(void 0, fetcher ? fetcher.data : void 0);
            state.fetchers.set(rf.key, revalidatingFetcher);
        });
        return new Map(state.fetchers);
    }
    function fetch2(key, routeId, href, opts) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            abortFetcher(key);
            var flushSync = (opts && opts.flushSync) === true;
            var routesToUse = inFlightDataRoutes || dataRoutes;
            var normalizedPath = normalizeTo(state.location, state.matches, basename, href, routeId, opts === null || opts === void 0 ? void 0 : opts.relative);
            var matches = matchRoutes(routesToUse, normalizedPath, basename);
            var fogOfWar = checkFogOfWar(matches, routesToUse, normalizedPath);
            if (fogOfWar.active && fogOfWar.matches) {
                matches = fogOfWar.matches;
            }
            if (!matches) {
                setFetcherError(key, routeId, getInternalRouterError(404, {
                    pathname: normalizedPath
                }), {
                    flushSync
                });
                return;
            }
            var { path, submission, error } = normalizeNavigateOptions(true, normalizedPath, opts);
            if (error) {
                setFetcherError(key, routeId, error, {
                    flushSync
                });
                return;
            }
            var scopedContext = init.getContext ? yield init.getContext() : new RouterContextProvider();
            var preventScrollReset = (opts && opts.preventScrollReset) === true;
            if (submission && isMutationMethod(submission.formMethod)) {
                yield handleFetcherAction(key, routeId, path, matches, scopedContext, fogOfWar.active, flushSync, preventScrollReset, submission, opts && opts.unstable_defaultShouldRevalidate);
                return;
            }
            fetchLoadMatches.set(key, {
                routeId,
                path
            });
            yield handleFetcherLoader(key, routeId, path, matches, scopedContext, fogOfWar.active, flushSync, preventScrollReset, submission);
        })();
    }
    function handleFetcherAction(key, routeId, path, requestMatches, scopedContext, isFogOfWar, flushSync, preventScrollReset, submission, callSiteDefaultShouldRevalidate) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            interruptActiveLoads();
            fetchLoadMatches.delete(key);
            var existingFetcher = state.fetchers.get(key);
            updateFetcherState(key, getSubmittingFetcher(submission, existingFetcher), {
                flushSync
            });
            var abortController = new AbortController();
            var fetchRequest = createClientSideRequest(init.history, path, abortController.signal, submission);
            if (isFogOfWar) {
                var discoverResult = yield discoverRoutes(requestMatches, new URL(fetchRequest.url).pathname, fetchRequest.signal, key);
                if (discoverResult.type === "aborted") {
                    return;
                } else if (discoverResult.type === "error") {
                    setFetcherError(key, routeId, discoverResult.error, {
                        flushSync
                    });
                    return;
                } else if (!discoverResult.matches) {
                    setFetcherError(key, routeId, getInternalRouterError(404, {
                        pathname: path
                    }), {
                        flushSync
                    });
                    return;
                } else {
                    requestMatches = discoverResult.matches;
                }
            }
            var match = getTargetMatch(requestMatches, path);
            if (!match.route.action && !match.route.lazy) {
                var error = getInternalRouterError(405, {
                    method: submission.formMethod,
                    pathname: path,
                    routeId
                });
                setFetcherError(key, routeId, error, {
                    flushSync
                });
                return;
            }
            fetchControllers.set(key, abortController);
            var originatingLoadId = incrementingLoadId;
            var fetchMatches = getTargetedDataStrategyMatches(mapRouteProperties2, manifest, fetchRequest, requestMatches, match, hydrationRouteProperties2, scopedContext);
            var actionResults = yield callDataStrategy(fetchRequest, fetchMatches, scopedContext, key);
            var actionResult = actionResults[match.route.id];
            if (!actionResult) {
                for (var match2 of fetchMatches){
                    if (actionResults[match2.route.id]) {
                        actionResult = actionResults[match2.route.id];
                        break;
                    }
                }
            }
            if (fetchRequest.signal.aborted) {
                if (fetchControllers.get(key) === abortController) {
                    fetchControllers.delete(key);
                }
                return;
            }
            if (fetchersQueuedForDeletion.has(key)) {
                if (isRedirectResult(actionResult) || isErrorResult(actionResult)) {
                    updateFetcherState(key, getDoneFetcher(void 0));
                    return;
                }
            } else {
                if (isRedirectResult(actionResult)) {
                    fetchControllers.delete(key);
                    if (pendingNavigationLoadId > originatingLoadId) {
                        updateFetcherState(key, getDoneFetcher(void 0));
                        return;
                    } else {
                        fetchRedirectIds.add(key);
                        updateFetcherState(key, getLoadingFetcher(submission));
                        return startRedirectNavigation(fetchRequest, actionResult, false, {
                            fetcherSubmission: submission,
                            preventScrollReset
                        });
                    }
                }
                if (isErrorResult(actionResult)) {
                    setFetcherError(key, routeId, actionResult.error);
                    return;
                }
            }
            var nextLocation = state.navigation.location || state.location;
            var revalidationRequest = createClientSideRequest(init.history, nextLocation, abortController.signal);
            var routesToUse = inFlightDataRoutes || dataRoutes;
            var matches = state.navigation.state !== "idle" ? matchRoutes(routesToUse, state.navigation.location, basename) : state.matches;
            invariant(matches, "Didn't find any matches after fetcher action");
            var loadId = ++incrementingLoadId;
            fetchReloadIds.set(key, loadId);
            var loadFetcher = getLoadingFetcher(submission, actionResult.data);
            state.fetchers.set(key, loadFetcher);
            var { dsMatches, revalidatingFetchers } = getMatchesToLoad(revalidationRequest, scopedContext, mapRouteProperties2, manifest, init.history, state, matches, submission, nextLocation, hydrationRouteProperties2, false, isRevalidationRequired, cancelledFetcherLoads, fetchersQueuedForDeletion, fetchLoadMatches, fetchRedirectIds, routesToUse, basename, init.patchRoutesOnNavigation != null, [
                match.route.id,
                actionResult
            ], callSiteDefaultShouldRevalidate);
            revalidatingFetchers.filter((rf)=>rf.key !== key).forEach((rf)=>{
                var staleKey = rf.key;
                var existingFetcher2 = state.fetchers.get(staleKey);
                var revalidatingFetcher = getLoadingFetcher(void 0, existingFetcher2 ? existingFetcher2.data : void 0);
                state.fetchers.set(staleKey, revalidatingFetcher);
                abortFetcher(staleKey);
                if (rf.controller) {
                    fetchControllers.set(staleKey, rf.controller);
                }
            });
            updateState({
                fetchers: new Map(state.fetchers)
            });
            var abortPendingFetchRevalidations = ()=>revalidatingFetchers.forEach((rf)=>abortFetcher(rf.key));
            abortController.signal.addEventListener("abort", abortPendingFetchRevalidations);
            var { loaderResults, fetcherResults } = yield callLoadersAndMaybeResolveData(dsMatches, revalidatingFetchers, revalidationRequest, scopedContext);
            if (abortController.signal.aborted) {
                return;
            }
            abortController.signal.removeEventListener("abort", abortPendingFetchRevalidations);
            fetchReloadIds.delete(key);
            fetchControllers.delete(key);
            revalidatingFetchers.forEach((r)=>fetchControllers.delete(r.key));
            if (state.fetchers.has(key)) {
                var doneFetcher = getDoneFetcher(actionResult.data);
                state.fetchers.set(key, doneFetcher);
            }
            var redirect2 = findRedirect(loaderResults);
            if (redirect2) {
                return startRedirectNavigation(revalidationRequest, redirect2.result, false, {
                    preventScrollReset
                });
            }
            redirect2 = findRedirect(fetcherResults);
            if (redirect2) {
                fetchRedirectIds.add(redirect2.key);
                return startRedirectNavigation(revalidationRequest, redirect2.result, false, {
                    preventScrollReset
                });
            }
            var { loaderData, errors } = processLoaderData(state, matches, loaderResults, void 0, revalidatingFetchers, fetcherResults);
            abortStaleFetchLoads(loadId);
            if (state.navigation.state === "loading" && loadId > pendingNavigationLoadId) {
                invariant(pendingAction, "Expected pending action");
                pendingNavigationController && pendingNavigationController.abort();
                completeNavigation(state.navigation.location, {
                    matches,
                    loaderData,
                    errors,
                    fetchers: new Map(state.fetchers)
                });
            } else {
                updateState({
                    errors,
                    loaderData: mergeLoaderData(state.loaderData, loaderData, matches, errors),
                    fetchers: new Map(state.fetchers)
                });
                isRevalidationRequired = false;
            }
        })();
    }
    function handleFetcherLoader(key, routeId, path, matches, scopedContext, isFogOfWar, flushSync, preventScrollReset, submission) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var existingFetcher = state.fetchers.get(key);
            updateFetcherState(key, getLoadingFetcher(submission, existingFetcher ? existingFetcher.data : void 0), {
                flushSync
            });
            var abortController = new AbortController();
            var fetchRequest = createClientSideRequest(init.history, path, abortController.signal);
            if (isFogOfWar) {
                var discoverResult = yield discoverRoutes(matches, new URL(fetchRequest.url).pathname, fetchRequest.signal, key);
                if (discoverResult.type === "aborted") {
                    return;
                } else if (discoverResult.type === "error") {
                    setFetcherError(key, routeId, discoverResult.error, {
                        flushSync
                    });
                    return;
                } else if (!discoverResult.matches) {
                    setFetcherError(key, routeId, getInternalRouterError(404, {
                        pathname: path
                    }), {
                        flushSync
                    });
                    return;
                } else {
                    matches = discoverResult.matches;
                }
            }
            var match = getTargetMatch(matches, path);
            fetchControllers.set(key, abortController);
            var originatingLoadId = incrementingLoadId;
            var dsMatches = getTargetedDataStrategyMatches(mapRouteProperties2, manifest, fetchRequest, matches, match, hydrationRouteProperties2, scopedContext);
            var results = yield callDataStrategy(fetchRequest, dsMatches, scopedContext, key);
            var result = results[match.route.id];
            if (fetchControllers.get(key) === abortController) {
                fetchControllers.delete(key);
            }
            if (fetchRequest.signal.aborted) {
                return;
            }
            if (fetchersQueuedForDeletion.has(key)) {
                updateFetcherState(key, getDoneFetcher(void 0));
                return;
            }
            if (isRedirectResult(result)) {
                if (pendingNavigationLoadId > originatingLoadId) {
                    updateFetcherState(key, getDoneFetcher(void 0));
                    return;
                } else {
                    fetchRedirectIds.add(key);
                    yield startRedirectNavigation(fetchRequest, result, false, {
                        preventScrollReset
                    });
                    return;
                }
            }
            if (isErrorResult(result)) {
                setFetcherError(key, routeId, result.error);
                return;
            }
            updateFetcherState(key, getDoneFetcher(result.data));
        })();
    }
    function startRedirectNavigation(_0, _1, _2) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*(request, redirect2, isNavigation) {
            var { submission, fetcherSubmission, preventScrollReset, replace: replace2 } = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {};
            if (!isNavigation) {
                pendingPopstateNavigationDfd === null || pendingPopstateNavigationDfd === void 0 ? void 0 : pendingPopstateNavigationDfd.resolve();
                pendingPopstateNavigationDfd = null;
            }
            if (redirect2.response.headers.has("X-Remix-Revalidate")) {
                isRevalidationRequired = true;
            }
            var location = redirect2.response.headers.get("Location");
            invariant(location, "Expected a Location header on the redirect Response");
            location = normalizeRedirectLocation(location, new URL(request.url), basename, init.history);
            var redirectLocation = createLocation(state.location, location, {
                _isRedirect: true
            });
            if (isBrowser3) {
                var isDocumentReload = false;
                if (redirect2.response.headers.has("X-Remix-Reload-Document")) {
                    isDocumentReload = true;
                } else if (isAbsoluteUrl(location)) {
                    var url = createBrowserURLImpl(location, true);
                    isDocumentReload = url.origin !== routerWindow.location.origin || // Hard reload if it's an absolute URL that does not match our basename
                    stripBasename(url.pathname, basename) == null;
                }
                if (isDocumentReload) {
                    if (replace2) {
                        routerWindow.location.replace(location);
                    } else {
                        routerWindow.location.assign(location);
                    }
                    return;
                }
            }
            pendingNavigationController = null;
            var redirectNavigationType = replace2 === true || redirect2.response.headers.has("X-Remix-Replace") ? "REPLACE" /* Replace */  : "PUSH" /* Push */ ;
            var { formMethod, formAction, formEncType } = state.navigation;
            if (!submission && !fetcherSubmission && formMethod && formAction && formEncType) {
                submission = getSubmissionFromNavigation(state.navigation);
            }
            var activeSubmission = submission || fetcherSubmission;
            if (redirectPreserveMethodStatusCodes.has(redirect2.response.status) && activeSubmission && isMutationMethod(activeSubmission.formMethod)) {
                yield startNavigation(redirectNavigationType, redirectLocation, {
                    submission: (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, activeSubmission), {
                        formAction: location
                    }),
                    // Preserve these flags across redirects
                    preventScrollReset: preventScrollReset || pendingPreventScrollReset,
                    enableViewTransition: isNavigation ? pendingViewTransitionEnabled : void 0
                });
            } else {
                var overrideNavigation = getLoadingNavigation(redirectLocation, submission);
                yield startNavigation(redirectNavigationType, redirectLocation, {
                    overrideNavigation,
                    // Send fetcher submissions through for shouldRevalidate
                    fetcherSubmission,
                    // Preserve these flags across redirects
                    preventScrollReset: preventScrollReset || pendingPreventScrollReset,
                    enableViewTransition: isNavigation ? pendingViewTransitionEnabled : void 0
                });
            }
        }).apply(this, arguments);
    }
    function callDataStrategy(request, matches, scopedContext, fetcherKey) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var results;
            var dataResults = {};
            try {
                results = yield callDataStrategyImpl(dataStrategyImpl, request, matches, fetcherKey, scopedContext, false);
            } catch (e) {
                matches.filter((m)=>m.shouldLoad).forEach((m)=>{
                    dataResults[m.route.id] = {
                        type: "error" /* error */ ,
                        error: e
                    };
                });
                return dataResults;
            }
            if (request.signal.aborted) {
                return dataResults;
            }
            if (!isMutationMethod(request.method)) {
                for (var match of matches){
                    var _results_match_route_id;
                    if (((_results_match_route_id = results[match.route.id]) === null || _results_match_route_id === void 0 ? void 0 : _results_match_route_id.type) === "error" /* error */ ) {
                        break;
                    }
                    if (!results.hasOwnProperty(match.route.id) && !state.loaderData.hasOwnProperty(match.route.id) && (!state.errors || !state.errors.hasOwnProperty(match.route.id)) && match.shouldCallHandler()) {
                        results[match.route.id] = {
                            type: "error" /* error */ ,
                            result: new Error("No result returned from dataStrategy for route ".concat(match.route.id))
                        };
                    }
                }
            }
            for (var [routeId, result] of Object.entries(results)){
                if (isRedirectDataStrategyResult(result)) {
                    var response = result.result;
                    dataResults[routeId] = {
                        type: "redirect" /* redirect */ ,
                        response: normalizeRelativeRoutingRedirectResponse(response, request, routeId, matches, basename)
                    };
                } else {
                    dataResults[routeId] = yield convertDataStrategyResultToDataResult(result);
                }
            }
            return dataResults;
        })();
    }
    function callLoadersAndMaybeResolveData(matches, fetchersToLoad, request, scopedContext) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var loaderResultsPromise = callDataStrategy(request, matches, scopedContext, null);
            var fetcherResultsPromise = Promise.all(fetchersToLoad.map((f)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                    if (f.matches && f.match && f.request && f.controller) {
                        var results = yield callDataStrategy(f.request, f.matches, scopedContext, f.key);
                        var result = results[f.match.route.id];
                        return {
                            [f.key]: result
                        };
                    } else {
                        return Promise.resolve({
                            [f.key]: {
                                type: "error" /* error */ ,
                                error: getInternalRouterError(404, {
                                    pathname: f.path
                                })
                            }
                        });
                    }
                })()));
            var loaderResults = yield loaderResultsPromise;
            var fetcherResults = (yield fetcherResultsPromise).reduce((acc, r)=>Object.assign(acc, r), {});
            return {
                loaderResults,
                fetcherResults
            };
        })();
    }
    function interruptActiveLoads() {
        isRevalidationRequired = true;
        fetchLoadMatches.forEach((_, key)=>{
            if (fetchControllers.has(key)) {
                cancelledFetcherLoads.add(key);
            }
            abortFetcher(key);
        });
    }
    function updateFetcherState(key, fetcher) {
        var opts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
        state.fetchers.set(key, fetcher);
        updateState({
            fetchers: new Map(state.fetchers)
        }, {
            flushSync: (opts && opts.flushSync) === true
        });
    }
    function setFetcherError(key, routeId, error) {
        var opts = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {};
        var boundaryMatch = findNearestBoundary(state.matches, routeId);
        deleteFetcher(key);
        updateState({
            errors: {
                [boundaryMatch.route.id]: error
            },
            fetchers: new Map(state.fetchers)
        }, {
            flushSync: (opts && opts.flushSync) === true
        });
    }
    function getFetcher(key) {
        activeFetchers.set(key, (activeFetchers.get(key) || 0) + 1);
        if (fetchersQueuedForDeletion.has(key)) {
            fetchersQueuedForDeletion.delete(key);
        }
        return state.fetchers.get(key) || IDLE_FETCHER;
    }
    function resetFetcher(key, opts) {
        abortFetcher(key, opts === null || opts === void 0 ? void 0 : opts.reason);
        updateFetcherState(key, getDoneFetcher(null));
    }
    function deleteFetcher(key) {
        var fetcher = state.fetchers.get(key);
        if (fetchControllers.has(key) && !(fetcher && fetcher.state === "loading" && fetchReloadIds.has(key))) {
            abortFetcher(key);
        }
        fetchLoadMatches.delete(key);
        fetchReloadIds.delete(key);
        fetchRedirectIds.delete(key);
        fetchersQueuedForDeletion.delete(key);
        cancelledFetcherLoads.delete(key);
        state.fetchers.delete(key);
    }
    function queueFetcherForDeletion(key) {
        var count = (activeFetchers.get(key) || 0) - 1;
        if (count <= 0) {
            activeFetchers.delete(key);
            fetchersQueuedForDeletion.add(key);
        } else {
            activeFetchers.set(key, count);
        }
        updateState({
            fetchers: new Map(state.fetchers)
        });
    }
    function abortFetcher(key, reason) {
        var controller = fetchControllers.get(key);
        if (controller) {
            controller.abort(reason);
            fetchControllers.delete(key);
        }
    }
    function markFetchersDone(keys) {
        for (var key of keys){
            var fetcher = getFetcher(key);
            var doneFetcher = getDoneFetcher(fetcher.data);
            state.fetchers.set(key, doneFetcher);
        }
    }
    function markFetchRedirectsDone() {
        var doneKeys = [];
        var updatedFetchers = false;
        for (var key of fetchRedirectIds){
            var fetcher = state.fetchers.get(key);
            invariant(fetcher, "Expected fetcher: ".concat(key));
            if (fetcher.state === "loading") {
                fetchRedirectIds.delete(key);
                doneKeys.push(key);
                updatedFetchers = true;
            }
        }
        markFetchersDone(doneKeys);
        return updatedFetchers;
    }
    function abortStaleFetchLoads(landedId) {
        var yeetedKeys = [];
        for (var [key, id] of fetchReloadIds){
            if (id < landedId) {
                var fetcher = state.fetchers.get(key);
                invariant(fetcher, "Expected fetcher: ".concat(key));
                if (fetcher.state === "loading") {
                    abortFetcher(key);
                    fetchReloadIds.delete(key);
                    yeetedKeys.push(key);
                }
            }
        }
        markFetchersDone(yeetedKeys);
        return yeetedKeys.length > 0;
    }
    function getBlocker(key, fn) {
        var blocker = state.blockers.get(key) || IDLE_BLOCKER;
        if (blockerFunctions.get(key) !== fn) {
            blockerFunctions.set(key, fn);
        }
        return blocker;
    }
    function deleteBlocker(key) {
        state.blockers.delete(key);
        blockerFunctions.delete(key);
    }
    function updateBlocker(key, newBlocker) {
        var blocker = state.blockers.get(key) || IDLE_BLOCKER;
        invariant(blocker.state === "unblocked" && newBlocker.state === "blocked" || blocker.state === "blocked" && newBlocker.state === "blocked" || blocker.state === "blocked" && newBlocker.state === "proceeding" || blocker.state === "blocked" && newBlocker.state === "unblocked" || blocker.state === "proceeding" && newBlocker.state === "unblocked", "Invalid blocker state transition: ".concat(blocker.state, " -> ").concat(newBlocker.state));
        var blockers = new Map(state.blockers);
        blockers.set(key, newBlocker);
        updateState({
            blockers
        });
    }
    function shouldBlockNavigation(param) {
        var { currentLocation, nextLocation, historyAction } = param;
        if (blockerFunctions.size === 0) {
            return;
        }
        if (blockerFunctions.size > 1) {
            warning(false, "A router only supports one blocker at a time");
        }
        var entries = Array.from(blockerFunctions.entries());
        var [blockerKey, blockerFunction] = entries[entries.length - 1];
        var blocker = state.blockers.get(blockerKey);
        if (blocker && blocker.state === "proceeding") {
            return;
        }
        if (blockerFunction({
            currentLocation,
            nextLocation,
            historyAction
        })) {
            return blockerKey;
        }
    }
    function handleNavigational404(pathname) {
        var error = getInternalRouterError(404, {
            pathname
        });
        var routesToUse = inFlightDataRoutes || dataRoutes;
        var { matches, route } = getShortCircuitMatches(routesToUse);
        return {
            notFoundMatches: matches,
            route,
            error
        };
    }
    function enableScrollRestoration(positions, getPosition, getKey) {
        savedScrollPositions2 = positions;
        getScrollPosition = getPosition;
        getScrollRestorationKey2 = getKey || null;
        if (!initialScrollRestored && state.navigation === IDLE_NAVIGATION) {
            initialScrollRestored = true;
            var y = getSavedScrollPosition(state.location, state.matches);
            if (y != null) {
                updateState({
                    restoreScrollPosition: y
                });
            }
        }
        return ()=>{
            savedScrollPositions2 = null;
            getScrollPosition = null;
            getScrollRestorationKey2 = null;
        };
    }
    function getScrollKey(location, matches) {
        if (getScrollRestorationKey2) {
            var key = getScrollRestorationKey2(location, matches.map((m)=>convertRouteMatchToUiMatch(m, state.loaderData)));
            return key || location.key;
        }
        return location.key;
    }
    function saveScrollPosition(location, matches) {
        if (savedScrollPositions2 && getScrollPosition) {
            var key = getScrollKey(location, matches);
            savedScrollPositions2[key] = getScrollPosition();
        }
    }
    function getSavedScrollPosition(location, matches) {
        if (savedScrollPositions2) {
            var key = getScrollKey(location, matches);
            var y = savedScrollPositions2[key];
            if (typeof y === "number") {
                return y;
            }
        }
        return null;
    }
    function checkFogOfWar(matches, routesToUse, pathname) {
        if (init.patchRoutesOnNavigation) {
            if (!matches) {
                var fogMatches = matchRoutesImpl(routesToUse, pathname, basename, true);
                return {
                    active: true,
                    matches: fogMatches || []
                };
            } else {
                if (Object.keys(matches[0].params).length > 0) {
                    var partialMatches = matchRoutesImpl(routesToUse, pathname, basename, true);
                    return {
                        active: true,
                        matches: partialMatches
                    };
                }
            }
        }
        return {
            active: false,
            matches: null
        };
    }
    function discoverRoutes(matches, pathname, signal, fetcherKey) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var _loop = function*() {
                var isNonHMR = inFlightDataRoutes == null;
                var routesToUse = inFlightDataRoutes || dataRoutes;
                var localManifest = manifest;
                try {
                    yield init.patchRoutesOnNavigation({
                        signal,
                        path: pathname,
                        matches: partialMatches,
                        fetcherKey,
                        patch: (routeId, children)=>{
                            if (signal.aborted) return;
                            patchRoutesImpl(routeId, children, routesToUse, localManifest, mapRouteProperties2, false);
                        }
                    });
                } catch (e) {
                    return {
                        v: {
                            type: "error",
                            error: e,
                            partialMatches
                        }
                    };
                } finally{
                    if (isNonHMR && !signal.aborted) {
                        dataRoutes = [
                            ...dataRoutes
                        ];
                    }
                }
                if (signal.aborted) {
                    return {
                        v: {
                            type: "aborted"
                        }
                    };
                }
                var newMatches = matchRoutes(routesToUse, pathname, basename);
                var newPartialMatches = null;
                if (newMatches) {
                    if (Object.keys(newMatches[0].params).length === 0) {
                        return {
                            v: {
                                type: "success",
                                matches: newMatches
                            }
                        };
                    } else {
                        newPartialMatches = matchRoutesImpl(routesToUse, pathname, basename, true);
                        var matchedDeeper = newPartialMatches && partialMatches.length < newPartialMatches.length && compareMatches(partialMatches, newPartialMatches.slice(0, partialMatches.length));
                        if (!matchedDeeper) {
                            return {
                                v: {
                                    type: "success",
                                    matches: newMatches
                                }
                            };
                        }
                    }
                }
                if (!newPartialMatches) {
                    newPartialMatches = matchRoutesImpl(routesToUse, pathname, basename, true);
                }
                if (!newPartialMatches || compareMatches(partialMatches, newPartialMatches)) {
                    return {
                        v: {
                            type: "success",
                            matches: null
                        }
                    };
                }
                partialMatches = newPartialMatches;
            };
            if (!init.patchRoutesOnNavigation) {
                return {
                    type: "success",
                    matches
                };
            }
            var partialMatches = matches;
            while(true){
                var _ret = yield* _loop();
                if ((0,_swc_helpers_type_of__rspack_import_4._)(_ret) === "object") return _ret.v;
            }
        })();
    }
    function compareMatches(a, b) {
        return a.length === b.length && a.every((m, i)=>m.route.id === b[i].route.id);
    }
    function _internalSetRoutes(newRoutes) {
        manifest = {};
        inFlightDataRoutes = convertRoutesToDataRoutes(newRoutes, mapRouteProperties2, void 0, manifest);
    }
    function patchRoutes(routeId, children) {
        var unstable_allowElementMutations = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
        var isNonHMR = inFlightDataRoutes == null;
        var routesToUse = inFlightDataRoutes || dataRoutes;
        patchRoutesImpl(routeId, children, routesToUse, manifest, mapRouteProperties2, unstable_allowElementMutations);
        if (isNonHMR) {
            dataRoutes = [
                ...dataRoutes
            ];
            updateState({});
        }
    }
    router = {
        get basename () {
            return basename;
        },
        get future () {
            return future;
        },
        get state () {
            return state;
        },
        get routes () {
            return dataRoutes;
        },
        get window () {
            return routerWindow;
        },
        initialize,
        subscribe,
        enableScrollRestoration,
        navigate,
        fetch: fetch2,
        revalidate,
        // Passthrough to history-aware createHref used by useHref so we get proper
        // hash-aware URLs in DOM paths
        createHref: (to)=>init.history.createHref(to),
        encodeLocation: (to)=>init.history.encodeLocation(to),
        getFetcher,
        resetFetcher,
        deleteFetcher: queueFetcherForDeletion,
        dispose,
        getBlocker,
        deleteBlocker,
        patchRoutes,
        _internalFetchControllers: fetchControllers,
        // TODO: Remove setRoutes, it's temporary to avoid dealing with
        // updating the tree while validating the update algorithm.
        _internalSetRoutes,
        _internalSetStateDoNotUseOrYouWillBreakYourApp (newState) {
            updateState(newState);
        }
    };
    if (init.unstable_instrumentations) {
        router = instrumentClientSideRouter(router, init.unstable_instrumentations.map((i)=>i.router).filter(Boolean));
    }
    return router;
}
function createStaticHandler(routes, opts) {
    invariant(routes.length > 0, "You must provide a non-empty routes array to createStaticHandler");
    var manifest = {};
    var basename = (opts ? opts.basename : null) || "/";
    var _mapRouteProperties = (opts === null || opts === void 0 ? void 0 : opts.mapRouteProperties) || defaultMapRouteProperties;
    var mapRouteProperties2 = _mapRouteProperties;
    if (opts === null || opts === void 0 ? void 0 : opts.unstable_instrumentations) {
        var instrumentations = opts.unstable_instrumentations;
        mapRouteProperties2 = (route)=>{
            return (0,_swc_helpers_object_spread__rspack_import_2._)({}, _mapRouteProperties(route), getRouteInstrumentationUpdates(instrumentations.map((i)=>i.route).filter(Boolean), route));
        };
    }
    var dataRoutes = convertRoutesToDataRoutes(routes, mapRouteProperties2, void 0, manifest);
    function query(_0) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*(request) {
            var { requestContext, filterMatchesToLoad, skipLoaderErrorBubbling, skipRevalidation, dataStrategy, generateMiddlewareResponse } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
            var url = new URL(request.url);
            var method = request.method;
            var location = createLocation("", createPath(url), null, "default");
            var matches = matchRoutes(dataRoutes, location, basename);
            requestContext = requestContext != null ? requestContext : new RouterContextProvider();
            if (!isValidMethod(method) && method !== "HEAD") {
                var error = getInternalRouterError(405, {
                    method
                });
                var { matches: methodNotAllowedMatches, route } = getShortCircuitMatches(dataRoutes);
                var staticContext = {
                    basename,
                    location,
                    matches: methodNotAllowedMatches,
                    loaderData: {},
                    actionData: null,
                    errors: {
                        [route.id]: error
                    },
                    statusCode: error.status,
                    loaderHeaders: {},
                    actionHeaders: {}
                };
                return generateMiddlewareResponse ? generateMiddlewareResponse(()=>Promise.resolve(staticContext)) : staticContext;
            } else if (!matches) {
                var error1 = getInternalRouterError(404, {
                    pathname: location.pathname
                });
                var { matches: notFoundMatches, route: route1 } = getShortCircuitMatches(dataRoutes);
                var staticContext1 = {
                    basename,
                    location,
                    matches: notFoundMatches,
                    loaderData: {},
                    actionData: null,
                    errors: {
                        [route1.id]: error1
                    },
                    statusCode: error1.status,
                    loaderHeaders: {},
                    actionHeaders: {}
                };
                return generateMiddlewareResponse ? generateMiddlewareResponse(()=>Promise.resolve(staticContext1)) : staticContext1;
            }
            if (generateMiddlewareResponse) {
                invariant(requestContext instanceof RouterContextProvider, "When using middleware in `staticHandler.query()`, any provided `requestContext` must be an instance of `RouterContextProvider`");
                try {
                    yield loadLazyMiddlewareForMatches(matches, manifest, mapRouteProperties2);
                    var renderedStaticContext;
                    var response = yield runServerMiddlewarePipeline({
                        request,
                        unstable_pattern: getRoutePattern(matches),
                        matches,
                        params: matches[0].params,
                        // If we're calling middleware then it must be enabled so we can cast
                        // this to the proper type knowing it's not an `AppLoadContext`
                        context: requestContext
                    }, ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                            var res = yield generateMiddlewareResponse(function(revalidationRequest) {
                                var opts2 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
                                return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                    var _opts2_filterMatchesToLoad;
                                    var result2 = yield queryImpl(revalidationRequest, location, matches, requestContext, dataStrategy || null, skipLoaderErrorBubbling === true, null, "filterMatchesToLoad" in opts2 ? (_opts2_filterMatchesToLoad = opts2.filterMatchesToLoad) !== null && _opts2_filterMatchesToLoad !== void 0 ? _opts2_filterMatchesToLoad : null : filterMatchesToLoad !== null && filterMatchesToLoad !== void 0 ? filterMatchesToLoad : null, skipRevalidation === true);
                                    if (isResponse(result2)) {
                                        return result2;
                                    }
                                    renderedStaticContext = (0,_swc_helpers_object_spread__rspack_import_2._)({
                                        location,
                                        basename
                                    }, result2);
                                    return renderedStaticContext;
                                })();
                            });
                            return res;
                        })(), (error, routeId)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                            if (isRedirectResponse(error)) {
                                return error;
                            }
                            if (isResponse(error)) {
                                try {
                                    error = new ErrorResponseImpl(error.status, error.statusText, (yield parseResponseBody(error)));
                                } catch (e) {
                                    error = e;
                                }
                            }
                            if (isDataWithResponseInit(error)) {
                                error = dataWithResponseInitToErrorResponse(error);
                            }
                            if (renderedStaticContext) {
                                if (routeId in renderedStaticContext.loaderData) {
                                    renderedStaticContext.loaderData[routeId] = void 0;
                                }
                                var staticContext = getStaticContextFromError(dataRoutes, renderedStaticContext, error, skipLoaderErrorBubbling ? routeId : findNearestBoundary(matches, routeId).route.id);
                                return generateMiddlewareResponse(()=>Promise.resolve(staticContext));
                            } else {
                                var _matches_find;
                                var boundaryRouteId = skipLoaderErrorBubbling ? routeId : findNearestBoundary(matches, ((_matches_find = matches.find((m)=>m.route.id === routeId || m.route.loader)) === null || _matches_find === void 0 ? void 0 : _matches_find.route.id) || routeId).route.id;
                                var staticContext1 = {
                                    matches,
                                    location,
                                    basename,
                                    loaderData: {},
                                    actionData: null,
                                    errors: {
                                        [boundaryRouteId]: error
                                    },
                                    statusCode: isRouteErrorResponse(error) ? error.status : 500,
                                    actionHeaders: {},
                                    loaderHeaders: {}
                                };
                                return generateMiddlewareResponse(()=>Promise.resolve(staticContext1));
                            }
                        })());
                    invariant(isResponse(response), "Expected a response in query()");
                    return response;
                } catch (e) {
                    if (isResponse(e)) {
                        return e;
                    }
                    throw e;
                }
            }
            var result = yield queryImpl(request, location, matches, requestContext, dataStrategy || null, skipLoaderErrorBubbling === true, null, filterMatchesToLoad || null, skipRevalidation === true);
            if (isResponse(result)) {
                return result;
            }
            return (0,_swc_helpers_object_spread__rspack_import_2._)({
                location,
                basename
            }, result);
        }).apply(this, arguments);
    }
    function queryRoute(_0) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*(request) {
            var { routeId, requestContext, dataStrategy, generateMiddlewareResponse } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
            var url = new URL(request.url);
            var method = request.method;
            var location = createLocation("", createPath(url), null, "default");
            var matches = matchRoutes(dataRoutes, location, basename);
            requestContext = requestContext != null ? requestContext : new RouterContextProvider();
            if (!isValidMethod(method) && method !== "HEAD" && method !== "OPTIONS") {
                throw getInternalRouterError(405, {
                    method
                });
            } else if (!matches) {
                throw getInternalRouterError(404, {
                    pathname: location.pathname
                });
            }
            var match = routeId ? matches.find((m)=>m.route.id === routeId) : getTargetMatch(matches, location);
            if (routeId && !match) {
                throw getInternalRouterError(403, {
                    pathname: location.pathname,
                    routeId
                });
            } else if (!match) {
                throw getInternalRouterError(404, {
                    pathname: location.pathname
                });
            }
            if (generateMiddlewareResponse) {
                invariant(requestContext instanceof RouterContextProvider, "When using middleware in `staticHandler.queryRoute()`, any provided `requestContext` must be an instance of `RouterContextProvider`");
                yield loadLazyMiddlewareForMatches(matches, manifest, mapRouteProperties2);
                var response = yield runServerMiddlewarePipeline({
                    request,
                    unstable_pattern: getRoutePattern(matches),
                    matches,
                    params: matches[0].params,
                    // If we're calling middleware then it must be enabled so we can cast
                    // this to the proper type knowing it's not an `AppLoadContext`
                    context: requestContext
                }, ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        var res = yield generateMiddlewareResponse((innerRequest)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                var result2 = yield queryImpl(innerRequest, location, matches, requestContext, dataStrategy || null, false, match, null, false);
                                var processed = handleQueryResult(result2);
                                return isResponse(processed) ? processed : typeof processed === "string" ? new Response(processed) : Response.json(processed);
                            })());
                        return res;
                    })(), (error)=>{
                    if (isDataWithResponseInit(error)) {
                        return Promise.resolve(dataWithResponseInitToResponse(error));
                    }
                    if (isResponse(error)) {
                        return Promise.resolve(error);
                    }
                    throw error;
                });
                return response;
            }
            var result = yield queryImpl(request, location, matches, requestContext, dataStrategy || null, false, match, null, false);
            return handleQueryResult(result);
            function handleQueryResult(result2) {
                if (isResponse(result2)) {
                    return result2;
                }
                var error = result2.errors ? Object.values(result2.errors)[0] : void 0;
                if (error !== void 0) {
                    throw error;
                }
                if (result2.actionData) {
                    return Object.values(result2.actionData)[0];
                }
                if (result2.loaderData) {
                    return Object.values(result2.loaderData)[0];
                }
                return void 0;
            }
        }).apply(this, arguments);
    }
    function queryImpl(request, location, matches, requestContext, dataStrategy, skipLoaderErrorBubbling, routeMatch, filterMatchesToLoad, skipRevalidation) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            invariant(request.signal, "query()/queryRoute() requests must contain an AbortController signal");
            try {
                if (isMutationMethod(request.method)) {
                    var result2 = yield submit(request, matches, routeMatch || getTargetMatch(matches, location), requestContext, dataStrategy, skipLoaderErrorBubbling, routeMatch != null, filterMatchesToLoad, skipRevalidation);
                    return result2;
                }
                var result = yield loadRouteData(request, matches, requestContext, dataStrategy, skipLoaderErrorBubbling, routeMatch, filterMatchesToLoad);
                return isResponse(result) ? result : (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, result), {
                    actionData: null,
                    actionHeaders: {}
                });
            } catch (e) {
                if (isDataStrategyResult(e) && isResponse(e.result)) {
                    if (e.type === "error" /* error */ ) {
                        throw e.result;
                    }
                    return e.result;
                }
                if (isRedirectResponse(e)) {
                    return e;
                }
                throw e;
            }
        })();
    }
    function submit(request, matches, actionMatch, requestContext, dataStrategy, skipLoaderErrorBubbling, isRouteRequest, filterMatchesToLoad, skipRevalidation) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var result;
            if (!actionMatch.route.action && !actionMatch.route.lazy) {
                var error = getInternalRouterError(405, {
                    method: request.method,
                    pathname: new URL(request.url).pathname,
                    routeId: actionMatch.route.id
                });
                if (isRouteRequest) {
                    throw error;
                }
                result = {
                    type: "error" /* error */ ,
                    error
                };
            } else {
                var dsMatches = getTargetedDataStrategyMatches(mapRouteProperties2, manifest, request, matches, actionMatch, [], requestContext);
                var results = yield callDataStrategy(request, dsMatches, isRouteRequest, requestContext, dataStrategy);
                result = results[actionMatch.route.id];
                if (request.signal.aborted) {
                    throwStaticHandlerAbortedError(request, isRouteRequest);
                }
            }
            if (isRedirectResult(result)) {
                throw new Response(null, {
                    status: result.response.status,
                    headers: {
                        Location: result.response.headers.get("Location")
                    }
                });
            }
            if (isRouteRequest) {
                if (isErrorResult(result)) {
                    throw result.error;
                }
                return {
                    matches: [
                        actionMatch
                    ],
                    loaderData: {},
                    actionData: {
                        [actionMatch.route.id]: result.data
                    },
                    errors: null,
                    // Note: statusCode + headers are unused here since queryRoute will
                    // return the raw Response or value
                    statusCode: 200,
                    loaderHeaders: {},
                    actionHeaders: {}
                };
            }
            if (skipRevalidation) {
                if (isErrorResult(result)) {
                    var boundaryMatch = skipLoaderErrorBubbling ? actionMatch : findNearestBoundary(matches, actionMatch.route.id);
                    return {
                        statusCode: isRouteErrorResponse(result.error) ? result.error.status : result.statusCode != null ? result.statusCode : 500,
                        actionData: null,
                        actionHeaders: (0,_swc_helpers_object_spread__rspack_import_2._)({}, result.headers ? {
                            [actionMatch.route.id]: result.headers
                        } : {}),
                        matches,
                        loaderData: {},
                        errors: {
                            [boundaryMatch.route.id]: result.error
                        },
                        loaderHeaders: {}
                    };
                } else {
                    return {
                        actionData: {
                            [actionMatch.route.id]: result.data
                        },
                        actionHeaders: result.headers ? {
                            [actionMatch.route.id]: result.headers
                        } : {},
                        matches,
                        loaderData: {},
                        errors: null,
                        statusCode: result.statusCode || 200,
                        loaderHeaders: {}
                    };
                }
            }
            var loaderRequest = new Request(request.url, {
                headers: request.headers,
                redirect: request.redirect,
                signal: request.signal
            });
            if (isErrorResult(result)) {
                var boundaryMatch1 = skipLoaderErrorBubbling ? actionMatch : findNearestBoundary(matches, actionMatch.route.id);
                var handlerContext2 = yield loadRouteData(loaderRequest, matches, requestContext, dataStrategy, skipLoaderErrorBubbling, null, filterMatchesToLoad, [
                    boundaryMatch1.route.id,
                    result
                ]);
                return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, handlerContext2), {
                    statusCode: isRouteErrorResponse(result.error) ? result.error.status : result.statusCode != null ? result.statusCode : 500,
                    actionData: null,
                    actionHeaders: (0,_swc_helpers_object_spread__rspack_import_2._)({}, result.headers ? {
                        [actionMatch.route.id]: result.headers
                    } : {})
                });
            }
            var handlerContext = yield loadRouteData(loaderRequest, matches, requestContext, dataStrategy, skipLoaderErrorBubbling, null, filterMatchesToLoad);
            return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, handlerContext), {
                actionData: {
                    [actionMatch.route.id]: result.data
                }
            }), result.statusCode ? {
                statusCode: result.statusCode
            } : {}), {
                actionHeaders: result.headers ? {
                    [actionMatch.route.id]: result.headers
                } : {}
            });
        })();
    }
    function loadRouteData(request, matches, requestContext, dataStrategy, skipLoaderErrorBubbling, routeMatch, filterMatchesToLoad, pendingActionResult) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var isRouteRequest = routeMatch != null;
            if (isRouteRequest && !(routeMatch === null || routeMatch === void 0 ? void 0 : routeMatch.route.loader) && !(routeMatch === null || routeMatch === void 0 ? void 0 : routeMatch.route.lazy)) {
                throw getInternalRouterError(400, {
                    method: request.method,
                    pathname: new URL(request.url).pathname,
                    routeId: routeMatch === null || routeMatch === void 0 ? void 0 : routeMatch.route.id
                });
            }
            var dsMatches;
            if (routeMatch) {
                dsMatches = getTargetedDataStrategyMatches(mapRouteProperties2, manifest, request, matches, routeMatch, [], requestContext);
            } else {
                var maxIdx = pendingActionResult && isErrorResult(pendingActionResult[1]) ? // Up to but not including the boundary
                matches.findIndex((m)=>m.route.id === pendingActionResult[0]) - 1 : void 0;
                var pattern = getRoutePattern(matches);
                dsMatches = matches.map((match, index)=>{
                    if (maxIdx != null && index > maxIdx) {
                        return getDataStrategyMatch(mapRouteProperties2, manifest, request, pattern, match, [], requestContext, false);
                    }
                    return getDataStrategyMatch(mapRouteProperties2, manifest, request, pattern, match, [], requestContext, (match.route.loader || match.route.lazy) != null && (!filterMatchesToLoad || filterMatchesToLoad(match)));
                });
            }
            if (!dataStrategy && !dsMatches.some((m)=>m.shouldLoad)) {
                return {
                    matches,
                    loaderData: {},
                    errors: pendingActionResult && isErrorResult(pendingActionResult[1]) ? {
                        [pendingActionResult[0]]: pendingActionResult[1].error
                    } : null,
                    statusCode: 200,
                    loaderHeaders: {}
                };
            }
            var results = yield callDataStrategy(request, dsMatches, isRouteRequest, requestContext, dataStrategy);
            if (request.signal.aborted) {
                throwStaticHandlerAbortedError(request, isRouteRequest);
            }
            var handlerContext = processRouteLoaderData(matches, results, pendingActionResult, true, skipLoaderErrorBubbling);
            return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, handlerContext), {
                matches
            });
        })();
    }
    function callDataStrategy(request, matches, isRouteRequest, requestContext, dataStrategy) {
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var results = yield callDataStrategyImpl(dataStrategy || defaultDataStrategy, request, matches, null, requestContext, true);
            var dataResults = {};
            yield Promise.all(matches.map((match)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                    if (!(match.route.id in results)) {
                        return;
                    }
                    var result = results[match.route.id];
                    if (isRedirectDataStrategyResult(result)) {
                        var response = result.result;
                        throw normalizeRelativeRoutingRedirectResponse(response, request, match.route.id, matches, basename);
                    }
                    if (isRouteRequest) {
                        if (isResponse(result.result)) {
                            throw result;
                        } else if (isDataWithResponseInit(result.result)) {
                            throw dataWithResponseInitToResponse(result.result);
                        }
                    }
                    dataResults[match.route.id] = yield convertDataStrategyResultToDataResult(result);
                })()));
            return dataResults;
        })();
    }
    return {
        dataRoutes,
        query,
        queryRoute
    };
}
function getStaticContextFromError(routes, handlerContext, error, boundaryId) {
    var errorBoundaryId = boundaryId || handlerContext._deepestRenderedBoundaryId || routes[0].id;
    return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, handlerContext), {
        statusCode: isRouteErrorResponse(error) ? error.status : 500,
        errors: {
            [errorBoundaryId]: error
        }
    });
}
function throwStaticHandlerAbortedError(request, isRouteRequest) {
    if (request.signal.reason !== void 0) {
        throw request.signal.reason;
    }
    var method = isRouteRequest ? "queryRoute" : "query";
    throw new Error("".concat(method, "() call aborted without an `AbortSignal.reason`: ").concat(request.method, " ").concat(request.url));
}
function isSubmissionNavigation(opts) {
    return opts != null && ("formData" in opts && opts.formData != null || "body" in opts && opts.body !== void 0);
}
function normalizeTo(location, matches, basename, to, fromRouteId, relative) {
    var contextualMatches;
    var activeRouteMatch;
    if (fromRouteId) {
        contextualMatches = [];
        for (var match of matches){
            contextualMatches.push(match);
            if (match.route.id === fromRouteId) {
                activeRouteMatch = match;
                break;
            }
        }
    } else {
        contextualMatches = matches;
        activeRouteMatch = matches[matches.length - 1];
    }
    var path = resolveTo(to ? to : ".", getResolveToMatches(contextualMatches), stripBasename(location.pathname, basename) || location.pathname, relative === "path");
    if (to == null) {
        path.search = location.search;
        path.hash = location.hash;
    }
    if ((to == null || to === "" || to === ".") && activeRouteMatch) {
        var nakedIndex = hasNakedIndexQuery(path.search);
        if (activeRouteMatch.route.index && !nakedIndex) {
            path.search = path.search ? path.search.replace(/^\?/, "?index&") : "?index";
        } else if (!activeRouteMatch.route.index && nakedIndex) {
            var params = new URLSearchParams(path.search);
            var indexValues = params.getAll("index");
            params.delete("index");
            indexValues.filter((v)=>v).forEach((v)=>params.append("index", v));
            var qs = params.toString();
            path.search = qs ? "?".concat(qs) : "";
        }
    }
    if (basename !== "/") {
        path.pathname = prependBasename({
            basename,
            pathname: path.pathname
        });
    }
    return createPath(path);
}
function normalizeNavigateOptions(isFetcher, path, opts) {
    if (!opts || !isSubmissionNavigation(opts)) {
        return {
            path
        };
    }
    if (opts.formMethod && !isValidMethod(opts.formMethod)) {
        return {
            path,
            error: getInternalRouterError(405, {
                method: opts.formMethod
            })
        };
    }
    var getInvalidBodyError = ()=>({
            path,
            error: getInternalRouterError(400, {
                type: "invalid-body"
            })
        });
    var rawFormMethod = opts.formMethod || "get";
    var formMethod = rawFormMethod.toUpperCase();
    var formAction = stripHashFromPath(path);
    if (opts.body !== void 0) {
        if (opts.formEncType === "text/plain") {
            if (!isMutationMethod(formMethod)) {
                return getInvalidBodyError();
            }
            var text = typeof opts.body === "string" ? opts.body : opts.body instanceof FormData || opts.body instanceof URLSearchParams ? // https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#plain-text-form-data
            Array.from(opts.body.entries()).reduce((acc, param)=>{
                var [name, value] = param;
                return "".concat(acc).concat(name, "=").concat(value, "\n");
            }, "") : String(opts.body);
            return {
                path,
                submission: {
                    formMethod,
                    formAction,
                    formEncType: opts.formEncType,
                    formData: void 0,
                    json: void 0,
                    text
                }
            };
        } else if (opts.formEncType === "application/json") {
            if (!isMutationMethod(formMethod)) {
                return getInvalidBodyError();
            }
            try {
                var json = typeof opts.body === "string" ? JSON.parse(opts.body) : opts.body;
                return {
                    path,
                    submission: {
                        formMethod,
                        formAction,
                        formEncType: opts.formEncType,
                        formData: void 0,
                        json,
                        text: void 0
                    }
                };
            } catch (e) {
                return getInvalidBodyError();
            }
        }
    }
    invariant(typeof FormData === "function", "FormData is not available in this environment");
    var searchParams;
    var formData;
    if (opts.formData) {
        searchParams = convertFormDataToSearchParams(opts.formData);
        formData = opts.formData;
    } else if (opts.body instanceof FormData) {
        searchParams = convertFormDataToSearchParams(opts.body);
        formData = opts.body;
    } else if (opts.body instanceof URLSearchParams) {
        searchParams = opts.body;
        formData = convertSearchParamsToFormData(searchParams);
    } else if (opts.body == null) {
        searchParams = new URLSearchParams();
        formData = new FormData();
    } else {
        try {
            searchParams = new URLSearchParams(opts.body);
            formData = convertSearchParamsToFormData(searchParams);
        } catch (e) {
            return getInvalidBodyError();
        }
    }
    var submission = {
        formMethod,
        formAction,
        formEncType: opts && opts.formEncType || "application/x-www-form-urlencoded",
        formData,
        json: void 0,
        text: void 0
    };
    if (isMutationMethod(submission.formMethod)) {
        return {
            path,
            submission
        };
    }
    var parsedPath = parsePath(path);
    if (isFetcher && parsedPath.search && hasNakedIndexQuery(parsedPath.search)) {
        searchParams.append("index", "");
    }
    parsedPath.search = "?".concat(searchParams);
    return {
        path: createPath(parsedPath),
        submission
    };
}
function getMatchesToLoad(request, scopedContext, mapRouteProperties2, manifest, history, state, matches, submission, location, lazyRoutePropertiesToSkip, initialHydration, isRevalidationRequired, cancelledFetcherLoads, fetchersQueuedForDeletion, fetchLoadMatches, fetchRedirectIds, routesToUse, basename, hasPatchRoutesOnNavigation, pendingActionResult, callSiteDefaultShouldRevalidate) {
    var _state_matches_;
    var actionResult = pendingActionResult ? isErrorResult(pendingActionResult[1]) ? pendingActionResult[1].error : pendingActionResult[1].data : void 0;
    var currentUrl = history.createURL(state.location);
    var nextUrl = history.createURL(location);
    var maxIdx;
    if (initialHydration && state.errors) {
        var boundaryId = Object.keys(state.errors)[0];
        maxIdx = matches.findIndex((m)=>m.route.id === boundaryId);
    } else if (pendingActionResult && isErrorResult(pendingActionResult[1])) {
        var boundaryId1 = pendingActionResult[0];
        maxIdx = matches.findIndex((m)=>m.route.id === boundaryId1) - 1;
    }
    var actionStatus = pendingActionResult ? pendingActionResult[1].statusCode : void 0;
    var shouldSkipRevalidation = actionStatus && actionStatus >= 400;
    var baseShouldRevalidateArgs = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({
        currentUrl,
        currentParams: ((_state_matches_ = state.matches[0]) === null || _state_matches_ === void 0 ? void 0 : _state_matches_.params) || {},
        nextUrl,
        nextParams: matches[0].params
    }, submission), {
        actionResult,
        actionStatus
    });
    var pattern = getRoutePattern(matches);
    var dsMatches = matches.map((match, index)=>{
        var { route } = match;
        var forceShouldLoad = null;
        if (maxIdx != null && index > maxIdx) {
            forceShouldLoad = false;
        } else if (route.lazy) {
            forceShouldLoad = true;
        } else if (!routeHasLoaderOrMiddleware(route)) {
            forceShouldLoad = false;
        } else if (initialHydration) {
            forceShouldLoad = shouldLoadRouteOnHydration(route, state.loaderData, state.errors);
        } else if (isNewLoader(state.loaderData, state.matches[index], match)) {
            forceShouldLoad = true;
        }
        if (forceShouldLoad !== null) {
            return getDataStrategyMatch(mapRouteProperties2, manifest, request, pattern, match, lazyRoutePropertiesToSkip, scopedContext, forceShouldLoad);
        }
        var defaultShouldRevalidate = false;
        if (typeof callSiteDefaultShouldRevalidate === "boolean") {
            defaultShouldRevalidate = callSiteDefaultShouldRevalidate;
        } else if (shouldSkipRevalidation) {
            defaultShouldRevalidate = false;
        } else if (isRevalidationRequired) {
            defaultShouldRevalidate = true;
        } else if (currentUrl.pathname + currentUrl.search === nextUrl.pathname + nextUrl.search) {
            defaultShouldRevalidate = true;
        } else if (currentUrl.search !== nextUrl.search) {
            defaultShouldRevalidate = true;
        } else if (isNewRouteInstance(state.matches[index], match)) {
            defaultShouldRevalidate = true;
        }
        var shouldRevalidateArgs = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, baseShouldRevalidateArgs), {
            defaultShouldRevalidate
        });
        var shouldLoad = shouldRevalidateLoader(match, shouldRevalidateArgs);
        return getDataStrategyMatch(mapRouteProperties2, manifest, request, pattern, match, lazyRoutePropertiesToSkip, scopedContext, shouldLoad, shouldRevalidateArgs, callSiteDefaultShouldRevalidate);
    });
    var revalidatingFetchers = [];
    fetchLoadMatches.forEach((f, key)=>{
        if (initialHydration || !matches.some((m)=>m.route.id === f.routeId) || fetchersQueuedForDeletion.has(key)) {
            return;
        }
        var fetcher = state.fetchers.get(key);
        var isMidInitialLoad = fetcher && fetcher.state !== "idle" && fetcher.data === void 0;
        var fetcherMatches = matchRoutes(routesToUse, f.path, basename);
        if (!fetcherMatches) {
            if (hasPatchRoutesOnNavigation && isMidInitialLoad) {
                return;
            }
            revalidatingFetchers.push({
                key,
                routeId: f.routeId,
                path: f.path,
                matches: null,
                match: null,
                request: null,
                controller: null
            });
            return;
        }
        if (fetchRedirectIds.has(key)) {
            return;
        }
        var fetcherMatch = getTargetMatch(fetcherMatches, f.path);
        var fetchController = new AbortController();
        var fetchRequest = createClientSideRequest(history, f.path, fetchController.signal);
        var fetcherDsMatches = null;
        if (cancelledFetcherLoads.has(key)) {
            cancelledFetcherLoads.delete(key);
            fetcherDsMatches = getTargetedDataStrategyMatches(mapRouteProperties2, manifest, fetchRequest, fetcherMatches, fetcherMatch, lazyRoutePropertiesToSkip, scopedContext);
        } else if (isMidInitialLoad) {
            if (isRevalidationRequired) {
                fetcherDsMatches = getTargetedDataStrategyMatches(mapRouteProperties2, manifest, fetchRequest, fetcherMatches, fetcherMatch, lazyRoutePropertiesToSkip, scopedContext);
            }
        } else {
            var defaultShouldRevalidate;
            if (typeof callSiteDefaultShouldRevalidate === "boolean") {
                defaultShouldRevalidate = callSiteDefaultShouldRevalidate;
            } else if (shouldSkipRevalidation) {
                defaultShouldRevalidate = false;
            } else {
                defaultShouldRevalidate = isRevalidationRequired;
            }
            var shouldRevalidateArgs = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, baseShouldRevalidateArgs), {
                defaultShouldRevalidate
            });
            if (shouldRevalidateLoader(fetcherMatch, shouldRevalidateArgs)) {
                fetcherDsMatches = getTargetedDataStrategyMatches(mapRouteProperties2, manifest, fetchRequest, fetcherMatches, fetcherMatch, lazyRoutePropertiesToSkip, scopedContext, shouldRevalidateArgs);
            }
        }
        if (fetcherDsMatches) {
            revalidatingFetchers.push({
                key,
                routeId: f.routeId,
                path: f.path,
                matches: fetcherDsMatches,
                match: fetcherMatch,
                request: fetchRequest,
                controller: fetchController
            });
        }
    });
    return {
        dsMatches,
        revalidatingFetchers
    };
}
function routeHasLoaderOrMiddleware(route) {
    return route.loader != null || route.middleware != null && route.middleware.length > 0;
}
function shouldLoadRouteOnHydration(route, loaderData, errors) {
    if (route.lazy) {
        return true;
    }
    if (!routeHasLoaderOrMiddleware(route)) {
        return false;
    }
    var hasData = loaderData != null && route.id in loaderData;
    var hasError = errors != null && errors[route.id] !== void 0;
    if (!hasData && hasError) {
        return false;
    }
    if (typeof route.loader === "function" && route.loader.hydrate === true) {
        return true;
    }
    return !hasData && !hasError;
}
function isNewLoader(currentLoaderData, currentMatch, match) {
    var isNew = // [a] -> [a, b]
    !currentMatch || // [a, b] -> [a, c]
    match.route.id !== currentMatch.route.id;
    var isMissingData = !currentLoaderData.hasOwnProperty(match.route.id);
    return isNew || isMissingData;
}
function isNewRouteInstance(currentMatch, match) {
    var currentPath = currentMatch.route.path;
    return(// param change for this match, /users/123 -> /users/456
    currentMatch.pathname !== match.pathname || // splat param changed, which is not present in match.path
    // e.g. /files/images/avatar.jpg -> files/finances.xls
    currentPath != null && currentPath.endsWith("*") && currentMatch.params["*"] !== match.params["*"]);
}
function shouldRevalidateLoader(loaderMatch, arg) {
    if (loaderMatch.route.shouldRevalidate) {
        var routeChoice = loaderMatch.route.shouldRevalidate(arg);
        if (typeof routeChoice === "boolean") {
            return routeChoice;
        }
    }
    return arg.defaultShouldRevalidate;
}
function patchRoutesImpl(routeId, children, routesToUse, manifest, mapRouteProperties2, allowElementMutations) {
    var childrenToPatch;
    if (routeId) {
        var route = manifest[routeId];
        invariant(route, "No route found to patch children into: routeId = ".concat(routeId));
        if (!route.children) {
            route.children = [];
        }
        childrenToPatch = route.children;
    } else {
        childrenToPatch = routesToUse;
    }
    var uniqueChildren = [];
    var existingChildren = [];
    children.forEach((newRoute)=>{
        var existingRoute = childrenToPatch.find((existingRoute2)=>isSameRoute(newRoute, existingRoute2));
        if (existingRoute) {
            existingChildren.push({
                existingRoute,
                newRoute
            });
        } else {
            uniqueChildren.push(newRoute);
        }
    });
    if (uniqueChildren.length > 0) {
        var newRoutes = convertRoutesToDataRoutes(uniqueChildren, mapRouteProperties2, [
            routeId || "_",
            "patch",
            String((childrenToPatch === null || childrenToPatch === void 0 ? void 0 : childrenToPatch.length) || "0")
        ], manifest);
        childrenToPatch.push(...newRoutes);
    }
    if (allowElementMutations && existingChildren.length > 0) {
        for(var i = 0; i < existingChildren.length; i++){
            var { existingRoute, newRoute } = existingChildren[i];
            var existingRouteTyped = existingRoute;
            var [newRouteTyped] = convertRoutesToDataRoutes([
                newRoute
            ], mapRouteProperties2, [], // Doesn't matter for mutated routes since they already have an id
            {}, // Don't touch the manifest here since we're updating in place
            true);
            Object.assign(existingRouteTyped, {
                element: newRouteTyped.element ? newRouteTyped.element : existingRouteTyped.element,
                errorElement: newRouteTyped.errorElement ? newRouteTyped.errorElement : existingRouteTyped.errorElement,
                hydrateFallbackElement: newRouteTyped.hydrateFallbackElement ? newRouteTyped.hydrateFallbackElement : existingRouteTyped.hydrateFallbackElement
            });
        }
    }
}
function isSameRoute(newRoute, existingRoute) {
    if ("id" in newRoute && "id" in existingRoute && newRoute.id === existingRoute.id) {
        return true;
    }
    if (!(newRoute.index === existingRoute.index && newRoute.path === existingRoute.path && newRoute.caseSensitive === existingRoute.caseSensitive)) {
        return false;
    }
    if ((!newRoute.children || newRoute.children.length === 0) && (!existingRoute.children || existingRoute.children.length === 0)) {
        return true;
    }
    return newRoute.children.every((aChild, i)=>{
        var _existingRoute_children;
        return (_existingRoute_children = existingRoute.children) === null || _existingRoute_children === void 0 ? void 0 : _existingRoute_children.some((bChild)=>isSameRoute(aChild, bChild));
    });
}
var lazyRoutePropertyCache = /* @__PURE__ */ new WeakMap();
var loadLazyRouteProperty = (param)=>{
    var { key, route, manifest, mapRouteProperties: mapRouteProperties2 } = param;
    var routeToUpdate = manifest[route.id];
    invariant(routeToUpdate, "No route found in manifest");
    if (!routeToUpdate.lazy || typeof routeToUpdate.lazy !== "object") {
        return;
    }
    var lazyFn = routeToUpdate.lazy[key];
    if (!lazyFn) {
        return;
    }
    var cache = lazyRoutePropertyCache.get(routeToUpdate);
    if (!cache) {
        cache = {};
        lazyRoutePropertyCache.set(routeToUpdate, cache);
    }
    var cachedPromise = cache[key];
    if (cachedPromise) {
        return cachedPromise;
    }
    var propertyPromise = (()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var isUnsupported = isUnsupportedLazyRouteObjectKey(key);
            var staticRouteValue = routeToUpdate[key];
            var isStaticallyDefined = staticRouteValue !== void 0 && key !== "hasErrorBoundary";
            if (isUnsupported) {
                warning(!isUnsupported, "Route property " + key + " is not a supported lazy route property. This property will be ignored.");
                cache[key] = Promise.resolve();
            } else if (isStaticallyDefined) {
                warning(false, 'Route "'.concat(routeToUpdate.id, '" has a static property "').concat(key, '" defined. The lazy property will be ignored.'));
            } else {
                var value = yield lazyFn();
                if (value != null) {
                    Object.assign(routeToUpdate, {
                        [key]: value
                    });
                    Object.assign(routeToUpdate, mapRouteProperties2(routeToUpdate));
                }
            }
            if (typeof routeToUpdate.lazy === "object") {
                routeToUpdate.lazy[key] = void 0;
                if (Object.values(routeToUpdate.lazy).every((value)=>value === void 0)) {
                    routeToUpdate.lazy = void 0;
                }
            }
        })())();
    cache[key] = propertyPromise;
    return propertyPromise;
};
var lazyRouteFunctionCache = /* @__PURE__ */ new WeakMap();
function loadLazyRoute(route, type, manifest, mapRouteProperties2, lazyRoutePropertiesToSkip) {
    var routeToUpdate = manifest[route.id];
    invariant(routeToUpdate, "No route found in manifest");
    if (!route.lazy) {
        return {
            lazyRoutePromise: void 0,
            lazyHandlerPromise: void 0
        };
    }
    if (typeof route.lazy === "function") {
        var cachedPromise = lazyRouteFunctionCache.get(routeToUpdate);
        if (cachedPromise) {
            return {
                lazyRoutePromise: cachedPromise,
                lazyHandlerPromise: cachedPromise
            };
        }
        var lazyRoutePromise2 = (()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                invariant(typeof route.lazy === "function", "No lazy route function found");
                var lazyRoute = yield route.lazy();
                var routeUpdates = {};
                for(var lazyRouteProperty in lazyRoute){
                    var lazyValue = lazyRoute[lazyRouteProperty];
                    if (lazyValue === void 0) {
                        continue;
                    }
                    var isUnsupported = isUnsupportedLazyRouteFunctionKey(lazyRouteProperty);
                    var staticRouteValue = routeToUpdate[lazyRouteProperty];
                    var isStaticallyDefined = staticRouteValue !== void 0 && // This property isn't static since it should always be updated based
                    // on the route updates
                    lazyRouteProperty !== "hasErrorBoundary";
                    if (isUnsupported) {
                        warning(!isUnsupported, "Route property " + lazyRouteProperty + " is not a supported property to be returned from a lazy route function. This property will be ignored.");
                    } else if (isStaticallyDefined) {
                        warning(!isStaticallyDefined, 'Route "'.concat(routeToUpdate.id, '" has a static property "').concat(lazyRouteProperty, '" defined but its lazy function is also returning a value for this property. The lazy route property "').concat(lazyRouteProperty, '" will be ignored.'));
                    } else {
                        routeUpdates[lazyRouteProperty] = lazyValue;
                    }
                }
                Object.assign(routeToUpdate, routeUpdates);
                Object.assign(routeToUpdate, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, mapRouteProperties2(routeToUpdate)), {
                    lazy: void 0
                }));
            })())();
        lazyRouteFunctionCache.set(routeToUpdate, lazyRoutePromise2);
        lazyRoutePromise2.catch(()=>{});
        return {
            lazyRoutePromise: lazyRoutePromise2,
            lazyHandlerPromise: lazyRoutePromise2
        };
    }
    var lazyKeys = Object.keys(route.lazy);
    var lazyPropertyPromises = [];
    var lazyHandlerPromise = void 0;
    for (var key of lazyKeys){
        if (lazyRoutePropertiesToSkip && lazyRoutePropertiesToSkip.includes(key)) {
            continue;
        }
        var promise = loadLazyRouteProperty({
            key,
            route,
            manifest,
            mapRouteProperties: mapRouteProperties2
        });
        if (promise) {
            lazyPropertyPromises.push(promise);
            if (key === type) {
                lazyHandlerPromise = promise;
            }
        }
    }
    var lazyRoutePromise = lazyPropertyPromises.length > 0 ? Promise.all(lazyPropertyPromises).then(()=>{}) : void 0;
    lazyRoutePromise === null || lazyRoutePromise === void 0 ? void 0 : lazyRoutePromise.catch(()=>{});
    lazyHandlerPromise === null || lazyHandlerPromise === void 0 ? void 0 : lazyHandlerPromise.catch(()=>{});
    return {
        lazyRoutePromise,
        lazyHandlerPromise
    };
}
function isNonNullable(value) {
    return value !== void 0;
}
function loadLazyMiddlewareForMatches(matches, manifest, mapRouteProperties2) {
    var promises = matches.map((param)=>{
        var { route } = param;
        if (typeof route.lazy !== "object" || !route.lazy.middleware) {
            return void 0;
        }
        return loadLazyRouteProperty({
            key: "middleware",
            route,
            manifest,
            mapRouteProperties: mapRouteProperties2
        });
    }).filter(isNonNullable);
    return promises.length > 0 ? Promise.all(promises) : void 0;
}
function defaultDataStrategy(args) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var matchesToLoad = args.matches.filter((m)=>m.shouldLoad);
        var keyedResults = {};
        var results = yield Promise.all(matchesToLoad.map((m)=>m.resolve()));
        results.forEach((result, i)=>{
            keyedResults[matchesToLoad[i].route.id] = result;
        });
        return keyedResults;
    })();
}
function defaultDataStrategyWithMiddleware(args) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        if (!args.matches.some((m)=>m.route.middleware)) {
            return defaultDataStrategy(args);
        }
        return runClientMiddlewarePipeline(args, ()=>defaultDataStrategy(args));
    })();
}
function runServerMiddlewarePipeline(args, handler, errorHandler) {
    return runMiddlewarePipeline(args, handler, processResult, isResponse, errorHandler);
    function processResult(result) {
        return isDataWithResponseInit(result) ? dataWithResponseInitToResponse(result) : result;
    }
}
function runClientMiddlewarePipeline(args, handler) {
    return runMiddlewarePipeline(args, handler, (r)=>{
        if (isRedirectResponse(r)) {
            throw r;
        }
        return r;
    }, isDataStrategyResults, errorHandler);
    function errorHandler(error, routeId, nextResult) {
        if (nextResult) {
            return Promise.resolve(Object.assign(nextResult.value, {
                [routeId]: {
                    type: "error",
                    result: error
                }
            }));
        } else {
            var { matches } = args;
            var maxBoundaryIdx = Math.min(// Throwing route
            Math.max(matches.findIndex((m)=>m.route.id === routeId), 0), // or the shallowest route that needs to load data
            Math.max(matches.findIndex((m)=>m.shouldCallHandler()), 0));
            var boundaryRouteId = findNearestBoundary(matches, matches[maxBoundaryIdx].route.id).route.id;
            return Promise.resolve({
                [boundaryRouteId]: {
                    type: "error",
                    result: error
                }
            });
        }
    }
}
function runMiddlewarePipeline(args, handler, processResult, isResult, errorHandler) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var { matches, request, params, context, unstable_pattern } = args;
        var tuples = matches.flatMap((m)=>m.route.middleware ? m.route.middleware.map((fn)=>[
                    m.route.id,
                    fn
                ]) : []);
        var result = yield callRouteMiddleware({
            request,
            params,
            context,
            unstable_pattern
        }, tuples, handler, processResult, isResult, errorHandler);
        return result;
    })();
}
function callRouteMiddleware(args, middlewares, handler, processResult, isResult, errorHandler) {
    var idx = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : 0;
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var { request } = args;
        if (request.signal.aborted) {
            var _request_signal_reason;
            throw (_request_signal_reason = request.signal.reason) !== null && _request_signal_reason !== void 0 ? _request_signal_reason : new Error("Request aborted: ".concat(request.method, " ").concat(request.url));
        }
        var tuple = middlewares[idx];
        if (!tuple) {
            var result = yield handler();
            return result;
        }
        var [routeId, middleware] = tuple;
        var nextResult;
        var next = ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                if (nextResult) {
                    throw new Error("You may only call `next()` once per middleware");
                }
                try {
                    var result = yield callRouteMiddleware(args, middlewares, handler, processResult, isResult, errorHandler, idx + 1);
                    nextResult = {
                        value: result
                    };
                    return nextResult.value;
                } catch (error) {
                    nextResult = {
                        value: yield errorHandler(error, routeId, nextResult)
                    };
                    return nextResult.value;
                }
            })();
        try {
            var value = yield middleware(args, next);
            var result1 = value != null ? processResult(value) : void 0;
            if (isResult(result1)) {
                return result1;
            } else if (nextResult) {
                return result1 !== null && result1 !== void 0 ? result1 : nextResult.value;
            } else {
                nextResult = {
                    value: yield next()
                };
                return nextResult.value;
            }
        } catch (error) {
            var response = yield errorHandler(error, routeId, nextResult);
            return response;
        }
    })();
}
function getDataStrategyMatchLazyPromises(mapRouteProperties2, manifest, request, match, lazyRoutePropertiesToSkip) {
    var lazyMiddlewarePromise = loadLazyRouteProperty({
        key: "middleware",
        route: match.route,
        manifest,
        mapRouteProperties: mapRouteProperties2
    });
    var lazyRoutePromises = loadLazyRoute(match.route, isMutationMethod(request.method) ? "action" : "loader", manifest, mapRouteProperties2, lazyRoutePropertiesToSkip);
    return {
        middleware: lazyMiddlewarePromise,
        route: lazyRoutePromises.lazyRoutePromise,
        handler: lazyRoutePromises.lazyHandlerPromise
    };
}
function getDataStrategyMatch(mapRouteProperties2, manifest, request, unstable_pattern, match, lazyRoutePropertiesToSkip, scopedContext, shouldLoad) {
    var shouldRevalidateArgs = arguments.length > 8 && arguments[8] !== void 0 ? arguments[8] : null, callSiteDefaultShouldRevalidate = arguments.length > 9 ? arguments[9] : void 0;
    var isUsingNewApi = false;
    var _lazyPromises = getDataStrategyMatchLazyPromises(mapRouteProperties2, manifest, request, match, lazyRoutePropertiesToSkip);
    return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, match), {
        _lazyPromises,
        shouldLoad,
        shouldRevalidateArgs,
        shouldCallHandler (defaultShouldRevalidate) {
            isUsingNewApi = true;
            if (!shouldRevalidateArgs) {
                return shouldLoad;
            }
            if (typeof callSiteDefaultShouldRevalidate === "boolean") {
                return shouldRevalidateLoader(match, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, shouldRevalidateArgs), {
                    defaultShouldRevalidate: callSiteDefaultShouldRevalidate
                }));
            }
            if (typeof defaultShouldRevalidate === "boolean") {
                return shouldRevalidateLoader(match, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, shouldRevalidateArgs), {
                    defaultShouldRevalidate
                }));
            }
            return shouldRevalidateLoader(match, shouldRevalidateArgs);
        },
        resolve (handlerOverride) {
            var { lazy, loader, middleware } = match.route;
            var callHandler = isUsingNewApi || shouldLoad || handlerOverride && !isMutationMethod(request.method) && (lazy || loader);
            var isMiddlewareOnlyRoute = middleware && middleware.length > 0 && !loader && !lazy;
            if (callHandler && (isMutationMethod(request.method) || !isMiddlewareOnlyRoute)) {
                return callLoaderOrAction({
                    request,
                    unstable_pattern,
                    match,
                    lazyHandlerPromise: _lazyPromises === null || _lazyPromises === void 0 ? void 0 : _lazyPromises.handler,
                    lazyRoutePromise: _lazyPromises === null || _lazyPromises === void 0 ? void 0 : _lazyPromises.route,
                    handlerOverride,
                    scopedContext
                });
            }
            return Promise.resolve({
                type: "data" /* data */ ,
                result: void 0
            });
        }
    });
}
function getTargetedDataStrategyMatches(mapRouteProperties2, manifest, request, matches, targetMatch, lazyRoutePropertiesToSkip, scopedContext) {
    var shouldRevalidateArgs = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : null;
    return matches.map((match)=>{
        if (match.route.id !== targetMatch.route.id) {
            return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, match), {
                shouldLoad: false,
                shouldRevalidateArgs,
                shouldCallHandler: ()=>false,
                _lazyPromises: getDataStrategyMatchLazyPromises(mapRouteProperties2, manifest, request, match, lazyRoutePropertiesToSkip),
                resolve: ()=>Promise.resolve({
                        type: "data",
                        result: void 0
                    })
            });
        }
        return getDataStrategyMatch(mapRouteProperties2, manifest, request, getRoutePattern(matches), match, lazyRoutePropertiesToSkip, scopedContext, true, shouldRevalidateArgs);
    });
}
function callDataStrategyImpl(dataStrategyImpl, request, matches, fetcherKey, scopedContext, isStaticHandler) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        if (matches.some((m)=>{
            var _m__lazyPromises;
            return (_m__lazyPromises = m._lazyPromises) === null || _m__lazyPromises === void 0 ? void 0 : _m__lazyPromises.middleware;
        })) {
            yield Promise.all(matches.map((m)=>{
                var _m__lazyPromises;
                return (_m__lazyPromises = m._lazyPromises) === null || _m__lazyPromises === void 0 ? void 0 : _m__lazyPromises.middleware;
            }));
        }
        var dataStrategyArgs = {
            request,
            unstable_pattern: getRoutePattern(matches),
            params: matches[0].params,
            context: scopedContext,
            matches
        };
        var runClientMiddleware = isStaticHandler ? ()=>{
            throw new Error("You cannot call `runClientMiddleware()` from a static handler `dataStrategy`. Middleware is run outside of `dataStrategy` during SSR in order to bubble up the Response.  You can enable middleware via the `respond` API in `query`/`queryRoute`");
        } : (cb)=>{
            var typedDataStrategyArgs = dataStrategyArgs;
            return runClientMiddlewarePipeline(typedDataStrategyArgs, ()=>{
                return cb((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, typedDataStrategyArgs), {
                    fetcherKey,
                    runClientMiddleware: ()=>{
                        throw new Error("Cannot call `runClientMiddleware()` from within an `runClientMiddleware` handler");
                    }
                }));
            });
        };
        var results = yield dataStrategyImpl((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, dataStrategyArgs), {
            fetcherKey,
            runClientMiddleware
        }));
        try {
            yield Promise.all(matches.flatMap((m)=>{
                var _m__lazyPromises, _m__lazyPromises1;
                return [
                    (_m__lazyPromises = m._lazyPromises) === null || _m__lazyPromises === void 0 ? void 0 : _m__lazyPromises.handler,
                    (_m__lazyPromises1 = m._lazyPromises) === null || _m__lazyPromises1 === void 0 ? void 0 : _m__lazyPromises1.route
                ];
            }));
        } catch (e) {}
        return results;
    })();
}
function callLoaderOrAction(_0) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*(param) {
        var { request, unstable_pattern, match, lazyHandlerPromise, lazyRoutePromise, handlerOverride, scopedContext } = param;
        var result;
        var onReject;
        var isAction = isMutationMethod(request.method);
        var type = isAction ? "action" : "loader";
        var runHandler = (handler)=>{
            var reject;
            var abortPromise = new Promise((_, r)=>reject = r);
            onReject = ()=>reject();
            request.signal.addEventListener("abort", onReject);
            var actualHandler = (ctx)=>{
                if (typeof handler !== "function") {
                    return Promise.reject(new Error('You cannot call the handler for a route which defines a boolean "'.concat(type, '" [routeId: ').concat(match.route.id, "]")));
                }
                return handler({
                    request,
                    unstable_pattern,
                    params: match.params,
                    context: scopedContext
                }, ...ctx !== void 0 ? [
                    ctx
                ] : []);
            };
            var handlerPromise = (()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                    try {
                        var val = yield handlerOverride ? handlerOverride((ctx)=>actualHandler(ctx)) : actualHandler();
                        return {
                            type: "data",
                            result: val
                        };
                    } catch (e) {
                        return {
                            type: "error",
                            result: e
                        };
                    }
                })())();
            return Promise.race([
                handlerPromise,
                abortPromise
            ]);
        };
        try {
            var handler = isAction ? match.route.action : match.route.loader;
            if (lazyHandlerPromise || lazyRoutePromise) {
                if (handler) {
                    var handlerError;
                    var [value] = yield Promise.all([
                        // If the handler throws, don't let it immediately bubble out,
                        // since we need to let the lazy() execution finish so we know if this
                        // route has a boundary that can handle the error
                        runHandler(handler).catch((e)=>{
                            handlerError = e;
                        }),
                        // Ensure all lazy route promises are resolved before continuing
                        lazyHandlerPromise,
                        lazyRoutePromise
                    ]);
                    if (handlerError !== void 0) {
                        throw handlerError;
                    }
                    result = value;
                } else {
                    yield lazyHandlerPromise;
                    var handler2 = isAction ? match.route.action : match.route.loader;
                    if (handler2) {
                        [result] = yield Promise.all([
                            runHandler(handler2),
                            lazyRoutePromise
                        ]);
                    } else if (type === "action") {
                        var url = new URL(request.url);
                        var pathname = url.pathname + url.search;
                        throw getInternalRouterError(405, {
                            method: request.method,
                            pathname,
                            routeId: match.route.id
                        });
                    } else {
                        return {
                            type: "data" /* data */ ,
                            result: void 0
                        };
                    }
                }
            } else if (!handler) {
                var url1 = new URL(request.url);
                var pathname1 = url1.pathname + url1.search;
                throw getInternalRouterError(404, {
                    pathname: pathname1
                });
            } else {
                result = yield runHandler(handler);
            }
        } catch (e) {
            return {
                type: "error" /* error */ ,
                result: e
            };
        } finally{
            if (onReject) {
                request.signal.removeEventListener("abort", onReject);
            }
        }
        return result;
    }).apply(this, arguments);
}
function parseResponseBody(response) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var contentType = response.headers.get("Content-Type");
        if (contentType && /\bapplication\/json\b/.test(contentType)) {
            return response.body == null ? null : response.json();
        }
        return response.text();
    })();
}
function convertDataStrategyResultToDataResult(dataStrategyResult) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var { result, type } = dataStrategyResult;
        if (isResponse(result)) {
            var data2;
            try {
                data2 = yield parseResponseBody(result);
            } catch (e) {
                return {
                    type: "error" /* error */ ,
                    error: e
                };
            }
            if (type === "error" /* error */ ) {
                return {
                    type: "error" /* error */ ,
                    error: new ErrorResponseImpl(result.status, result.statusText, data2),
                    statusCode: result.status,
                    headers: result.headers
                };
            }
            return {
                type: "data" /* data */ ,
                data: data2,
                statusCode: result.status,
                headers: result.headers
            };
        }
        if (type === "error" /* error */ ) {
            if (isDataWithResponseInit(result)) {
                var _result_init;
                if (result.data instanceof Error) {
                    var _result_init1, _result_init2;
                    return {
                        type: "error" /* error */ ,
                        error: result.data,
                        statusCode: (_result_init1 = result.init) === null || _result_init1 === void 0 ? void 0 : _result_init1.status,
                        headers: ((_result_init2 = result.init) === null || _result_init2 === void 0 ? void 0 : _result_init2.headers) ? new Headers(result.init.headers) : void 0
                    };
                }
                return {
                    type: "error" /* error */ ,
                    error: dataWithResponseInitToErrorResponse(result),
                    statusCode: isRouteErrorResponse(result) ? result.status : void 0,
                    headers: ((_result_init = result.init) === null || _result_init === void 0 ? void 0 : _result_init.headers) ? new Headers(result.init.headers) : void 0
                };
            }
            return {
                type: "error" /* error */ ,
                error: result,
                statusCode: isRouteErrorResponse(result) ? result.status : void 0
            };
        }
        if (isDataWithResponseInit(result)) {
            var _result_init3, _result_init4;
            return {
                type: "data" /* data */ ,
                data: result.data,
                statusCode: (_result_init3 = result.init) === null || _result_init3 === void 0 ? void 0 : _result_init3.status,
                headers: ((_result_init4 = result.init) === null || _result_init4 === void 0 ? void 0 : _result_init4.headers) ? new Headers(result.init.headers) : void 0
            };
        }
        return {
            type: "data" /* data */ ,
            data: result
        };
    })();
}
function normalizeRelativeRoutingRedirectResponse(response, request, routeId, matches, basename) {
    var location = response.headers.get("Location");
    invariant(location, "Redirects returned/thrown from loaders/actions must have a Location header");
    if (!isAbsoluteUrl(location)) {
        var trimmedMatches = matches.slice(0, matches.findIndex((m)=>m.route.id === routeId) + 1);
        location = normalizeTo(new URL(request.url), trimmedMatches, basename, location);
        response.headers.set("Location", location);
    }
    return response;
}
function normalizeRedirectLocation(location, currentUrl, basename, historyInstance) {
    var invalidProtocols = [
        "about:",
        "blob:",
        "chrome:",
        "chrome-untrusted:",
        "content:",
        "data:",
        "devtools:",
        "file:",
        "filesystem:",
        // eslint-disable-next-line no-script-url
        "javascript:"
    ];
    if (isAbsoluteUrl(location)) {
        var normalizedLocation = location;
        var url = normalizedLocation.startsWith("//") ? new URL(currentUrl.protocol + normalizedLocation) : new URL(normalizedLocation);
        if (invalidProtocols.includes(url.protocol)) {
            throw new Error("Invalid redirect location");
        }
        var isSameBasename = stripBasename(url.pathname, basename) != null;
        if (url.origin === currentUrl.origin && isSameBasename) {
            return url.pathname + url.search + url.hash;
        }
    }
    try {
        var url1 = historyInstance.createURL(location);
        if (invalidProtocols.includes(url1.protocol)) {
            throw new Error("Invalid redirect location");
        }
    } catch (e) {}
    return location;
}
function createClientSideRequest(history, location, signal, submission) {
    var url = history.createURL(stripHashFromPath(location)).toString();
    var init = {
        signal
    };
    if (submission && isMutationMethod(submission.formMethod)) {
        var { formMethod, formEncType } = submission;
        init.method = formMethod.toUpperCase();
        if (formEncType === "application/json") {
            init.headers = new Headers({
                "Content-Type": formEncType
            });
            init.body = JSON.stringify(submission.json);
        } else if (formEncType === "text/plain") {
            init.body = submission.text;
        } else if (formEncType === "application/x-www-form-urlencoded" && submission.formData) {
            init.body = convertFormDataToSearchParams(submission.formData);
        } else {
            init.body = submission.formData;
        }
    }
    return new Request(url, init);
}
function convertFormDataToSearchParams(formData) {
    var searchParams = new URLSearchParams();
    for (var [key, value] of formData.entries()){
        searchParams.append(key, typeof value === "string" ? value : value.name);
    }
    return searchParams;
}
function convertSearchParamsToFormData(searchParams) {
    var formData = new FormData();
    for (var [key, value] of searchParams.entries()){
        formData.append(key, value);
    }
    return formData;
}
function processRouteLoaderData(matches, results, pendingActionResult) {
    var isStaticHandler = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false, skipLoaderErrorBubbling = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
    var loaderData = {};
    var errors = null;
    var statusCode;
    var foundError = false;
    var loaderHeaders = {};
    var pendingError = pendingActionResult && isErrorResult(pendingActionResult[1]) ? pendingActionResult[1].error : void 0;
    matches.forEach((match)=>{
        if (!(match.route.id in results)) {
            return;
        }
        var id = match.route.id;
        var result = results[id];
        invariant(!isRedirectResult(result), "Cannot handle redirect results in processLoaderData");
        if (isErrorResult(result)) {
            var error = result.error;
            if (pendingError !== void 0) {
                error = pendingError;
                pendingError = void 0;
            }
            errors = errors || {};
            if (skipLoaderErrorBubbling) {
                errors[id] = error;
            } else {
                var boundaryMatch = findNearestBoundary(matches, id);
                if (errors[boundaryMatch.route.id] == null) {
                    errors[boundaryMatch.route.id] = error;
                }
            }
            if (!isStaticHandler) {
                loaderData[id] = ResetLoaderDataSymbol;
            }
            if (!foundError) {
                foundError = true;
                statusCode = isRouteErrorResponse(result.error) ? result.error.status : 500;
            }
            if (result.headers) {
                loaderHeaders[id] = result.headers;
            }
        } else {
            loaderData[id] = result.data;
            if (result.statusCode && result.statusCode !== 200 && !foundError) {
                statusCode = result.statusCode;
            }
            if (result.headers) {
                loaderHeaders[id] = result.headers;
            }
        }
    });
    if (pendingError !== void 0 && pendingActionResult) {
        errors = {
            [pendingActionResult[0]]: pendingError
        };
        if (pendingActionResult[2]) {
            loaderData[pendingActionResult[2]] = void 0;
        }
    }
    return {
        loaderData,
        errors,
        statusCode: statusCode || 200,
        loaderHeaders
    };
}
function processLoaderData(state, matches, results, pendingActionResult, revalidatingFetchers, fetcherResults) {
    var { loaderData, errors } = processRouteLoaderData(matches, results, pendingActionResult);
    revalidatingFetchers.filter((f)=>!f.matches || f.matches.some((m)=>m.shouldLoad)).forEach((rf)=>{
        var { key, match, controller } = rf;
        if (controller && controller.signal.aborted) {
            return;
        }
        var result = fetcherResults[key];
        invariant(result, "Did not find corresponding fetcher result");
        if (isErrorResult(result)) {
            var boundaryMatch = findNearestBoundary(state.matches, match === null || match === void 0 ? void 0 : match.route.id);
            if (!(errors && errors[boundaryMatch.route.id])) {
                errors = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, errors), {
                    [boundaryMatch.route.id]: result.error
                });
            }
            state.fetchers.delete(key);
        } else if (isRedirectResult(result)) {
            invariant(false, "Unhandled fetcher revalidation redirect");
        } else {
            var doneFetcher = getDoneFetcher(result.data);
            state.fetchers.set(key, doneFetcher);
        }
    });
    return {
        loaderData,
        errors
    };
}
function mergeLoaderData(loaderData, newLoaderData, matches, errors) {
    var mergedLoaderData = Object.entries(newLoaderData).filter((param)=>{
        var [, v] = param;
        return v !== ResetLoaderDataSymbol;
    }).reduce((merged, param)=>{
        var [k, v] = param;
        merged[k] = v;
        return merged;
    }, {});
    for (var match of matches){
        var id = match.route.id;
        if (!newLoaderData.hasOwnProperty(id) && loaderData.hasOwnProperty(id) && match.route.loader) {
            mergedLoaderData[id] = loaderData[id];
        }
        if (errors && errors.hasOwnProperty(id)) {
            break;
        }
    }
    return mergedLoaderData;
}
function getActionDataForCommit(pendingActionResult) {
    if (!pendingActionResult) {
        return {};
    }
    return isErrorResult(pendingActionResult[1]) ? {
        // Clear out prior actionData on errors
        actionData: {}
    } : {
        actionData: {
            [pendingActionResult[0]]: pendingActionResult[1].data
        }
    };
}
function findNearestBoundary(matches, routeId) {
    var eligibleMatches = routeId ? matches.slice(0, matches.findIndex((m)=>m.route.id === routeId) + 1) : [
        ...matches
    ];
    return eligibleMatches.reverse().find((m)=>m.route.hasErrorBoundary === true) || matches[0];
}
function getShortCircuitMatches(routes) {
    var route = routes.length === 1 ? routes[0] : routes.find((r)=>r.index || !r.path || r.path === "/") || {
        id: "__shim-error-route__"
    };
    return {
        matches: [
            {
                params: {},
                pathname: "",
                pathnameBase: "",
                route
            }
        ],
        route
    };
}
function getInternalRouterError(status) {
    var { pathname, routeId, method, type, message } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var statusText = "Unknown Server Error";
    var errorMessage = "Unknown @remix-run/router error";
    if (status === 400) {
        statusText = "Bad Request";
        if (method && pathname && routeId) {
            errorMessage = "You made a ".concat(method, ' request to "').concat(pathname, '" but did not provide a `loader` for route "').concat(routeId, '", so there is no way to handle the request.');
        } else if (type === "invalid-body") {
            errorMessage = "Unable to encode submission body";
        }
    } else if (status === 403) {
        statusText = "Forbidden";
        errorMessage = 'Route "'.concat(routeId, '" does not match URL "').concat(pathname, '"');
    } else if (status === 404) {
        statusText = "Not Found";
        errorMessage = 'No route matches URL "'.concat(pathname, '"');
    } else if (status === 405) {
        statusText = "Method Not Allowed";
        if (method && pathname && routeId) {
            errorMessage = "You made a ".concat(method.toUpperCase(), ' request to "').concat(pathname, '" but did not provide an `action` for route "').concat(routeId, '", so there is no way to handle the request.');
        } else if (method) {
            errorMessage = 'Invalid request method "'.concat(method.toUpperCase(), '"');
        }
    }
    return new ErrorResponseImpl(status || 500, statusText, new Error(errorMessage), true);
}
function findRedirect(results) {
    var entries = Object.entries(results);
    for(var i = entries.length - 1; i >= 0; i--){
        var [key, result] = entries[i];
        if (isRedirectResult(result)) {
            return {
                key,
                result
            };
        }
    }
}
function stripHashFromPath(path) {
    var parsedPath = typeof path === "string" ? parsePath(path) : path;
    return createPath((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, parsedPath), {
        hash: ""
    }));
}
function isHashChangeOnly(a, b) {
    if (a.pathname !== b.pathname || a.search !== b.search) {
        return false;
    }
    if (a.hash === "") {
        return b.hash !== "";
    } else if (a.hash === b.hash) {
        return true;
    } else if (b.hash !== "") {
        return true;
    }
    return false;
}
function dataWithResponseInitToResponse(data2) {
    var _data2_init;
    return Response.json(data2.data, (_data2_init = data2.init) !== null && _data2_init !== void 0 ? _data2_init : void 0);
}
function dataWithResponseInitToErrorResponse(data2) {
    var _ref, _ref1;
    var _data2_init, _data2_init1;
    return new ErrorResponseImpl((_ref = (_data2_init = data2.init) === null || _data2_init === void 0 ? void 0 : _data2_init.status) !== null && _ref !== void 0 ? _ref : 500, (_ref1 = (_data2_init1 = data2.init) === null || _data2_init1 === void 0 ? void 0 : _data2_init1.statusText) !== null && _ref1 !== void 0 ? _ref1 : "Internal Server Error", data2.data);
}
function isDataStrategyResults(result) {
    return result != null && typeof result === "object" && Object.entries(result).every((param)=>{
        var [key, value] = param;
        return typeof key === "string" && isDataStrategyResult(value);
    });
}
function isDataStrategyResult(result) {
    return result != null && typeof result === "object" && "type" in result && "result" in result && (result.type === "data" /* data */  || result.type === "error" /* error */ );
}
function isRedirectDataStrategyResult(result) {
    return isResponse(result.result) && redirectStatusCodes.has(result.result.status);
}
function isErrorResult(result) {
    return result.type === "error" /* error */ ;
}
function isRedirectResult(result) {
    return (result && result.type) === "redirect" /* redirect */ ;
}
function isDataWithResponseInit(value) {
    return typeof value === "object" && value != null && "type" in value && "data" in value && "init" in value && value.type === "DataWithResponseInit";
}
function isResponse(value) {
    return value != null && typeof value.status === "number" && typeof value.statusText === "string" && typeof value.headers === "object" && typeof value.body !== "undefined";
}
function isRedirectStatusCode(statusCode) {
    return redirectStatusCodes.has(statusCode);
}
function isRedirectResponse(result) {
    return isResponse(result) && isRedirectStatusCode(result.status) && result.headers.has("Location");
}
function isValidMethod(method) {
    return validRequestMethods.has(method.toUpperCase());
}
function isMutationMethod(method) {
    return validMutationMethods.has(method.toUpperCase());
}
function hasNakedIndexQuery(search) {
    return new URLSearchParams(search).getAll("index").some((v)=>v === "");
}
function getTargetMatch(matches, location) {
    var search = typeof location === "string" ? parsePath(location).search : location.search;
    if (matches[matches.length - 1].route.index && hasNakedIndexQuery(search || "")) {
        return matches[matches.length - 1];
    }
    var pathMatches = getPathContributingMatches(matches);
    return pathMatches[pathMatches.length - 1];
}
function getSubmissionFromNavigation(navigation) {
    var { formMethod, formAction, formEncType, text, formData, json } = navigation;
    if (!formMethod || !formAction || !formEncType) {
        return;
    }
    if (text != null) {
        return {
            formMethod,
            formAction,
            formEncType,
            formData: void 0,
            json: void 0,
            text
        };
    } else if (formData != null) {
        return {
            formMethod,
            formAction,
            formEncType,
            formData,
            json: void 0,
            text: void 0
        };
    } else if (json !== void 0) {
        return {
            formMethod,
            formAction,
            formEncType,
            formData: void 0,
            json,
            text: void 0
        };
    }
}
function getLoadingNavigation(location, submission) {
    if (submission) {
        var navigation = {
            state: "loading",
            location,
            formMethod: submission.formMethod,
            formAction: submission.formAction,
            formEncType: submission.formEncType,
            formData: submission.formData,
            json: submission.json,
            text: submission.text
        };
        return navigation;
    } else {
        var navigation1 = {
            state: "loading",
            location,
            formMethod: void 0,
            formAction: void 0,
            formEncType: void 0,
            formData: void 0,
            json: void 0,
            text: void 0
        };
        return navigation1;
    }
}
function getSubmittingNavigation(location, submission) {
    var navigation = {
        state: "submitting",
        location,
        formMethod: submission.formMethod,
        formAction: submission.formAction,
        formEncType: submission.formEncType,
        formData: submission.formData,
        json: submission.json,
        text: submission.text
    };
    return navigation;
}
function getLoadingFetcher(submission, data2) {
    if (submission) {
        var fetcher = {
            state: "loading",
            formMethod: submission.formMethod,
            formAction: submission.formAction,
            formEncType: submission.formEncType,
            formData: submission.formData,
            json: submission.json,
            text: submission.text,
            data: data2
        };
        return fetcher;
    } else {
        var fetcher1 = {
            state: "loading",
            formMethod: void 0,
            formAction: void 0,
            formEncType: void 0,
            formData: void 0,
            json: void 0,
            text: void 0,
            data: data2
        };
        return fetcher1;
    }
}
function getSubmittingFetcher(submission, existingFetcher) {
    var fetcher = {
        state: "submitting",
        formMethod: submission.formMethod,
        formAction: submission.formAction,
        formEncType: submission.formEncType,
        formData: submission.formData,
        json: submission.json,
        text: submission.text,
        data: existingFetcher ? existingFetcher.data : void 0
    };
    return fetcher;
}
function getDoneFetcher(data2) {
    var fetcher = {
        state: "idle",
        formMethod: void 0,
        formAction: void 0,
        formEncType: void 0,
        formData: void 0,
        json: void 0,
        text: void 0,
        data: data2
    };
    return fetcher;
}
function restoreAppliedTransitions(_window, transitions) {
    try {
        var sessionPositions = _window.sessionStorage.getItem(TRANSITIONS_STORAGE_KEY);
        if (sessionPositions) {
            var json = JSON.parse(sessionPositions);
            for (var [k, v] of Object.entries(json || {})){
                if (v && Array.isArray(v)) {
                    transitions.set(k, new Set(v || []));
                }
            }
        }
    } catch (e) {}
}
function persistAppliedTransitions(_window, transitions) {
    if (transitions.size > 0) {
        var json = {};
        for (var [k, v] of transitions){
            json[k] = [
                ...v
            ];
        }
        try {
            _window.sessionStorage.setItem(TRANSITIONS_STORAGE_KEY, JSON.stringify(json));
        } catch (error) {
            warning(false, "Failed to save applied view transitions in sessionStorage (".concat(error, ")."));
        }
    }
}
function createDeferred() {
    var resolve;
    var reject;
    var promise = new Promise((res, rej)=>{
        resolve = (val)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                res(val);
                try {
                    yield promise;
                } catch (e) {}
            })();
        reject = (error)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                rej(error);
                try {
                    yield promise;
                } catch (e) {}
            })();
    });
    return {
        promise,
        //@ts-ignore
        resolve,
        //@ts-ignore
        reject
    };
}
// lib/context.ts

var DataRouterContext = /*#__PURE__*/ react__rspack_import_0.createContext(null);
DataRouterContext.displayName = "DataRouter";
var DataRouterStateContext = /*#__PURE__*/ react__rspack_import_0.createContext(null);
DataRouterStateContext.displayName = "DataRouterState";
var RSCRouterContext = /*#__PURE__*/ react__rspack_import_0.createContext(false);
function useIsRSCRouterContext() {
    _s();
    return react__rspack_import_0.useContext(RSCRouterContext);
}
_s(useIsRSCRouterContext, "gDsCjeeItUuvgOWf1v4qoK9RF6k=");
var ViewTransitionContext = /*#__PURE__*/ react__rspack_import_0.createContext({
    isTransitioning: false
});
ViewTransitionContext.displayName = "ViewTransition";
var FetchersContext = /*#__PURE__*/ react__rspack_import_0.createContext(/* @__PURE__ */ new Map());
FetchersContext.displayName = "Fetchers";
var AwaitContext = /*#__PURE__*/ react__rspack_import_0.createContext(null);
AwaitContext.displayName = "Await";
var AwaitContextProvider = (props)=>/*#__PURE__*/ react__rspack_import_0.createElement(AwaitContext.Provider, props);
_c = AwaitContextProvider;
var NavigationContext = /*#__PURE__*/ react__rspack_import_0.createContext(null);
NavigationContext.displayName = "Navigation";
var LocationContext = /*#__PURE__*/ react__rspack_import_0.createContext(null);
LocationContext.displayName = "Location";
var RouteContext = /*#__PURE__*/ react__rspack_import_0.createContext({
    outlet: null,
    matches: [],
    isDataRoute: false
});
RouteContext.displayName = "Route";
var RouteErrorContext = /*#__PURE__*/ react__rspack_import_0.createContext(null);
RouteErrorContext.displayName = "RouteError";
var ENABLE_DEV_WARNINGS = true;
// lib/hooks.tsx

// lib/errors.ts
var ERROR_DIGEST_BASE = "REACT_ROUTER_ERROR";
var ERROR_DIGEST_REDIRECT = "REDIRECT";
var ERROR_DIGEST_ROUTE_ERROR_RESPONSE = "ROUTE_ERROR_RESPONSE";
function decodeRedirectErrorDigest(digest) {
    if (digest.startsWith("".concat(ERROR_DIGEST_BASE, ":").concat(ERROR_DIGEST_REDIRECT, ":{"))) {
        try {
            var parsed = JSON.parse(digest.slice(28));
            if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string" && typeof parsed.location === "string" && typeof parsed.reloadDocument === "boolean" && typeof parsed.replace === "boolean") {
                return parsed;
            }
        } catch (unused) {}
    }
}
function decodeRouteErrorResponseDigest(digest) {
    if (digest.startsWith("".concat(ERROR_DIGEST_BASE, ":").concat(ERROR_DIGEST_ROUTE_ERROR_RESPONSE, ":{"))) {
        try {
            var parsed = JSON.parse(digest.slice(40));
            if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string") {
                return new ErrorResponseImpl(parsed.status, parsed.statusText, parsed.data);
            }
        } catch (unused) {}
    }
}
// lib/hooks.tsx
function useHref(to) {
    var { relative } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    _s1();
    invariant(useInRouterContext(), // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useHref() may be used only in the context of a <Router> component.");
    var { basename, navigator } = react__rspack_import_0.useContext(NavigationContext);
    var { hash, pathname, search } = useResolvedPath(to, {
        relative
    });
    var joinedPathname = pathname;
    if (basename !== "/") {
        joinedPathname = pathname === "/" ? basename : joinPaths([
            basename,
            pathname
        ]);
    }
    return navigator.createHref({
        pathname: joinedPathname,
        search,
        hash
    });
}
_s1(useHref, "czNRFIljZWYc3H2RCLyg2qMCEEE=", false, function() {
    return [
        useInRouterContext,
        useResolvedPath
    ];
});
function useInRouterContext() {
    _s2();
    return react__rspack_import_0.useContext(LocationContext) != null;
}
_s2(useInRouterContext, "gDsCjeeItUuvgOWf1v4qoK9RF6k=");
function useLocation() {
    _s3();
    invariant(useInRouterContext(), // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useLocation() may be used only in the context of a <Router> component.");
    return react__rspack_import_0.useContext(LocationContext).location;
}
_s3(useLocation, "pnBsrBzOSKrTAsifUkfhXR0cM+Y=", false, function() {
    return [
        useInRouterContext
    ];
});
function useNavigationType() {
    _s4();
    return react__rspack_import_0.useContext(LocationContext).navigationType;
}
_s4(useNavigationType, "gDsCjeeItUuvgOWf1v4qoK9RF6k=");
function useMatch(pattern) {
    _s5();
    invariant(useInRouterContext(), // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useMatch() may be used only in the context of a <Router> component.");
    var { pathname } = useLocation();
    return react__rspack_import_0.useMemo(()=>matchPath(pattern, decodePath(pathname)), [
        pathname,
        pattern
    ]);
}
_s5(useMatch, "F+Qr/2L5F/MIguoNPbc+31GVsq8=", false, function() {
    return [
        useInRouterContext,
        useLocation
    ];
});
var navigateEffectWarning = "You should call navigate() in a React.useEffect(), not when your component is first rendered.";
function useIsomorphicLayoutEffect(cb) {
    _s6();
    var isStatic = react__rspack_import_0.useContext(NavigationContext).static;
    if (!isStatic) {
        react__rspack_import_0.useLayoutEffect(cb);
    }
}
_s6(useIsomorphicLayoutEffect, "gDsCjeeItUuvgOWf1v4qoK9RF6k=");
function useNavigate() {
    _s7();
    var { isDataRoute } = react__rspack_import_0.useContext(RouteContext);
    return isDataRoute ? useNavigateStable() : useNavigateUnstable();
}
_s7(useNavigate, "87qJ8YKE/C7dhb2ZUM5zG8Bf8Gc=", false, function() {
    return [
        useNavigateStable,
        useNavigateUnstable
    ];
});
function useNavigateUnstable() {
    _s8();
    invariant(useInRouterContext(), // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useNavigate() may be used only in the context of a <Router> component.");
    var dataRouterContext = react__rspack_import_0.useContext(DataRouterContext);
    var { basename, navigator } = react__rspack_import_0.useContext(NavigationContext);
    var { matches } = react__rspack_import_0.useContext(RouteContext);
    var { pathname: locationPathname } = useLocation();
    var routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
    var activeRef = react__rspack_import_0.useRef(false);
    useIsomorphicLayoutEffect(()=>{
        activeRef.current = true;
    });
    var navigate = react__rspack_import_0.useCallback(function(to) {
        var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        warning(activeRef.current, navigateEffectWarning);
        if (!activeRef.current) return;
        if (typeof to === "number") {
            navigator.go(to);
            return;
        }
        var path = resolveTo(to, JSON.parse(routePathnamesJson), locationPathname, options.relative === "path");
        if (dataRouterContext == null && basename !== "/") {
            path.pathname = path.pathname === "/" ? basename : joinPaths([
                basename,
                path.pathname
            ]);
        }
        (!!options.replace ? navigator.replace : navigator.push)(path, options.state, options);
    }, [
        basename,
        navigator,
        routePathnamesJson,
        locationPathname,
        dataRouterContext
    ]);
    return navigate;
}
_s8(useNavigateUnstable, "Rr9Tk9bjLVJb1NwZQRzEH7tsZJ0=", false, function() {
    return [
        useInRouterContext,
        useLocation,
        useIsomorphicLayoutEffect
    ];
});
var OutletContext = /*#__PURE__*/ react__rspack_import_0.createContext(null);
function useOutletContext() {
    _s9();
    return react__rspack_import_0.useContext(OutletContext);
}
_s9(useOutletContext, "gDsCjeeItUuvgOWf1v4qoK9RF6k=");
function useOutlet(context) {
    _s10();
    var outlet = react__rspack_import_0.useContext(RouteContext).outlet;
    return react__rspack_import_0.useMemo(()=>outlet && /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(OutletContext.Provider, {
            value: context
        }, outlet), [
        outlet,
        context
    ]);
}
_s10(useOutlet, "shxTVlB2oWfKttdhhGhChpfITWQ=");
function useParams() {
    _s11();
    var { matches } = react__rspack_import_0.useContext(RouteContext);
    var routeMatch = matches[matches.length - 1];
    return routeMatch ? routeMatch.params : {};
}
_s11(useParams, "aACHCg1nBL7qxrc7W18sEZHcShg=");
function useResolvedPath(to) {
    var { relative } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    _s12();
    var { matches } = react__rspack_import_0.useContext(RouteContext);
    var { pathname: locationPathname } = useLocation();
    var routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
    return react__rspack_import_0.useMemo(()=>resolveTo(to, JSON.parse(routePathnamesJson), locationPathname, relative === "path"), [
        to,
        routePathnamesJson,
        locationPathname,
        relative
    ]);
}
_s12(useResolvedPath, "Jvt7K4oIyKQnY36YSxuKVxJHhgo=", false, function() {
    return [
        useLocation
    ];
});
function useRoutes(routes, locationArg) {
    _s13();
    return useRoutesImpl(routes, locationArg);
}
_s13(useRoutes, "bxXQ1PjoUitFHQG/730mCYNdT00=", false, function() {
    return [
        useRoutesImpl
    ];
});
function useRoutesImpl(routes, locationArg, dataRouterState, onError, future) {
    _s14();
    invariant(useInRouterContext(), // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useRoutes() may be used only in the context of a <Router> component.");
    var { navigator } = react__rspack_import_0.useContext(NavigationContext);
    var { matches: parentMatches } = react__rspack_import_0.useContext(RouteContext);
    var routeMatch = parentMatches[parentMatches.length - 1];
    var parentParams = routeMatch ? routeMatch.params : {};
    var parentPathname = routeMatch ? routeMatch.pathname : "/";
    var parentPathnameBase = routeMatch ? routeMatch.pathnameBase : "/";
    var parentRoute = routeMatch && routeMatch.route;
    if (ENABLE_DEV_WARNINGS) {
        var parentPath = parentRoute && parentRoute.path || "";
        warningOnce(parentPathname, !parentRoute || parentPath.endsWith("*") || parentPath.endsWith("*?"), 'You rendered descendant <Routes> (or called `useRoutes()`) at "'.concat(parentPathname, '" (under <Route path="').concat(parentPath, '">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won\'t match anymore and therefore the child routes will never render.\n\nPlease change the parent <Route path="').concat(parentPath, '"> to <Route path="').concat(parentPath === "/" ? "*" : "".concat(parentPath, "/*"), '">.'));
    }
    var locationFromContext = useLocation();
    var location;
    if (locationArg) {
        var _parsedLocationArg_pathname;
        var parsedLocationArg = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
        invariant(parentPathnameBase === "/" || ((_parsedLocationArg_pathname = parsedLocationArg.pathname) === null || _parsedLocationArg_pathname === void 0 ? void 0 : _parsedLocationArg_pathname.startsWith(parentPathnameBase)), 'When overriding the location using `<Routes location>` or `useRoutes(routes, location)`, the location pathname must begin with the portion of the URL pathname that was matched by all parent routes. The current pathname base is "'.concat(parentPathnameBase, '" but pathname "').concat(parsedLocationArg.pathname, '" was given in the `location` prop.'));
        location = parsedLocationArg;
    } else {
        location = locationFromContext;
    }
    var pathname = location.pathname || "/";
    var remainingPathname = pathname;
    if (parentPathnameBase !== "/") {
        var parentSegments = parentPathnameBase.replace(/^\//, "").split("/");
        var segments = pathname.replace(/^\//, "").split("/");
        remainingPathname = "/" + segments.slice(parentSegments.length).join("/");
    }
    var matches = matchRoutes(routes, {
        pathname: remainingPathname
    });
    if (ENABLE_DEV_WARNINGS) {
        warning(parentRoute || matches != null, 'No routes matched location "'.concat(location.pathname).concat(location.search).concat(location.hash, '" '));
        warning(matches == null || matches[matches.length - 1].route.element !== void 0 || matches[matches.length - 1].route.Component !== void 0 || matches[matches.length - 1].route.lazy !== void 0, 'Matched leaf route at location "'.concat(location.pathname).concat(location.search).concat(location.hash, '" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.'));
    }
    var renderedMatches = _renderMatches(matches && matches.map((match)=>Object.assign({}, match, {
            params: Object.assign({}, parentParams, match.params),
            pathname: joinPaths([
                parentPathnameBase,
                // Re-encode pathnames that were decoded inside matchRoutes.
                // Pre-encode `?` and `#` ahead of `encodeLocation` because it uses
                // `new URL()` internally and we need to prevent it from treating
                // them as separators
                navigator.encodeLocation ? navigator.encodeLocation(match.pathname.replace(/\?/g, "%3F").replace(/#/g, "%23")).pathname : match.pathname
            ]),
            pathnameBase: match.pathnameBase === "/" ? parentPathnameBase : joinPaths([
                parentPathnameBase,
                // Re-encode pathnames that were decoded inside matchRoutes
                // Pre-encode `?` and `#` ahead of `encodeLocation` because it uses
                // `new URL()` internally and we need to prevent it from treating
                // them as separators
                navigator.encodeLocation ? navigator.encodeLocation(match.pathnameBase.replace(/\?/g, "%3F").replace(/#/g, "%23")).pathname : match.pathnameBase
            ])
        })), parentMatches, dataRouterState, onError, future);
    if (locationArg && renderedMatches) {
        return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(LocationContext.Provider, {
            value: {
                location: (0,_swc_helpers_object_spread__rspack_import_2._)({
                    pathname: "/",
                    search: "",
                    hash: "",
                    state: null,
                    key: "default"
                }, location),
                navigationType: "POP" /* Pop */ 
            }
        }, renderedMatches);
    }
    return renderedMatches;
}
_s14(useRoutesImpl, "e+u1GYcog+2GcbdQ/bAfxHpgJcA=", false, function() {
    return [
        useInRouterContext,
        useLocation
    ];
});
function DefaultErrorComponent() {
    _s15();
    var error = useRouteError();
    var message = isRouteErrorResponse(error) ? "".concat(error.status, " ").concat(error.statusText) : error instanceof Error ? error.message : JSON.stringify(error);
    var stack = error instanceof Error ? error.stack : null;
    var lightgrey = "rgba(200,200,200, 0.5)";
    var preStyles = {
        padding: "0.5rem",
        backgroundColor: lightgrey
    };
    var codeStyles = {
        padding: "2px 4px",
        backgroundColor: lightgrey
    };
    var devInfo = null;
    if (ENABLE_DEV_WARNINGS) {
        console.error("Error handled by React Router default ErrorBoundary:", error);
        devInfo = /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("p", null, "\uD83D\uDCBF Hey developer \uD83D\uDC4B"), /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("p", null, "You can provide a way better UX than this when your app throws errors by providing your own ", /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("code", {
            style: codeStyles
        }, "ErrorBoundary"), " or", " ", /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("code", {
            style: codeStyles
        }, "errorElement"), " prop on your route."));
    }
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("h2", null, "Unexpected Application Error!"), /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("h3", {
        style: {
            fontStyle: "italic"
        }
    }, message), stack ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("pre", {
        style: preStyles
    }, stack) : null, devInfo);
}
_s15(DefaultErrorComponent, "oAgjgbJzsRXlB89+MoVumxMQqKM=", false, function() {
    return [
        useRouteError
    ];
});
_c1 = DefaultErrorComponent;
var defaultErrorElement = /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(DefaultErrorComponent, null);
var RenderErrorBoundary = class RenderErrorBoundary extends react__rspack_import_0.Component {
    static getDerivedStateFromError(error) {
        return {
            error
        };
    }
    static getDerivedStateFromProps(props, state) {
        if (state.location !== props.location || state.revalidation !== "idle" && props.revalidation === "idle") {
            return {
                error: props.error,
                location: props.location,
                revalidation: props.revalidation
            };
        }
        return {
            error: props.error !== void 0 ? props.error : state.error,
            location: state.location,
            revalidation: props.revalidation || state.revalidation
        };
    }
    componentDidCatch(error, errorInfo) {
        if (this.props.onError) {
            this.props.onError(error, errorInfo);
        } else {
            console.error("React Router caught the following error during render", error);
        }
    }
    render() {
        var error = this.state.error;
        if (this.context && typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
            var decoded = decodeRouteErrorResponseDigest(error.digest);
            if (decoded) error = decoded;
        }
        var result = error !== void 0 ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(RouteContext.Provider, {
            value: this.props.routeContext
        }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(RouteErrorContext.Provider, {
            value: error,
            children: this.props.component
        })) : this.props.children;
        if (this.context) {
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(RSCErrorHandler, {
                error
            }, result);
        }
        return result;
    }
    constructor(props){
        super(props);
        this.state = {
            location: props.location,
            revalidation: props.revalidation,
            error: props.error
        };
    }
};
RenderErrorBoundary.contextType = RSCRouterContext;
var errorRedirectHandledMap = /* @__PURE__ */ new WeakMap();
function RSCErrorHandler(param) {
    var { children, error } = param;
    _s16();
    var { basename } = react__rspack_import_0.useContext(NavigationContext);
    if (typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
        var redirect2 = decodeRedirectErrorDigest(error.digest);
        if (redirect2) {
            var existingRedirect = errorRedirectHandledMap.get(error);
            if (existingRedirect) throw existingRedirect;
            var parsed = parseToInfo(redirect2.location, basename);
            if (isBrowser && !errorRedirectHandledMap.get(error)) {
                if (parsed.isExternal || redirect2.reloadDocument) {
                    window.location.href = parsed.absoluteURL || parsed.to;
                } else {
                    var redirectPromise = Promise.resolve().then(()=>window.__reactRouterDataRouter.navigate(parsed.to, {
                            replace: redirect2.replace
                        }));
                    errorRedirectHandledMap.set(error, redirectPromise);
                    throw redirectPromise;
                }
            }
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("meta", {
                httpEquiv: "refresh",
                content: "0;url=".concat(parsed.absoluteURL || parsed.to)
            });
        }
    }
    return children;
}
_s16(RSCErrorHandler, "JsNd8k07RucuRK9PVdsWO9ph+Gw=");
_c2 = RSCErrorHandler;
function RenderedRoute(param) {
    var { routeContext, match, children } = param;
    _s17();
    var dataRouterContext = react__rspack_import_0.useContext(DataRouterContext);
    if (dataRouterContext && dataRouterContext.static && dataRouterContext.staticContext && (match.route.errorElement || match.route.ErrorBoundary)) {
        dataRouterContext.staticContext._deepestRenderedBoundaryId = match.route.id;
    }
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(RouteContext.Provider, {
        value: routeContext
    }, children);
}
_s17(RenderedRoute, "CDKlwIqxcpySUP3IDCQMJZjTo1Q=");
_c3 = RenderedRoute;
function _renderMatches(matches) {
    var parentMatches = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [], dataRouterState = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null, onErrorHandler = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : null, future = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : null;
    if (matches == null) {
        if (!dataRouterState) {
            return null;
        }
        if (dataRouterState.errors) {
            matches = dataRouterState.matches;
        } else if (parentMatches.length === 0 && !dataRouterState.initialized && dataRouterState.matches.length > 0) {
            matches = dataRouterState.matches;
        } else {
            return null;
        }
    }
    var renderedMatches = matches;
    var errors = dataRouterState === null || dataRouterState === void 0 ? void 0 : dataRouterState.errors;
    if (errors != null) {
        var errorIndex = renderedMatches.findIndex((m)=>m.route.id && (errors === null || errors === void 0 ? void 0 : errors[m.route.id]) !== void 0);
        invariant(errorIndex >= 0, "Could not find a matching route for errors on route IDs: ".concat(Object.keys(errors).join(",")));
        renderedMatches = renderedMatches.slice(0, Math.min(renderedMatches.length, errorIndex + 1));
    }
    var renderFallback = false;
    var fallbackIndex = -1;
    if (dataRouterState) {
        for(var i = 0; i < renderedMatches.length; i++){
            var match = renderedMatches[i];
            if (match.route.HydrateFallback || match.route.hydrateFallbackElement) {
                fallbackIndex = i;
            }
            if (match.route.id) {
                var { loaderData, errors: errors2 } = dataRouterState;
                var needsToRunLoader = match.route.loader && !loaderData.hasOwnProperty(match.route.id) && (!errors2 || errors2[match.route.id] === void 0);
                if (match.route.lazy || needsToRunLoader) {
                    renderFallback = true;
                    if (fallbackIndex >= 0) {
                        renderedMatches = renderedMatches.slice(0, fallbackIndex + 1);
                    } else {
                        renderedMatches = [
                            renderedMatches[0]
                        ];
                    }
                    break;
                }
            }
        }
    }
    var onError = dataRouterState && onErrorHandler ? (error, errorInfo)=>{
        var _ref;
        var _dataRouterState_matches_, _dataRouterState_matches;
        onErrorHandler(error, {
            location: dataRouterState.location,
            params: (_ref = (_dataRouterState_matches = dataRouterState.matches) === null || _dataRouterState_matches === void 0 ? void 0 : (_dataRouterState_matches_ = _dataRouterState_matches[0]) === null || _dataRouterState_matches_ === void 0 ? void 0 : _dataRouterState_matches_.params) !== null && _ref !== void 0 ? _ref : {},
            unstable_pattern: getRoutePattern(dataRouterState.matches),
            errorInfo
        });
    } : void 0;
    return renderedMatches.reduceRight((outlet, match, index)=>{
        var error;
        var shouldRenderHydrateFallback = false;
        var errorElement = null;
        var hydrateFallbackElement = null;
        if (dataRouterState) {
            error = errors && match.route.id ? errors[match.route.id] : void 0;
            errorElement = match.route.errorElement || defaultErrorElement;
            if (renderFallback) {
                if (fallbackIndex < 0 && index === 0) {
                    warningOnce("route-fallback", false, "No `HydrateFallback` element provided to render during initial hydration");
                    shouldRenderHydrateFallback = true;
                    hydrateFallbackElement = null;
                } else if (fallbackIndex === index) {
                    shouldRenderHydrateFallback = true;
                    hydrateFallbackElement = match.route.hydrateFallbackElement || null;
                }
            }
        }
        var matches2 = parentMatches.concat(renderedMatches.slice(0, index + 1));
        var getChildren = ()=>{
            var children;
            if (error) {
                children = errorElement;
            } else if (shouldRenderHydrateFallback) {
                children = hydrateFallbackElement;
            } else if (match.route.Component) {
                children = /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(match.route.Component, null);
            } else if (match.route.element) {
                children = match.route.element;
            } else {
                children = outlet;
            }
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(RenderedRoute, {
                match,
                routeContext: {
                    outlet,
                    matches: matches2,
                    isDataRoute: dataRouterState != null
                },
                children
            });
        };
        return dataRouterState && (match.route.ErrorBoundary || match.route.errorElement || index === 0) ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(RenderErrorBoundary, {
            location: dataRouterState.location,
            revalidation: dataRouterState.revalidation,
            component: errorElement,
            error,
            children: getChildren(),
            routeContext: {
                outlet: null,
                matches: matches2,
                isDataRoute: true
            },
            onError
        }) : getChildren();
    }, null);
}
function getDataRouterConsoleError(hookName) {
    return "".concat(hookName, " must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.");
}
function useDataRouterContext(hookName) {
    _s18();
    var ctx = react__rspack_import_0.useContext(DataRouterContext);
    invariant(ctx, getDataRouterConsoleError(hookName));
    return ctx;
}
_s18(useDataRouterContext, "/dMy7t63NXD4eYACoT93CePwGrg=");
function useDataRouterState(hookName) {
    _s19();
    var state = react__rspack_import_0.useContext(DataRouterStateContext);
    invariant(state, getDataRouterConsoleError(hookName));
    return state;
}
_s19(useDataRouterState, "kdLImxeHm3898p7tqgy8Fclg5s8=");
function useRouteContext(hookName) {
    _s20();
    var route = react__rspack_import_0.useContext(RouteContext);
    invariant(route, getDataRouterConsoleError(hookName));
    return route;
}
_s20(useRouteContext, "+p67gHdeicVxIDd5SykDKMiYfaw=");
function useCurrentRouteId(hookName) {
    _s21();
    var route = useRouteContext(hookName);
    var thisRoute = route.matches[route.matches.length - 1];
    invariant(thisRoute.route.id, "".concat(hookName, ' can only be used on routes that contain a unique "id"'));
    return thisRoute.route.id;
}
_s21(useCurrentRouteId, "wASpERyQLx+zgcw/PyUNGa0MV3E=", false, function() {
    return [
        useRouteContext
    ];
});
function useRouteId() {
    _s22();
    return useCurrentRouteId("useRouteId" /* UseRouteId */ );
}
_s22(useRouteId, "pD4QEmuO4Ll++v9bSotMnGBlCbU=", false, function() {
    return [
        useCurrentRouteId
    ];
});
function useNavigation() {
    _s23();
    var state = useDataRouterState("useNavigation" /* UseNavigation */ );
    return state.navigation;
}
_s23(useNavigation, "ksx0pUFe0Qs5FOQ3bx/kwJ/n1pg=", false, function() {
    return [
        useDataRouterState
    ];
});
function useRevalidator() {
    _s24();
    var dataRouterContext = useDataRouterContext("useRevalidator" /* UseRevalidator */ );
    var state = useDataRouterState("useRevalidator" /* UseRevalidator */ );
    var revalidate = react__rspack_import_0.useCallback(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            yield dataRouterContext.router.revalidate();
        })(), [
        dataRouterContext.router
    ]);
    return react__rspack_import_0.useMemo(()=>({
            revalidate,
            state: state.revalidation
        }), [
        revalidate,
        state.revalidation
    ]);
}
_s24(useRevalidator, "WDa5ljgSx/rXVRusujWzzPwlOsg=", false, function() {
    return [
        useDataRouterContext,
        useDataRouterState
    ];
});
function useMatches() {
    _s25();
    var { matches, loaderData } = useDataRouterState("useMatches" /* UseMatches */ );
    return react__rspack_import_0.useMemo(()=>matches.map((m)=>convertRouteMatchToUiMatch(m, loaderData)), [
        matches,
        loaderData
    ]);
}
_s25(useMatches, "1eYHo/gXFDg50Pygzj5kIEMYooA=", false, function() {
    return [
        useDataRouterState
    ];
});
function useLoaderData() {
    _s26();
    var state = useDataRouterState("useLoaderData" /* UseLoaderData */ );
    var routeId = useCurrentRouteId("useLoaderData" /* UseLoaderData */ );
    return state.loaderData[routeId];
}
_s26(useLoaderData, "9OzlB4eTxA/d/duIIR6ygqOjcHs=", false, function() {
    return [
        useDataRouterState,
        useCurrentRouteId
    ];
});
function useRouteLoaderData(routeId) {
    _s27();
    var state = useDataRouterState("useRouteLoaderData" /* UseRouteLoaderData */ );
    return state.loaderData[routeId];
}
_s27(useRouteLoaderData, "ksx0pUFe0Qs5FOQ3bx/kwJ/n1pg=", false, function() {
    return [
        useDataRouterState
    ];
});
function useActionData() {
    _s28();
    var state = useDataRouterState("useActionData" /* UseActionData */ );
    var routeId = useCurrentRouteId("useLoaderData" /* UseLoaderData */ );
    return state.actionData ? state.actionData[routeId] : void 0;
}
_s28(useActionData, "9OzlB4eTxA/d/duIIR6ygqOjcHs=", false, function() {
    return [
        useDataRouterState,
        useCurrentRouteId
    ];
});
function useRouteError() {
    var _state_errors;
    _s29();
    var error = react__rspack_import_0.useContext(RouteErrorContext);
    var state = useDataRouterState("useRouteError" /* UseRouteError */ );
    var routeId = useCurrentRouteId("useRouteError" /* UseRouteError */ );
    if (error !== void 0) {
        return error;
    }
    return (_state_errors = state.errors) === null || _state_errors === void 0 ? void 0 : _state_errors[routeId];
}
_s29(useRouteError, "MKznRPOFPCBNjzCcIHhUV3MxLu8=", false, function() {
    return [
        useDataRouterState,
        useCurrentRouteId
    ];
});
function useAsyncValue() {
    _s30();
    var value = react__rspack_import_0.useContext(AwaitContext);
    return value === null || value === void 0 ? void 0 : value._data;
}
_s30(useAsyncValue, "ksutO2/Ix3UeCrGnhyM+QEP505Y=");
function useAsyncError() {
    _s31();
    var value = react__rspack_import_0.useContext(AwaitContext);
    return value === null || value === void 0 ? void 0 : value._error;
}
_s31(useAsyncError, "ksutO2/Ix3UeCrGnhyM+QEP505Y=");
var blockerId = 0;
function useBlocker(shouldBlock) {
    _s32();
    var { router, basename } = useDataRouterContext("useBlocker" /* UseBlocker */ );
    var state = useDataRouterState("useBlocker" /* UseBlocker */ );
    var [blockerKey, setBlockerKey] = react__rspack_import_0.useState("");
    var blockerFunction = react__rspack_import_0.useCallback((arg)=>{
        if (typeof shouldBlock !== "function") {
            return !!shouldBlock;
        }
        if (basename === "/") {
            return shouldBlock(arg);
        }
        var { currentLocation, nextLocation, historyAction } = arg;
        return shouldBlock({
            currentLocation: (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, currentLocation), {
                pathname: stripBasename(currentLocation.pathname, basename) || currentLocation.pathname
            }),
            nextLocation: (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, nextLocation), {
                pathname: stripBasename(nextLocation.pathname, basename) || nextLocation.pathname
            }),
            historyAction
        });
    }, [
        basename,
        shouldBlock
    ]);
    react__rspack_import_0.useEffect(()=>{
        var key = String(++blockerId);
        setBlockerKey(key);
        return ()=>router.deleteBlocker(key);
    }, [
        router
    ]);
    react__rspack_import_0.useEffect(()=>{
        if (blockerKey !== "") {
            router.getBlocker(blockerKey, blockerFunction);
        }
    }, [
        router,
        blockerKey,
        blockerFunction
    ]);
    return blockerKey && state.blockers.has(blockerKey) ? state.blockers.get(blockerKey) : IDLE_BLOCKER;
}
_s32(useBlocker, "4YJ/qERc/GgP6OdGFBGTu+iEbZM=", false, function() {
    return [
        useDataRouterContext,
        useDataRouterState
    ];
});
function useNavigateStable() {
    _s33();
    var { router } = useDataRouterContext("useNavigate" /* UseNavigateStable */ );
    var id = useCurrentRouteId("useNavigate" /* UseNavigateStable */ );
    var activeRef = react__rspack_import_0.useRef(false);
    useIsomorphicLayoutEffect(()=>{
        activeRef.current = true;
    });
    var navigate = react__rspack_import_0.useCallback(function(to) {
        var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            warning(activeRef.current, navigateEffectWarning);
            if (!activeRef.current) return;
            if (typeof to === "number") {
                yield router.navigate(to);
            } else {
                yield router.navigate(to, (0,_swc_helpers_object_spread__rspack_import_2._)({
                    fromRouteId: id
                }, options));
            }
        })();
    }, [
        router,
        id
    ]);
    return navigate;
}
_s33(useNavigateStable, "RXplNN49pFD+nlWCQplXVXDhH0o=", false, function() {
    return [
        useDataRouterContext,
        useCurrentRouteId,
        useIsomorphicLayoutEffect
    ];
});
var alreadyWarned = {};
function warningOnce(key, cond, message) {
    if (!cond && !alreadyWarned[key]) {
        alreadyWarned[key] = true;
        warning(false, message);
    }
}
function useRoute() {
    for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
        args[_key] = arguments[_key];
    }
    var _args_;
    var _state_actionData;
    _s34();
    var currentRouteId = useCurrentRouteId("useRoute" /* UseRoute */ );
    var id = (_args_ = args[0]) !== null && _args_ !== void 0 ? _args_ : currentRouteId;
    var state = useDataRouterState("useRoute" /* UseRoute */ );
    var route = state.matches.find((param)=>{
        var { route: route2 } = param;
        return route2.id === id;
    });
    if (route === void 0) return void 0;
    return {
        handle: route.route.handle,
        loaderData: state.loaderData[id],
        actionData: (_state_actionData = state.actionData) === null || _state_actionData === void 0 ? void 0 : _state_actionData[id]
    };
}
_s34(useRoute, "yFYAPtgpPuxkW4PbmBugG4HAOD0=", false, function() {
    return [
        useCurrentRouteId,
        useDataRouterState
    ];
});
// lib/components.tsx

// lib/server-runtime/warnings.ts
var alreadyWarned2 = {};
function warnOnce(condition, message) {
    if (!condition && !alreadyWarned2[message]) {
        alreadyWarned2[message] = true;
        console.warn(message);
    }
}
// lib/components.tsx
var USE_OPTIMISTIC = "useOptimistic";
var useOptimisticImpl = react__rspack_import_0[USE_OPTIMISTIC];
var stableUseOptimisticSetter = ()=>void 0;
function useOptimisticSafe(val) {
    if (useOptimisticImpl) {
        return useOptimisticImpl(val);
    } else {
        return [
            val,
            stableUseOptimisticSetter
        ];
    }
}
function mapRouteProperties(route) {
    var updates = {
        // Note: this check also occurs in createRoutesFromChildren so update
        // there if you change this -- please and thank you!
        hasErrorBoundary: route.hasErrorBoundary || route.ErrorBoundary != null || route.errorElement != null
    };
    if (route.Component) {
        if (ENABLE_DEV_WARNINGS) {
            if (route.element) {
                warning(false, "You should not include both `Component` and `element` on your route - `Component` will be used.");
            }
        }
        Object.assign(updates, {
            element: /*#__PURE__*/ react__rspack_import_0.createElement(route.Component),
            Component: void 0
        });
    }
    if (route.HydrateFallback) {
        if (ENABLE_DEV_WARNINGS) {
            if (route.hydrateFallbackElement) {
                warning(false, "You should not include both `HydrateFallback` and `hydrateFallbackElement` on your route - `HydrateFallback` will be used.");
            }
        }
        Object.assign(updates, {
            hydrateFallbackElement: /*#__PURE__*/ react__rspack_import_0.createElement(route.HydrateFallback),
            HydrateFallback: void 0
        });
    }
    if (route.ErrorBoundary) {
        if (ENABLE_DEV_WARNINGS) {
            if (route.errorElement) {
                warning(false, "You should not include both `ErrorBoundary` and `errorElement` on your route - `ErrorBoundary` will be used.");
            }
        }
        Object.assign(updates, {
            errorElement: /*#__PURE__*/ react__rspack_import_0.createElement(route.ErrorBoundary),
            ErrorBoundary: void 0
        });
    }
    return updates;
}
var hydrationRouteProperties = [
    "HydrateFallback",
    "hydrateFallbackElement"
];
function createMemoryRouter(routes, opts) {
    return createRouter({
        basename: opts === null || opts === void 0 ? void 0 : opts.basename,
        getContext: opts === null || opts === void 0 ? void 0 : opts.getContext,
        future: opts === null || opts === void 0 ? void 0 : opts.future,
        history: createMemoryHistory({
            initialEntries: opts === null || opts === void 0 ? void 0 : opts.initialEntries,
            initialIndex: opts === null || opts === void 0 ? void 0 : opts.initialIndex
        }),
        hydrationData: opts === null || opts === void 0 ? void 0 : opts.hydrationData,
        routes,
        hydrationRouteProperties,
        mapRouteProperties,
        dataStrategy: opts === null || opts === void 0 ? void 0 : opts.dataStrategy,
        patchRoutesOnNavigation: opts === null || opts === void 0 ? void 0 : opts.patchRoutesOnNavigation,
        unstable_instrumentations: opts === null || opts === void 0 ? void 0 : opts.unstable_instrumentations
    }).initialize();
}
var Deferred = class Deferred {
    constructor(){
        this.status = "pending";
        this.promise = new Promise((resolve, reject)=>{
            this.resolve = (value)=>{
                if (this.status === "pending") {
                    this.status = "resolved";
                    resolve(value);
                }
            };
            this.reject = (reason)=>{
                if (this.status === "pending") {
                    this.status = "rejected";
                    reject(reason);
                }
            };
        });
    }
};
function RouterProvider(param) {
    var { router, flushSync: reactDomFlushSyncImpl, onError, unstable_useTransitions } = param;
    _s35();
    var unstable_rsc = useIsRSCRouterContext();
    unstable_useTransitions = unstable_rsc || unstable_useTransitions;
    var [_state, setStateImpl] = react__rspack_import_0.useState(router.state);
    var [state, setOptimisticState] = useOptimisticSafe(_state);
    var [pendingState, setPendingState] = react__rspack_import_0.useState();
    var [vtContext, setVtContext] = react__rspack_import_0.useState({
        isTransitioning: false
    });
    var [renderDfd, setRenderDfd] = react__rspack_import_0.useState();
    var [transition, setTransition] = react__rspack_import_0.useState();
    var [interruption, setInterruption] = react__rspack_import_0.useState();
    var fetcherData = react__rspack_import_0.useRef(/* @__PURE__ */ new Map());
    var setState = react__rspack_import_0.useCallback((newState, param)=>{
        var { deletedFetchers, newErrors, flushSync, viewTransitionOpts } = param;
        if (newErrors && onError) {
            Object.values(newErrors).forEach((error)=>{
                var _ref;
                var _newState_matches_;
                return onError(error, {
                    location: newState.location,
                    params: (_ref = (_newState_matches_ = newState.matches[0]) === null || _newState_matches_ === void 0 ? void 0 : _newState_matches_.params) !== null && _ref !== void 0 ? _ref : {},
                    unstable_pattern: getRoutePattern(newState.matches)
                });
            });
        }
        newState.fetchers.forEach((fetcher, key)=>{
            if (fetcher.data !== void 0) {
                fetcherData.current.set(key, fetcher.data);
            }
        });
        deletedFetchers.forEach((key)=>fetcherData.current.delete(key));
        warnOnce(flushSync === false || reactDomFlushSyncImpl != null, 'You provided the `flushSync` option to a router update, but you are not using the `<RouterProvider>` from `react-router/dom` so `ReactDOM.flushSync()` is unavailable.  Please update your app to `import { RouterProvider } from "react-router/dom"` and ensure you have `react-dom` installed as a dependency to use the `flushSync` option.');
        var isViewTransitionAvailable = router.window != null && router.window.document != null && typeof router.window.document.startViewTransition === "function";
        warnOnce(viewTransitionOpts == null || isViewTransitionAvailable, "You provided the `viewTransition` option to a router update, but you do not appear to be running in a DOM environment as `window.startViewTransition` is not available.");
        if (!viewTransitionOpts || !isViewTransitionAvailable) {
            if (reactDomFlushSyncImpl && flushSync) {
                reactDomFlushSyncImpl(()=>setStateImpl(newState));
            } else if (unstable_useTransitions === false) {
                setStateImpl(newState);
            } else {
                react__rspack_import_0.startTransition(()=>{
                    if (unstable_useTransitions === true) {
                        setOptimisticState((s)=>getOptimisticRouterState(s, newState));
                    }
                    setStateImpl(newState);
                });
            }
            return;
        }
        if (reactDomFlushSyncImpl && flushSync) {
            reactDomFlushSyncImpl(()=>{
                if (transition) {
                    renderDfd === null || renderDfd === void 0 ? void 0 : renderDfd.resolve();
                    transition.skipTransition();
                }
                setVtContext({
                    isTransitioning: true,
                    flushSync: true,
                    currentLocation: viewTransitionOpts.currentLocation,
                    nextLocation: viewTransitionOpts.nextLocation
                });
            });
            var t = router.window.document.startViewTransition(()=>{
                reactDomFlushSyncImpl(()=>setStateImpl(newState));
            });
            t.finished.finally(()=>{
                reactDomFlushSyncImpl(()=>{
                    setRenderDfd(void 0);
                    setTransition(void 0);
                    setPendingState(void 0);
                    setVtContext({
                        isTransitioning: false
                    });
                });
            });
            reactDomFlushSyncImpl(()=>setTransition(t));
            return;
        }
        if (transition) {
            renderDfd === null || renderDfd === void 0 ? void 0 : renderDfd.resolve();
            transition.skipTransition();
            setInterruption({
                state: newState,
                currentLocation: viewTransitionOpts.currentLocation,
                nextLocation: viewTransitionOpts.nextLocation
            });
        } else {
            setPendingState(newState);
            setVtContext({
                isTransitioning: true,
                flushSync: false,
                currentLocation: viewTransitionOpts.currentLocation,
                nextLocation: viewTransitionOpts.nextLocation
            });
        }
    }, [
        router.window,
        reactDomFlushSyncImpl,
        transition,
        renderDfd,
        unstable_useTransitions,
        setOptimisticState,
        onError
    ]);
    react__rspack_import_0.useLayoutEffect(()=>router.subscribe(setState), [
        router,
        setState
    ]);
    react__rspack_import_0.useEffect(()=>{
        if (vtContext.isTransitioning && !vtContext.flushSync) {
            setRenderDfd(new Deferred());
        }
    }, [
        vtContext
    ]);
    react__rspack_import_0.useEffect(()=>{
        if (renderDfd && pendingState && router.window) {
            var newState = pendingState;
            var renderPromise = renderDfd.promise;
            var transition2 = router.window.document.startViewTransition(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                    if (unstable_useTransitions === false) {
                        setStateImpl(newState);
                    } else {
                        react__rspack_import_0.startTransition(()=>{
                            if (unstable_useTransitions === true) {
                                setOptimisticState((s)=>getOptimisticRouterState(s, newState));
                            }
                            setStateImpl(newState);
                        });
                    }
                    yield renderPromise;
                })());
            transition2.finished.finally(()=>{
                setRenderDfd(void 0);
                setTransition(void 0);
                setPendingState(void 0);
                setVtContext({
                    isTransitioning: false
                });
            });
            setTransition(transition2);
        }
    }, [
        pendingState,
        renderDfd,
        router.window,
        unstable_useTransitions,
        setOptimisticState
    ]);
    react__rspack_import_0.useEffect(()=>{
        if (renderDfd && pendingState && state.location.key === pendingState.location.key) {
            renderDfd.resolve();
        }
    }, [
        renderDfd,
        transition,
        state.location,
        pendingState
    ]);
    react__rspack_import_0.useEffect(()=>{
        if (!vtContext.isTransitioning && interruption) {
            setPendingState(interruption.state);
            setVtContext({
                isTransitioning: true,
                flushSync: false,
                currentLocation: interruption.currentLocation,
                nextLocation: interruption.nextLocation
            });
            setInterruption(void 0);
        }
    }, [
        vtContext.isTransitioning,
        interruption
    ]);
    var navigator = react__rspack_import_0.useMemo(()=>{
        return {
            createHref: router.createHref,
            encodeLocation: router.encodeLocation,
            go: (n)=>router.navigate(n),
            push: (to, state2, opts)=>router.navigate(to, {
                    state: state2,
                    preventScrollReset: opts === null || opts === void 0 ? void 0 : opts.preventScrollReset
                }),
            replace: (to, state2, opts)=>router.navigate(to, {
                    replace: true,
                    state: state2,
                    preventScrollReset: opts === null || opts === void 0 ? void 0 : opts.preventScrollReset
                })
        };
    }, [
        router
    ]);
    var basename = router.basename || "/";
    var dataRouterContext = react__rspack_import_0.useMemo(()=>({
            router,
            navigator,
            static: false,
            basename,
            onError
        }), [
        router,
        navigator,
        basename,
        onError
    ]);
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(DataRouterContext.Provider, {
        value: dataRouterContext
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(DataRouterStateContext.Provider, {
        value: state
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(FetchersContext.Provider, {
        value: fetcherData.current
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(ViewTransitionContext.Provider, {
        value: vtContext
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Router, {
        basename,
        location: state.location,
        navigationType: state.historyAction,
        navigator,
        unstable_useTransitions
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(MemoizedDataRoutes, {
        routes: router.routes,
        future: router.future,
        state,
        onError
    })))))), null);
}
_s35(RouterProvider, "15Z9pmU81gIHDHqIZpuVH8gSTDU=", false, function() {
    return [
        useIsRSCRouterContext,
        useOptimisticSafe
    ];
});
_c4 = RouterProvider;
function getOptimisticRouterState(currentState, newState) {
    return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, currentState), {
        // Only surface "pending/in-flight stuff"
        // (navigation, revalidation, actionData, fetchers, )
        navigation: newState.navigation.state !== "idle" ? newState.navigation : currentState.navigation,
        revalidation: newState.revalidation !== "idle" ? newState.revalidation : currentState.revalidation,
        actionData: newState.navigation.state !== "submitting" ? newState.actionData : currentState.actionData,
        fetchers: newState.fetchers
    });
}
var MemoizedDataRoutes = /*#__PURE__*/ react__rspack_import_0.memo(DataRoutes);
_c5 = MemoizedDataRoutes;
function DataRoutes(param) {
    var { routes, future, state, onError } = param;
    _s36();
    return useRoutesImpl(routes, void 0, state, onError, future);
}
_s36(DataRoutes, "bxXQ1PjoUitFHQG/730mCYNdT00=", false, function() {
    return [
        useRoutesImpl
    ];
});
_c6 = DataRoutes;
function MemoryRouter(param) {
    var { basename, children, initialEntries, initialIndex, unstable_useTransitions } = param;
    _s37();
    var historyRef = react__rspack_import_0.useRef();
    if (historyRef.current == null) {
        historyRef.current = createMemoryHistory({
            initialEntries,
            initialIndex,
            v5Compat: true
        });
    }
    var history = historyRef.current;
    var [state, setStateImpl] = react__rspack_import_0.useState({
        action: history.action,
        location: history.location
    });
    var setState = react__rspack_import_0.useCallback((newState)=>{
        if (unstable_useTransitions === false) {
            setStateImpl(newState);
        } else {
            react__rspack_import_0.startTransition(()=>setStateImpl(newState));
        }
    }, [
        unstable_useTransitions
    ]);
    react__rspack_import_0.useLayoutEffect(()=>history.listen(setState), [
        history,
        setState
    ]);
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Router, {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        unstable_useTransitions
    });
}
_s37(MemoryRouter, "4hUJUqHydkgNqll0yBdcphm8IwU=");
_c7 = MemoryRouter;
function Navigate(param) {
    var { to, replace: replace2, state, relative } = param;
    _s38();
    invariant(useInRouterContext(), // TODO: This error is probably because they somehow have 2 versions of
    // the router loaded. We can help them understand how to avoid that.
    "<Navigate> may be used only in the context of a <Router> component.");
    var { static: isStatic } = react__rspack_import_0.useContext(NavigationContext);
    warning(!isStatic, "<Navigate> must not be used on the initial render in a <StaticRouter>. This is a no-op, but you should modify your code so the <Navigate> is only ever rendered in response to some user interaction or state change.");
    var { matches } = react__rspack_import_0.useContext(RouteContext);
    var { pathname: locationPathname } = useLocation();
    var navigate = useNavigate();
    var path = resolveTo(to, getResolveToMatches(matches), locationPathname, relative === "path");
    var jsonPath = JSON.stringify(path);
    react__rspack_import_0.useEffect(()=>{
        navigate(JSON.parse(jsonPath), {
            replace: replace2,
            state,
            relative
        });
    }, [
        navigate,
        jsonPath,
        relative,
        replace2,
        state
    ]);
    return null;
}
_s38(Navigate, "IqBYE0vt0UbMhMc14u6Y2Uw6QKc=", false, function() {
    return [
        useInRouterContext,
        useLocation,
        useNavigate
    ];
});
_c8 = Navigate;
function Outlet(props) {
    _s39();
    return useOutlet(props.context);
}
_s39(Outlet, "UOsMKhZRqBWgnX+NkNBlxu0sfe8=", false, function() {
    return [
        useOutlet
    ];
});
_c9 = Outlet;
function Route(props) {
    invariant(false, "A <Route> is only ever to be used as the child of <Routes> element, never rendered directly. Please wrap your <Route> in a <Routes>.");
}
_c10 = Route;
function Router(param) {
    var { basename: basenameProp = "/", children = null, location: locationProp, navigationType = "POP" /* Pop */ , navigator, static: staticProp = false, unstable_useTransitions } = param;
    _s40();
    invariant(!useInRouterContext(), "You cannot render a <Router> inside another <Router>. You should never have more than one in your app.");
    var basename = basenameProp.replace(/^\/*/, "/");
    var navigationContext = react__rspack_import_0.useMemo(()=>({
            basename,
            navigator,
            static: staticProp,
            unstable_useTransitions,
            future: {}
        }), [
        basename,
        navigator,
        staticProp,
        unstable_useTransitions
    ]);
    if (typeof locationProp === "string") {
        locationProp = parsePath(locationProp);
    }
    var { pathname = "/", search = "", hash = "", state = null, key = "default" } = locationProp;
    var locationContext = react__rspack_import_0.useMemo(()=>{
        var trailingPathname = stripBasename(pathname, basename);
        if (trailingPathname == null) {
            return null;
        }
        return {
            location: {
                pathname: trailingPathname,
                search,
                hash,
                state,
                key
            },
            navigationType
        };
    }, [
        basename,
        pathname,
        search,
        hash,
        state,
        key,
        navigationType
    ]);
    warning(locationContext != null, '<Router basename="'.concat(basename, '"> is not able to match the URL "').concat(pathname).concat(search).concat(hash, "\" because it does not start with the basename, so the <Router> won't render anything."));
    if (locationContext == null) {
        return null;
    }
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(NavigationContext.Provider, {
        value: navigationContext
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(LocationContext.Provider, {
        children,
        value: locationContext
    }));
}
_s40(Router, "cWELrQ0qrnJGlFKBUy43aV+1NAk=", false, function() {
    return [
        useInRouterContext
    ];
});
_c11 = Router;
function Routes(param) {
    var { children, location } = param;
    _s41();
    return useRoutes(createRoutesFromChildren(children), location);
}
_s41(Routes, "mNBMh7IZrKf5fDyw9x3t8azefmI=", false, function() {
    return [
        useRoutes
    ];
});
_c12 = Routes;
function Await(param) {
    var { children, errorElement, resolve } = param;
    _s42();
    var dataRouterContext = react__rspack_import_0.useContext(DataRouterContext);
    var dataRouterStateContext = react__rspack_import_0.useContext(DataRouterStateContext);
    var onError = react__rspack_import_0.useCallback((error, errorInfo)=>{
        if (dataRouterContext && dataRouterContext.onError && dataRouterStateContext) {
            var _dataRouterStateContext_matches_;
            dataRouterContext.onError(error, {
                location: dataRouterStateContext.location,
                params: ((_dataRouterStateContext_matches_ = dataRouterStateContext.matches[0]) === null || _dataRouterStateContext_matches_ === void 0 ? void 0 : _dataRouterStateContext_matches_.params) || {},
                unstable_pattern: getRoutePattern(dataRouterStateContext.matches),
                errorInfo
            });
        }
    }, [
        dataRouterContext,
        dataRouterStateContext
    ]);
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(AwaitErrorBoundary, {
        resolve,
        errorElement,
        onError
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(ResolveAwait, null, children));
}
_s42(Await, "SUe3oTY6C760zDppLI5O7/FhNMs=");
_c13 = Await;
var AwaitErrorBoundary = class AwaitErrorBoundary extends react__rspack_import_0.Component {
    static getDerivedStateFromError(error) {
        return {
            error
        };
    }
    componentDidCatch(error, errorInfo) {
        if (this.props.onError) {
            this.props.onError(error, errorInfo);
        } else {
            console.error("<Await> caught the following error during render", error, errorInfo);
        }
    }
    render() {
        var { children, errorElement, resolve } = this.props;
        var promise = null;
        var status = 0 /* pending */ ;
        if (!(resolve instanceof Promise)) {
            status = 1 /* success */ ;
            promise = Promise.resolve();
            Object.defineProperty(promise, "_tracked", {
                get: ()=>true
            });
            Object.defineProperty(promise, "_data", {
                get: ()=>resolve
            });
        } else if (this.state.error) {
            status = 2 /* error */ ;
            var renderError = this.state.error;
            promise = Promise.reject().catch(()=>{});
            Object.defineProperty(promise, "_tracked", {
                get: ()=>true
            });
            Object.defineProperty(promise, "_error", {
                get: ()=>renderError
            });
        } else if (resolve._tracked) {
            promise = resolve;
            status = "_error" in promise ? 2 /* error */  : "_data" in promise ? 1 /* success */  : 0 /* pending */ ;
        } else {
            status = 0 /* pending */ ;
            Object.defineProperty(resolve, "_tracked", {
                get: ()=>true
            });
            promise = resolve.then((data2)=>Object.defineProperty(resolve, "_data", {
                    get: ()=>data2
                }), (error)=>{
                var _this_props_onError, _this_props;
                (_this_props_onError = (_this_props = this.props).onError) === null || _this_props_onError === void 0 ? void 0 : _this_props_onError.call(_this_props, error);
                Object.defineProperty(resolve, "_error", {
                    get: ()=>error
                });
            });
        }
        if (status === 2 /* error */  && !errorElement) {
            throw promise._error;
        }
        if (status === 2 /* error */ ) {
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(AwaitContext.Provider, {
                value: promise,
                children: errorElement
            });
        }
        if (status === 1 /* success */ ) {
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(AwaitContext.Provider, {
                value: promise,
                children
            });
        }
        throw promise;
    }
    constructor(props){
        super(props);
        this.state = {
            error: null
        };
    }
};
function ResolveAwait(param) {
    var { children } = param;
    _s43();
    var data2 = useAsyncValue();
    var toRender = typeof children === "function" ? children(data2) : children;
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, toRender);
}
_s43(ResolveAwait, "8Z6BgPDBYRsgORuIHCVIk9qQeK8=", false, function() {
    return [
        useAsyncValue
    ];
});
_c14 = ResolveAwait;
function createRoutesFromChildren(children) {
    var parentPath = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
    var routes = [];
    react__rspack_import_0.Children.forEach(children, (element, index)=>{
        if (!/*#__PURE__*/ react__rspack_import_0.isValidElement(element)) {
            return;
        }
        var treePath = [
            ...parentPath,
            index
        ];
        if (element.type === react__rspack_import_0.Fragment) {
            routes.push.apply(routes, createRoutesFromChildren(element.props.children, treePath));
            return;
        }
        invariant(element.type === Route, "[".concat(typeof element.type === "string" ? element.type : element.type.name, "] is not a <Route> component. All component children of <Routes> must be a <Route> or <React.Fragment>"));
        invariant(!element.props.index || !element.props.children, "An index route cannot have child routes.");
        var route = {
            id: element.props.id || treePath.join("-"),
            caseSensitive: element.props.caseSensitive,
            element: element.props.element,
            Component: element.props.Component,
            index: element.props.index,
            path: element.props.path,
            middleware: element.props.middleware,
            loader: element.props.loader,
            action: element.props.action,
            hydrateFallbackElement: element.props.hydrateFallbackElement,
            HydrateFallback: element.props.HydrateFallback,
            errorElement: element.props.errorElement,
            ErrorBoundary: element.props.ErrorBoundary,
            hasErrorBoundary: element.props.hasErrorBoundary === true || element.props.ErrorBoundary != null || element.props.errorElement != null,
            shouldRevalidate: element.props.shouldRevalidate,
            handle: element.props.handle,
            lazy: element.props.lazy
        };
        if (element.props.children) {
            route.children = createRoutesFromChildren(element.props.children, treePath);
        }
        routes.push(route);
    });
    return routes;
}
var createRoutesFromElements = createRoutesFromChildren;
function renderMatches(matches) {
    return _renderMatches(matches);
}
function useRouteComponentProps() {
    _s44();
    return {
        params: useParams(),
        loaderData: useLoaderData(),
        actionData: useActionData(),
        matches: useMatches()
    };
}
_s44(useRouteComponentProps, "UOcmTMVs4ZRZ1ipbMfcbH+CTbDQ=", false, function() {
    return [
        useParams,
        useLoaderData,
        useActionData,
        useMatches
    ];
});
function WithComponentProps(param) {
    var { children } = param;
    _s45();
    var props = useRouteComponentProps();
    return /*#__PURE__*/ react__rspack_import_0.cloneElement(children, props);
}
_s45(WithComponentProps, "/Wk5xc5ej6jQNt6kqD7XhXjF83s=", false, function() {
    return [
        useRouteComponentProps
    ];
});
_c15 = WithComponentProps;
function withComponentProps(Component4) {
    var _s = $RefreshSig$();
    return _s(function WithComponentProps2() {
        _s();
        var props = useRouteComponentProps();
        return /*#__PURE__*/ react__rspack_import_0.createElement(Component4, props);
    }, "/Wk5xc5ej6jQNt6kqD7XhXjF83s=", false, function() {
        return [
            useRouteComponentProps
        ];
    });
}
function useHydrateFallbackProps() {
    _s46();
    return {
        params: useParams(),
        loaderData: useLoaderData(),
        actionData: useActionData()
    };
}
_s46(useHydrateFallbackProps, "Rm/3XQ6BjX9s9HMBRwsDqXN4UyA=", false, function() {
    return [
        useParams,
        useLoaderData,
        useActionData
    ];
});
function WithHydrateFallbackProps(param) {
    var { children } = param;
    _s47();
    var props = useHydrateFallbackProps();
    return /*#__PURE__*/ react__rspack_import_0.cloneElement(children, props);
}
_s47(WithHydrateFallbackProps, "uux2Uo262i4Y/DLv5GsAkixdiSA=", false, function() {
    return [
        useHydrateFallbackProps
    ];
});
_c16 = WithHydrateFallbackProps;
function withHydrateFallbackProps(HydrateFallback) {
    var _s = $RefreshSig$();
    return _s(function WithHydrateFallbackProps2() {
        _s();
        var props = useHydrateFallbackProps();
        return /*#__PURE__*/ react__rspack_import_0.createElement(HydrateFallback, props);
    }, "uux2Uo262i4Y/DLv5GsAkixdiSA=", false, function() {
        return [
            useHydrateFallbackProps
        ];
    });
}
function useErrorBoundaryProps() {
    _s48();
    return {
        params: useParams(),
        loaderData: useLoaderData(),
        actionData: useActionData(),
        error: useRouteError()
    };
}
_s48(useErrorBoundaryProps, "4h6p2Zc6uWv27NvS5jyYfBLUZPg=", false, function() {
    return [
        useParams,
        useLoaderData,
        useActionData,
        useRouteError
    ];
});
function WithErrorBoundaryProps(param) {
    var { children } = param;
    _s49();
    var props = useErrorBoundaryProps();
    return /*#__PURE__*/ react__rspack_import_0.cloneElement(children, props);
}
_s49(WithErrorBoundaryProps, "Sj1faioYmTZPKo4L4I3/STK8SH0=", false, function() {
    return [
        useErrorBoundaryProps
    ];
});
_c17 = WithErrorBoundaryProps;
function withErrorBoundaryProps(ErrorBoundary) {
    var _s = $RefreshSig$();
    return _s(function WithErrorBoundaryProps2() {
        _s();
        var props = useErrorBoundaryProps();
        return /*#__PURE__*/ react__rspack_import_0.createElement(ErrorBoundary, props);
    }, "Sj1faioYmTZPKo4L4I3/STK8SH0=", false, function() {
        return [
            useErrorBoundaryProps
        ];
    });
}
// lib/dom/dom.ts
var defaultMethod = "get";
var defaultEncType = "application/x-www-form-urlencoded";
function isHtmlElement(object) {
    return typeof HTMLElement !== "undefined" && object instanceof HTMLElement;
}
function isButtonElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "button";
}
function isFormElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "form";
}
function isInputElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "input";
}
function isModifiedEvent(event) {
    return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
}
function shouldProcessLinkClick(event, target) {
    return event.button === 0 && // Ignore everything but left clicks
    (!target || target === "_self") && // Let browser handle "target=_blank" etc.
    !isModifiedEvent(event);
}
function createSearchParams() {
    var init = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "";
    return new URLSearchParams(typeof init === "string" || Array.isArray(init) || init instanceof URLSearchParams ? init : Object.keys(init).reduce((memo2, key)=>{
        var value = init[key];
        return memo2.concat(Array.isArray(value) ? value.map((v)=>[
                key,
                v
            ]) : [
            [
                key,
                value
            ]
        ]);
    }, []));
}
function getSearchParamsForLocation(locationSearch, defaultSearchParams) {
    var searchParams = createSearchParams(locationSearch);
    if (defaultSearchParams) {
        defaultSearchParams.forEach((_, key)=>{
            if (!searchParams.has(key)) {
                defaultSearchParams.getAll(key).forEach((value)=>{
                    searchParams.append(key, value);
                });
            }
        });
    }
    return searchParams;
}
var _formDataSupportsSubmitter = null;
function isFormDataSubmitterSupported() {
    if (_formDataSupportsSubmitter === null) {
        try {
            new FormData(document.createElement("form"), // @ts-expect-error if FormData supports the submitter parameter, this will throw
            0);
            _formDataSupportsSubmitter = false;
        } catch (e) {
            _formDataSupportsSubmitter = true;
        }
    }
    return _formDataSupportsSubmitter;
}
var supportedFormEncTypes = /* @__PURE__ */ new Set([
    "application/x-www-form-urlencoded",
    "multipart/form-data",
    "text/plain"
]);
function getFormEncType(encType) {
    if (encType != null && !supportedFormEncTypes.has(encType)) {
        warning(false, '"'.concat(encType, '" is not a valid `encType` for `<Form>`/`<fetcher.Form>` and will default to "').concat(defaultEncType, '"'));
        return null;
    }
    return encType;
}
function getFormSubmissionInfo(target, basename) {
    var method;
    var action;
    var encType;
    var formData;
    var body;
    if (isFormElement(target)) {
        var attr = target.getAttribute("action");
        action = attr ? stripBasename(attr, basename) : null;
        method = target.getAttribute("method") || defaultMethod;
        encType = getFormEncType(target.getAttribute("enctype")) || defaultEncType;
        formData = new FormData(target);
    } else if (isButtonElement(target) || isInputElement(target) && (target.type === "submit" || target.type === "image")) {
        var form = target.form;
        if (form == null) {
            throw new Error('Cannot submit a <button> or <input type="submit"> without a <form>');
        }
        var attr1 = target.getAttribute("formaction") || form.getAttribute("action");
        action = attr1 ? stripBasename(attr1, basename) : null;
        method = target.getAttribute("formmethod") || form.getAttribute("method") || defaultMethod;
        encType = getFormEncType(target.getAttribute("formenctype")) || getFormEncType(form.getAttribute("enctype")) || defaultEncType;
        formData = new FormData(form, target);
        if (!isFormDataSubmitterSupported()) {
            var { name, type, value } = target;
            if (type === "image") {
                var prefix = name ? "".concat(name, ".") : "";
                formData.append("".concat(prefix, "x"), "0");
                formData.append("".concat(prefix, "y"), "0");
            } else if (name) {
                formData.append(name, value);
            }
        }
    } else if (isHtmlElement(target)) {
        throw new Error('Cannot submit element that is not <form>, <button>, or <input type="submit|image">');
    } else {
        method = defaultMethod;
        action = null;
        encType = defaultEncType;
        body = target;
    }
    if (formData && encType === "text/plain") {
        body = formData;
        formData = void 0;
    }
    return {
        action,
        method: method.toLowerCase(),
        encType,
        formData,
        body
    };
}
// lib/dom/ssr/single-fetch.tsx

// vendor/turbo-stream-v2/utils.ts
var HOLE = -1;
var NAN = -2;
var NEGATIVE_INFINITY = -3;
var NEGATIVE_ZERO = -4;
var NULL = -5;
var POSITIVE_INFINITY = -6;
var UNDEFINED = -7;
var TYPE_BIGINT = "B";
var TYPE_DATE = "D";
var TYPE_ERROR = "E";
var TYPE_MAP = "M";
var TYPE_NULL_OBJECT = "N";
var TYPE_PROMISE = "P";
var TYPE_REGEXP = "R";
var TYPE_SET = "S";
var TYPE_SYMBOL = "Y";
var TYPE_URL = "U";
var TYPE_PREVIOUS_RESOLVED = "Z";
var Deferred2 = class Deferred2 {
    constructor(){
        this.promise = new Promise((resolve, reject)=>{
            this.resolve = resolve;
            this.reject = reject;
        });
    }
};
function createLineSplittingTransform() {
    var decoder = new TextDecoder();
    var leftover = "";
    return new TransformStream({
        transform (chunk, controller) {
            var str = decoder.decode(chunk, {
                stream: true
            });
            var parts = (leftover + str).split("\n");
            leftover = parts.pop() || "";
            for (var part of parts){
                controller.enqueue(part);
            }
        },
        flush (controller) {
            if (leftover) {
                controller.enqueue(leftover);
            }
        }
    });
}
// vendor/turbo-stream-v2/flatten.ts
function flatten(input) {
    var { indices } = this;
    var existing = indices.get(input);
    if (existing) return [
        existing
    ];
    if (input === void 0) return UNDEFINED;
    if (input === null) return NULL;
    if (Number.isNaN(input)) return NAN;
    if (input === Number.POSITIVE_INFINITY) return POSITIVE_INFINITY;
    if (input === Number.NEGATIVE_INFINITY) return NEGATIVE_INFINITY;
    if (input === 0 && 1 / input < 0) return NEGATIVE_ZERO;
    var index = this.index++;
    indices.set(input, index);
    stringify.call(this, input, index);
    return index;
}
function stringify(input, index) {
    var { deferred, plugins, postPlugins } = this;
    var str = this.stringified;
    var stack = [
        [
            input,
            index
        ]
    ];
    while(stack.length > 0){
        var [input2, index2] = stack.pop();
        var partsForObj = (obj)=>Object.keys(obj).map((k)=>'"_'.concat(flatten.call(this, k), '":').concat(flatten.call(this, obj[k]))).join(",");
        var error = null;
        switch(typeof input2){
            case "boolean":
            case "number":
            case "string":
                str[index2] = JSON.stringify(input2);
                break;
            case "bigint":
                str[index2] = '["'.concat(TYPE_BIGINT, '","').concat(input2, '"]');
                break;
            case "symbol":
                {
                    var keyFor = Symbol.keyFor(input2);
                    if (!keyFor) {
                        error = new Error("Cannot encode symbol unless created with Symbol.for()");
                    } else {
                        str[index2] = '["'.concat(TYPE_SYMBOL, '",').concat(JSON.stringify(keyFor), "]");
                    }
                    break;
                }
            case "object":
                {
                    if (!input2) {
                        str[index2] = "".concat(NULL);
                        break;
                    }
                    var isArray = Array.isArray(input2);
                    var pluginHandled = false;
                    if (!isArray && plugins) {
                        for (var plugin of plugins){
                            var pluginResult = plugin(input2);
                            if (Array.isArray(pluginResult)) {
                                pluginHandled = true;
                                var [pluginIdentifier, ...rest] = pluginResult;
                                str[index2] = "[".concat(JSON.stringify(pluginIdentifier));
                                if (rest.length > 0) {
                                    str[index2] += ",".concat(rest.map((v)=>flatten.call(this, v)).join(","));
                                }
                                str[index2] += "]";
                                break;
                            }
                        }
                    }
                    if (!pluginHandled) {
                        var result = isArray ? "[" : "{";
                        if (isArray) {
                            for(var i = 0; i < input2.length; i++)result += (i ? "," : "") + (i in input2 ? flatten.call(this, input2[i]) : HOLE);
                            str[index2] = "".concat(result, "]");
                        } else if (input2 instanceof Date) {
                            var dateTime = input2.getTime();
                            str[index2] = '["'.concat(TYPE_DATE, '",').concat(Number.isNaN(dateTime) ? JSON.stringify("invalid") : dateTime, "]");
                        } else if (input2 instanceof URL) {
                            str[index2] = '["'.concat(TYPE_URL, '",').concat(JSON.stringify(input2.href), "]");
                        } else if (input2 instanceof RegExp) {
                            str[index2] = '["'.concat(TYPE_REGEXP, '",').concat(JSON.stringify(input2.source), ",").concat(JSON.stringify(input2.flags), "]");
                        } else if (input2 instanceof Set) {
                            if (input2.size > 0) {
                                str[index2] = '["'.concat(TYPE_SET, '",').concat([
                                    ...input2
                                ].map((val)=>flatten.call(this, val)).join(","), "]");
                            } else {
                                str[index2] = '["'.concat(TYPE_SET, '"]');
                            }
                        } else if (input2 instanceof Map) {
                            if (input2.size > 0) {
                                str[index2] = '["'.concat(TYPE_MAP, '",').concat([
                                    ...input2
                                ].flatMap((param)=>{
                                    var [k, v] = param;
                                    return [
                                        flatten.call(this, k),
                                        flatten.call(this, v)
                                    ];
                                }).join(","), "]");
                            } else {
                                str[index2] = '["'.concat(TYPE_MAP, '"]');
                            }
                        } else if (input2 instanceof Promise) {
                            str[index2] = '["'.concat(TYPE_PROMISE, '",').concat(index2, "]");
                            deferred[index2] = input2;
                        } else if (input2 instanceof Error) {
                            str[index2] = '["'.concat(TYPE_ERROR, '",').concat(JSON.stringify(input2.message));
                            if (input2.name !== "Error") {
                                str[index2] += ",".concat(JSON.stringify(input2.name));
                            }
                            str[index2] += "]";
                        } else if (Object.getPrototypeOf(input2) === null) {
                            str[index2] = '["'.concat(TYPE_NULL_OBJECT, '",{').concat(partsForObj(input2), "}]");
                        } else if (isPlainObject2(input2)) {
                            str[index2] = "{".concat(partsForObj(input2), "}");
                        } else {
                            error = new Error("Cannot encode object with prototype");
                        }
                    }
                    break;
                }
            default:
                {
                    var isArray1 = Array.isArray(input2);
                    var pluginHandled1 = false;
                    if (!isArray1 && plugins) {
                        for (var plugin1 of plugins){
                            var pluginResult1 = plugin1(input2);
                            if (Array.isArray(pluginResult1)) {
                                pluginHandled1 = true;
                                var [pluginIdentifier1, ...rest1] = pluginResult1;
                                str[index2] = "[".concat(JSON.stringify(pluginIdentifier1));
                                if (rest1.length > 0) {
                                    str[index2] += ",".concat(rest1.map((v)=>flatten.call(this, v)).join(","));
                                }
                                str[index2] += "]";
                                break;
                            }
                        }
                    }
                    if (!pluginHandled1) {
                        error = new Error("Cannot encode function or unexpected type");
                    }
                }
        }
        if (error) {
            var pluginHandled2 = false;
            if (postPlugins) {
                for (var plugin2 of postPlugins){
                    var pluginResult2 = plugin2(input2);
                    if (Array.isArray(pluginResult2)) {
                        pluginHandled2 = true;
                        var [pluginIdentifier2, ...rest2] = pluginResult2;
                        str[index2] = "[".concat(JSON.stringify(pluginIdentifier2));
                        if (rest2.length > 0) {
                            str[index2] += ",".concat(rest2.map((v)=>flatten.call(this, v)).join(","));
                        }
                        str[index2] += "]";
                        break;
                    }
                }
            }
            if (!pluginHandled2) {
                throw error;
            }
        }
    }
}
var objectProtoNames2 = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
function isPlainObject2(thing) {
    var proto = Object.getPrototypeOf(thing);
    return proto === Object.prototype || proto === null || Object.getOwnPropertyNames(proto).sort().join("\0") === objectProtoNames2;
}
// vendor/turbo-stream-v2/unflatten.ts
var globalObj = typeof window !== "undefined" ? window : typeof globalThis !== "undefined" ? globalThis : void 0;
function unflatten(parsed) {
    var { hydrated, values } = this;
    if (typeof parsed === "number") return hydrate.call(this, parsed);
    if (!Array.isArray(parsed) || !parsed.length) throw new SyntaxError();
    var startIndex = values.length;
    for (var value of parsed){
        values.push(value);
    }
    hydrated.length = values.length;
    return hydrate.call(this, startIndex);
}
function hydrate(index) {
    var _loop = function() {
        var [index2, set] = stack.pop();
        switch(index2){
            case UNDEFINED:
                set(void 0);
                return "continue";
            case NULL:
                set(null);
                return "continue";
            case NAN:
                set(NaN);
                return "continue";
            case POSITIVE_INFINITY:
                set(Infinity);
                return "continue";
            case NEGATIVE_INFINITY:
                set(-Infinity);
                return "continue";
            case NEGATIVE_ZERO:
                set(-0);
                return "continue";
        }
        if (hydrated[index2]) {
            set(hydrated[index2]);
            return "continue";
        }
        var value = values[index2];
        if (!value || typeof value !== "object") {
            hydrated[index2] = value;
            set(value);
            return "continue";
        }
        if (Array.isArray(value)) {
            if (typeof value[0] === "string") {
                var [type, b, c] = value;
                switch(type){
                    case TYPE_DATE:
                        set(hydrated[index2] = new Date(b));
                        return "continue";
                    case TYPE_URL:
                        set(hydrated[index2] = new URL(b));
                        return "continue";
                    case TYPE_BIGINT:
                        set(hydrated[index2] = BigInt(b));
                        return "continue";
                    case TYPE_REGEXP:
                        set(hydrated[index2] = new RegExp(b, c));
                        return "continue";
                    case TYPE_SYMBOL:
                        set(hydrated[index2] = Symbol.for(b));
                        return "continue";
                    case TYPE_SET:
                        var newSet = /* @__PURE__ */ new Set();
                        hydrated[index2] = newSet;
                        for(var i = value.length - 1; i > 0; i--)stack.push([
                            value[i],
                            (v)=>{
                                newSet.add(v);
                            }
                        ]);
                        set(newSet);
                        return "continue";
                    case TYPE_MAP:
                        var _loop = function(i1) {
                            var r = [];
                            stack.push([
                                value[i1 + 1],
                                (v)=>{
                                    r[1] = v;
                                }
                            ]);
                            stack.push([
                                value[i1],
                                (k)=>{
                                    r[0] = k;
                                }
                            ]);
                            postRun.push(()=>{
                                map.set(r[0], r[1]);
                            });
                        };
                        var map = /* @__PURE__ */ new Map();
                        hydrated[index2] = map;
                        for(var i1 = value.length - 2; i1 > 0; i1 -= 2)_loop(i1);
                        set(map);
                        return "continue";
                    case TYPE_NULL_OBJECT:
                        var _loop1 = function(key) {
                            var r = [];
                            stack.push([
                                b[key],
                                (v)=>{
                                    r[1] = v;
                                }
                            ]);
                            stack.push([
                                Number(key.slice(1)),
                                (k)=>{
                                    r[0] = k;
                                }
                            ]);
                            postRun.push(()=>{
                                obj[r[0]] = r[1];
                            });
                        };
                        var obj = /* @__PURE__ */ Object.create(null);
                        hydrated[index2] = obj;
                        for (var key of Object.keys(b).reverse())_loop1(key);
                        set(obj);
                        return "continue";
                    case TYPE_PROMISE:
                        if (hydrated[b]) {
                            set(hydrated[index2] = hydrated[b]);
                        } else {
                            var d = new Deferred2();
                            deferred[b] = d;
                            set(hydrated[index2] = d.promise);
                        }
                        return "continue";
                    case TYPE_ERROR:
                        var [, message, errorType] = value;
                        var error = errorType && globalObj && globalObj[errorType] ? new globalObj[errorType](message) : new Error(message);
                        hydrated[index2] = error;
                        set(error);
                        return "continue";
                    case TYPE_PREVIOUS_RESOLVED:
                        set(hydrated[index2] = hydrated[b]);
                        return "continue";
                    default:
                        if (Array.isArray(plugins)) {
                            var _loop2 = function(i2) {
                                var v = vals[i2];
                                stack.push([
                                    v,
                                    (v2)=>{
                                        r[i2] = v2;
                                    }
                                ]);
                            };
                            var r = [];
                            var vals = value.slice(1);
                            for(var i2 = 0; i2 < vals.length; i2++)_loop2(i2);
                            postRun.push(()=>{
                                for (var plugin of plugins){
                                    var result2 = plugin(value[0], ...r);
                                    if (result2) {
                                        set(hydrated[index2] = result2.value);
                                        return;
                                    }
                                }
                                throw new SyntaxError();
                            });
                            return "continue";
                        }
                        throw new SyntaxError();
                }
            } else {
                var _loop3 = function(i3) {
                    var n = value[i3];
                    if (n !== HOLE) {
                        stack.push([
                            n,
                            (v)=>{
                                array[i3] = v;
                            }
                        ]);
                    }
                };
                var array = [];
                hydrated[index2] = array;
                for(var i3 = 0; i3 < value.length; i3++)_loop3(i3);
                set(array);
                return "continue";
            }
        } else {
            var _loop4 = function(key1) {
                var r = [];
                stack.push([
                    value[key1],
                    (v)=>{
                        r[1] = v;
                    }
                ]);
                stack.push([
                    Number(key1.slice(1)),
                    (k)=>{
                        r[0] = k;
                    }
                ]);
                postRun.push(()=>{
                    object[r[0]] = r[1];
                });
            };
            var object = {};
            hydrated[index2] = object;
            for (var key1 of Object.keys(value).reverse())_loop4(key1);
            set(object);
            return "continue";
        }
    };
    var { hydrated, values, deferred, plugins } = this;
    var result;
    var stack = [
        [
            index,
            (v)=>{
                result = v;
            }
        ]
    ];
    var postRun = [];
    while(stack.length > 0)_loop();
    while(postRun.length > 0){
        postRun.pop()();
    }
    return result;
}
// vendor/turbo-stream-v2/turbo-stream.ts
function decode(readable, options) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var { plugins } = options !== null && options !== void 0 ? options : {};
        var done = new Deferred2();
        var reader = readable.pipeThrough(createLineSplittingTransform()).getReader();
        var decoder = {
            values: [],
            hydrated: [],
            deferred: {},
            plugins
        };
        var decoded = yield decodeInitial.call(decoder, reader);
        var donePromise = done.promise;
        if (decoded.done) {
            done.resolve();
        } else {
            donePromise = decodeDeferred.call(decoder, reader).then(done.resolve).catch((reason)=>{
                for (var deferred of Object.values(decoder.deferred)){
                    deferred.reject(reason);
                }
                done.reject(reason);
            });
        }
        return {
            done: donePromise.then(()=>reader.closed),
            value: decoded.value
        };
    })();
}
function decodeInitial(reader) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var read = yield reader.read();
        if (!read.value) {
            throw new SyntaxError();
        }
        var line;
        try {
            line = JSON.parse(read.value);
        } catch (reason) {
            throw new SyntaxError();
        }
        return {
            done: read.done,
            value: unflatten.call(this, line)
        };
    }).call(this);
}
function decodeDeferred(reader) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var read = yield reader.read();
        while(!read.done){
            if (!read.value) continue;
            var line = read.value;
            switch(line[0]){
                case TYPE_PROMISE:
                    {
                        var colonIndex = line.indexOf(":");
                        var deferredId = Number(line.slice(1, colonIndex));
                        var deferred = this.deferred[deferredId];
                        if (!deferred) {
                            throw new Error("Deferred ID ".concat(deferredId, " not found in stream"));
                        }
                        var lineData = line.slice(colonIndex + 1);
                        var jsonLine = void 0;
                        try {
                            jsonLine = JSON.parse(lineData);
                        } catch (reason) {
                            throw new SyntaxError();
                        }
                        var value = unflatten.call(this, jsonLine);
                        deferred.resolve(value);
                        break;
                    }
                case TYPE_ERROR:
                    {
                        var colonIndex1 = line.indexOf(":");
                        var deferredId1 = Number(line.slice(1, colonIndex1));
                        var deferred1 = this.deferred[deferredId1];
                        if (!deferred1) {
                            throw new Error("Deferred ID ".concat(deferredId1, " not found in stream"));
                        }
                        var lineData1 = line.slice(colonIndex1 + 1);
                        var jsonLine1 = void 0;
                        try {
                            jsonLine1 = JSON.parse(lineData1);
                        } catch (reason) {
                            throw new SyntaxError();
                        }
                        var value1 = unflatten.call(this, jsonLine1);
                        deferred1.reject(value1);
                        break;
                    }
                default:
                    throw new SyntaxError();
            }
            read = yield reader.read();
        }
    }).call(this);
}
function encode(input, options) {
    var { plugins, postPlugins, signal } = options !== null && options !== void 0 ? options : {};
    var encoder = {
        deferred: {},
        index: 0,
        indices: /* @__PURE__ */ new Map(),
        stringified: [],
        plugins,
        postPlugins,
        signal
    };
    var textEncoder = new TextEncoder();
    var lastSentIndex = 0;
    var readable = new ReadableStream({
        start (controller) {
            return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                var id = flatten.call(encoder, input);
                if (Array.isArray(id)) {
                    throw new Error("This should never happen");
                }
                if (id < 0) {
                    controller.enqueue(textEncoder.encode("".concat(id, "\n")));
                } else {
                    controller.enqueue(textEncoder.encode("[".concat(encoder.stringified.join(","), "]\n")));
                    lastSentIndex = encoder.stringified.length - 1;
                }
                var seenPromises = /* @__PURE__ */ new WeakSet();
                if (Object.keys(encoder.deferred).length) {
                    var raceDone;
                    var racePromise = new Promise((resolve, reject)=>{
                        raceDone = resolve;
                        if (signal) {
                            var rejectPromise = ()=>reject(signal.reason || new Error("Signal was aborted."));
                            if (signal.aborted) {
                                rejectPromise();
                            } else {
                                signal.addEventListener("abort", (event)=>{
                                    rejectPromise();
                                });
                            }
                        }
                    });
                    while(Object.keys(encoder.deferred).length > 0){
                        var _loop = function(deferredId, deferred) {
                            if (seenPromises.has(deferred)) return "continue";
                            seenPromises.add(// biome-ignore lint/suspicious/noAssignInExpressions: <explanation>
                            encoder.deferred[Number(deferredId)] = Promise.race([
                                racePromise,
                                deferred
                            ]).then((resolved)=>{
                                var id2 = flatten.call(encoder, resolved);
                                if (Array.isArray(id2)) {
                                    controller.enqueue(textEncoder.encode("".concat(TYPE_PROMISE).concat(deferredId, ':[["').concat(TYPE_PREVIOUS_RESOLVED, '",').concat(id2[0], "]]\n")));
                                    encoder.index++;
                                    lastSentIndex++;
                                } else if (id2 < 0) {
                                    controller.enqueue(textEncoder.encode("".concat(TYPE_PROMISE).concat(deferredId, ":").concat(id2, "\n")));
                                } else {
                                    var values = encoder.stringified.slice(lastSentIndex + 1).join(",");
                                    controller.enqueue(textEncoder.encode("".concat(TYPE_PROMISE).concat(deferredId, ":[").concat(values, "]\n")));
                                    lastSentIndex = encoder.stringified.length - 1;
                                }
                            }, (reason)=>{
                                if (!reason || typeof reason !== "object" || !(reason instanceof Error)) {
                                    reason = new Error("An unknown error occurred");
                                }
                                var id2 = flatten.call(encoder, reason);
                                if (Array.isArray(id2)) {
                                    controller.enqueue(textEncoder.encode("".concat(TYPE_ERROR).concat(deferredId, ':[["').concat(TYPE_PREVIOUS_RESOLVED, '",').concat(id2[0], "]]\n")));
                                    encoder.index++;
                                    lastSentIndex++;
                                } else if (id2 < 0) {
                                    controller.enqueue(textEncoder.encode("".concat(TYPE_ERROR).concat(deferredId, ":").concat(id2, "\n")));
                                } else {
                                    var values = encoder.stringified.slice(lastSentIndex + 1).join(",");
                                    controller.enqueue(textEncoder.encode("".concat(TYPE_ERROR).concat(deferredId, ":[").concat(values, "]\n")));
                                    lastSentIndex = encoder.stringified.length - 1;
                                }
                            }).finally(()=>{
                                delete encoder.deferred[Number(deferredId)];
                            }));
                        };
                        for (var [deferredId, deferred] of Object.entries(encoder.deferred))_loop(deferredId, deferred);
                        yield Promise.race(Object.values(encoder.deferred));
                    }
                    raceDone();
                }
                yield Promise.all(Object.values(encoder.deferred));
                controller.close();
            })();
        }
    });
    return readable;
}
// lib/dom/ssr/data.ts
function createRequestInit(request) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var init = {
            signal: request.signal
        };
        if (request.method !== "GET") {
            init.method = request.method;
            var contentType = request.headers.get("Content-Type");
            if (contentType && /\bapplication\/json\b/.test(contentType)) {
                init.headers = {
                    "Content-Type": contentType
                };
                init.body = JSON.stringify((yield request.json()));
            } else if (contentType && /\btext\/plain\b/.test(contentType)) {
                init.headers = {
                    "Content-Type": contentType
                };
                init.body = yield request.text();
            } else if (contentType && /\bapplication\/x-www-form-urlencoded\b/.test(contentType)) {
                init.body = new URLSearchParams((yield request.text()));
            } else {
                init.body = yield request.formData();
            }
        }
        return init;
    })();
}
// lib/dom/ssr/markup.ts
var ESCAPE_LOOKUP = {
    "&": "\\u0026",
    ">": "\\u003e",
    "<": "\\u003c",
    "\u2028": "\\u2028",
    "\u2029": "\\u2029"
};
var ESCAPE_REGEX = /[&><\u2028\u2029]/g;
function escapeHtml(html) {
    return html.replace(ESCAPE_REGEX, (match)=>ESCAPE_LOOKUP[match]);
}
// lib/dom/ssr/invariant.ts
function invariant2(value, message) {
    if (value === false || value === null || typeof value === "undefined") {
        throw new Error(message);
    }
}
// lib/dom/ssr/single-fetch.tsx
var SingleFetchRedirectSymbol = Symbol("SingleFetchRedirect");
var SingleFetchNoResultError = class SingleFetchNoResultError extends Error {
};
var SINGLE_FETCH_REDIRECT_STATUS = 202;
var NO_BODY_STATUS_CODES = /* @__PURE__ */ new Set([
    100,
    101,
    204,
    205
]);
function StreamTransfer(param) {
    var { context, identifier, reader, textDecoder, nonce } = param;
    if (!context.renderMeta || !context.renderMeta.didRenderScripts) {
        return null;
    }
    if (!context.renderMeta.streamCache) {
        context.renderMeta.streamCache = {};
    }
    var { streamCache } = context.renderMeta;
    var promise = streamCache[identifier];
    if (!promise) {
        promise = streamCache[identifier] = reader.read().then((result)=>{
            streamCache[identifier].result = {
                done: result.done,
                value: textDecoder.decode(result.value, {
                    stream: true
                })
            };
        }).catch((e)=>{
            streamCache[identifier].error = e;
        });
    }
    if (promise.error) {
        throw promise.error;
    }
    if (promise.result === void 0) {
        throw promise;
    }
    var { done, value } = promise.result;
    var scriptTag = value ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", {
        nonce,
        dangerouslySetInnerHTML: {
            __html: "window.__reactRouterContext.streamController.enqueue(".concat(escapeHtml(JSON.stringify(value)), ");")
        }
    }) : null;
    if (done) {
        return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, scriptTag, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", {
            nonce,
            dangerouslySetInnerHTML: {
                __html: "window.__reactRouterContext.streamController.close();"
            }
        }));
    } else {
        return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, scriptTag, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Suspense, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(StreamTransfer, {
            context,
            identifier: identifier + 1,
            reader,
            textDecoder,
            nonce
        })));
    }
}
_c18 = StreamTransfer;
function getTurboStreamSingleFetchDataStrategy(getRouter, manifest, routeModules, ssr, basename, trailingSlashAware) {
    var dataStrategy = getSingleFetchDataStrategyImpl(getRouter, (match)=>{
        var manifestRoute = manifest.routes[match.route.id];
        invariant2(manifestRoute, "Route not found in manifest");
        var routeModule = routeModules[match.route.id];
        return {
            hasLoader: manifestRoute.hasLoader,
            hasClientLoader: manifestRoute.hasClientLoader,
            hasShouldRevalidate: Boolean(routeModule === null || routeModule === void 0 ? void 0 : routeModule.shouldRevalidate)
        };
    }, fetchAndDecodeViaTurboStream, ssr, basename, trailingSlashAware);
    return (args)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            return args.runClientMiddleware(dataStrategy);
        })();
}
function getSingleFetchDataStrategyImpl(getRouter, getRouteInfo, fetchAndDecode, ssr, basename, trailingSlashAware) {
    var shouldAllowOptOut = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : ()=>true;
    return (args)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var { request, matches, fetcherKey } = args;
            var router = getRouter();
            if (request.method !== "GET") {
                return singleFetchActionStrategy(args, fetchAndDecode, basename, trailingSlashAware);
            }
            var foundRevalidatingServerLoader = matches.some((m)=>{
                var { hasLoader, hasClientLoader } = getRouteInfo(m);
                return m.shouldCallHandler() && hasLoader && !hasClientLoader;
            });
            if (!ssr && !foundRevalidatingServerLoader) {
                return nonSsrStrategy(args, getRouteInfo, fetchAndDecode, basename, trailingSlashAware);
            }
            if (fetcherKey) {
                return singleFetchLoaderFetcherStrategy(args, fetchAndDecode, basename, trailingSlashAware);
            }
            return singleFetchLoaderNavigationStrategy(args, router, getRouteInfo, fetchAndDecode, ssr, basename, trailingSlashAware, shouldAllowOptOut);
        })();
}
function singleFetchActionStrategy(args, fetchAndDecode, basename, trailingSlashAware) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var actionMatch = args.matches.find((m)=>m.shouldCallHandler());
        invariant2(actionMatch, "No action match found");
        var actionStatus = void 0;
        var result = yield actionMatch.resolve((handler)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                var result2 = yield handler(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        var { data: data2, status } = yield fetchAndDecode(args, basename, trailingSlashAware, [
                            actionMatch.route.id
                        ]);
                        actionStatus = status;
                        return unwrapSingleFetchResult(data2, actionMatch.route.id);
                    })());
                return result2;
            })());
        if (isResponse(result.result) || isRouteErrorResponse(result.result) || isDataWithResponseInit(result.result)) {
            return {
                [actionMatch.route.id]: result
            };
        }
        return {
            [actionMatch.route.id]: {
                type: result.type,
                result: data(result.result, actionStatus)
            }
        };
    })();
}
function nonSsrStrategy(args, getRouteInfo, fetchAndDecode, basename, trailingSlashAware) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var matchesToLoad = args.matches.filter((m)=>m.shouldCallHandler());
        var results = {};
        yield Promise.all(matchesToLoad.map((m)=>m.resolve((handler)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                    try {
                        var { hasClientLoader } = getRouteInfo(m);
                        var routeId = m.route.id;
                        var result = hasClientLoader ? yield handler(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                var { data: data2 } = yield fetchAndDecode(args, basename, trailingSlashAware, [
                                    routeId
                                ]);
                                return unwrapSingleFetchResult(data2, routeId);
                            })()) : yield handler();
                        results[m.route.id] = {
                            type: "data",
                            result
                        };
                    } catch (e) {
                        results[m.route.id] = {
                            type: "error",
                            result: e
                        };
                    }
                })())));
        return results;
    })();
}
function singleFetchLoaderNavigationStrategy(args, router, getRouteInfo, fetchAndDecode, ssr, basename, trailingSlashAware) {
    var shouldAllowOptOut = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : ()=>true;
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var routesParams = /* @__PURE__ */ new Set();
        var foundOptOutRoute = false;
        var routeDfds = args.matches.map(()=>createDeferred2());
        var singleFetchDfd = createDeferred2();
        var results = {};
        var resolvePromise = Promise.all(args.matches.map((m, i)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                return m.resolve((handler)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        routeDfds[i].resolve();
                        var routeId = m.route.id;
                        var { hasLoader, hasClientLoader, hasShouldRevalidate } = getRouteInfo(m);
                        var defaultShouldRevalidate = !m.shouldRevalidateArgs || m.shouldRevalidateArgs.actionStatus == null || m.shouldRevalidateArgs.actionStatus < 400;
                        var shouldCall = m.shouldCallHandler(defaultShouldRevalidate);
                        if (!shouldCall) {
                            foundOptOutRoute || (foundOptOutRoute = m.shouldRevalidateArgs != null && // This is a revalidation,
                            hasLoader && // for a route with a server loader,
                            hasShouldRevalidate === true);
                            return;
                        }
                        if (shouldAllowOptOut(m) && hasClientLoader) {
                            if (hasLoader) {
                                foundOptOutRoute = true;
                            }
                            try {
                                var result = yield handler(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                        var { data: data2 } = yield fetchAndDecode(args, basename, trailingSlashAware, [
                                            routeId
                                        ]);
                                        return unwrapSingleFetchResult(data2, routeId);
                                    })());
                                results[routeId] = {
                                    type: "data",
                                    result
                                };
                            } catch (e) {
                                results[routeId] = {
                                    type: "error",
                                    result: e
                                };
                            }
                            return;
                        }
                        if (hasLoader) {
                            routesParams.add(routeId);
                        }
                        try {
                            var result1 = yield handler(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                    var data2 = yield singleFetchDfd.promise;
                                    return unwrapSingleFetchResult(data2, routeId);
                                })());
                            results[routeId] = {
                                type: "data",
                                result: result1
                            };
                        } catch (e) {
                            results[routeId] = {
                                type: "error",
                                result: e
                            };
                        }
                    })());
            })()));
        yield Promise.all(routeDfds.map((d)=>d.promise));
        var isInitialLoad = !router.state.initialized && router.state.navigation.state === "idle";
        if ((isInitialLoad || routesParams.size === 0) && !window.__reactRouterHdrActive) {
            singleFetchDfd.resolve({
                routes: {}
            });
        } else {
            var targetRoutes = ssr && foundOptOutRoute && routesParams.size > 0 ? [
                ...routesParams.keys()
            ] : void 0;
            try {
                var data2 = yield fetchAndDecode(args, basename, trailingSlashAware, targetRoutes);
                singleFetchDfd.resolve(data2.data);
            } catch (e) {
                singleFetchDfd.reject(e);
            }
        }
        yield resolvePromise;
        yield bubbleMiddlewareErrors(singleFetchDfd.promise, args.matches, routesParams, results);
        return results;
    })();
}
function bubbleMiddlewareErrors(singleFetchPromise, matches, routesParams, results) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        try {
            var middlewareError;
            var fetchedData = yield singleFetchPromise;
            if ("routes" in fetchedData) {
                for (var match of matches){
                    if (match.route.id in fetchedData.routes) {
                        var routeResult = fetchedData.routes[match.route.id];
                        if ("error" in routeResult) {
                            var _results_match_route_id;
                            middlewareError = routeResult.error;
                            if (((_results_match_route_id = results[match.route.id]) === null || _results_match_route_id === void 0 ? void 0 : _results_match_route_id.result) == null) {
                                results[match.route.id] = {
                                    type: "error",
                                    result: middlewareError
                                };
                            }
                            break;
                        }
                    }
                }
            }
            if (middlewareError !== void 0) {
                Array.from(routesParams.values()).forEach((routeId)=>{
                    if (results[routeId].result instanceof SingleFetchNoResultError) {
                        results[routeId].result = middlewareError;
                    }
                });
            }
        } catch (e) {}
    })();
}
function singleFetchLoaderFetcherStrategy(args, fetchAndDecode, basename, trailingSlashAware) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var fetcherMatch = args.matches.find((m)=>m.shouldCallHandler());
        invariant2(fetcherMatch, "No fetcher match found");
        var routeId = fetcherMatch.route.id;
        var result = yield fetcherMatch.resolve((handler)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                return handler(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        var { data: data2 } = yield fetchAndDecode(args, basename, trailingSlashAware, [
                            routeId
                        ]);
                        return unwrapSingleFetchResult(data2, routeId);
                    })());
            })());
        return {
            [fetcherMatch.route.id]: result
        };
    })();
}
function stripIndexParam(url) {
    var indexValues = url.searchParams.getAll("index");
    url.searchParams.delete("index");
    var indexValuesToKeep = [];
    for (var indexValue of indexValues){
        if (indexValue) {
            indexValuesToKeep.push(indexValue);
        }
    }
    for (var toKeep of indexValuesToKeep){
        url.searchParams.append("index", toKeep);
    }
    return url;
}
function singleFetchUrl(reqUrl, basename, trailingSlashAware, extension) {
    var url = typeof reqUrl === "string" ? new URL(reqUrl, // This can be called during the SSR flow via PrefetchPageLinksImpl so
    // don't assume window is available
    typeof window === "undefined" ? "server://singlefetch/" : window.location.origin) : reqUrl;
    if (trailingSlashAware) {
        if (url.pathname.endsWith("/")) {
            url.pathname = "".concat(url.pathname, "_.").concat(extension);
        } else {
            url.pathname = "".concat(url.pathname, ".").concat(extension);
        }
    } else {
        if (url.pathname === "/") {
            url.pathname = "_root.".concat(extension);
        } else if (basename && stripBasename(url.pathname, basename) === "/") {
            url.pathname = "".concat(basename.replace(/\/$/, ""), "/_root.").concat(extension);
        } else {
            url.pathname = "".concat(url.pathname.replace(/\/$/, ""), ".").concat(extension);
        }
    }
    return url;
}
function fetchAndDecodeViaTurboStream(args, basename, trailingSlashAware, targetRoutes) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var { request } = args;
        var url = singleFetchUrl(request.url, basename, trailingSlashAware, "data");
        if (request.method === "GET") {
            url = stripIndexParam(url);
            if (targetRoutes) {
                url.searchParams.set("_routes", targetRoutes.join(","));
            }
        }
        var res = yield fetch(url, (yield createRequestInit(request)));
        if (res.status >= 400 && !res.headers.has("X-Remix-Response")) {
            throw new ErrorResponseImpl(res.status, res.statusText, (yield res.text()));
        }
        if (res.status === 204 && res.headers.has("X-Remix-Redirect")) {
            return {
                status: SINGLE_FETCH_REDIRECT_STATUS,
                data: {
                    redirect: {
                        redirect: res.headers.get("X-Remix-Redirect"),
                        status: Number(res.headers.get("X-Remix-Status") || "302"),
                        revalidate: res.headers.get("X-Remix-Revalidate") === "true",
                        reload: res.headers.get("X-Remix-Reload-Document") === "true",
                        replace: res.headers.get("X-Remix-Replace") === "true"
                    }
                }
            };
        }
        if (NO_BODY_STATUS_CODES.has(res.status)) {
            var routes = {};
            if (targetRoutes && request.method !== "GET") {
                routes[targetRoutes[0]] = {
                    data: void 0
                };
            }
            return {
                status: res.status,
                data: {
                    routes
                }
            };
        }
        invariant2(res.body, "No response body to decode");
        try {
            var decoded = yield decodeViaTurboStream(res.body, window);
            var data2;
            if (request.method === "GET") {
                var typed = decoded.value;
                if (SingleFetchRedirectSymbol in typed) {
                    data2 = {
                        redirect: typed[SingleFetchRedirectSymbol]
                    };
                } else {
                    data2 = {
                        routes: typed
                    };
                }
            } else {
                var typed1 = decoded.value;
                var routeId = targetRoutes === null || targetRoutes === void 0 ? void 0 : targetRoutes[0];
                invariant2(routeId, "No routeId found for single fetch call decoding");
                if ("redirect" in typed1) {
                    data2 = {
                        redirect: typed1
                    };
                } else {
                    data2 = {
                        routes: {
                            [routeId]: typed1
                        }
                    };
                }
            }
            return {
                status: res.status,
                data: data2
            };
        } catch (e) {
            throw new Error("Unable to decode turbo-stream response");
        }
    })();
}
function decodeViaTurboStream(body, global) {
    return decode(body, {
        plugins: [
            function(type) {
                for(var _len = arguments.length, rest = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++){
                    rest[_key - 1] = arguments[_key];
                }
                if (type === "SanitizedError") {
                    var [name, message, stack] = rest;
                    var Constructor = Error;
                    if (name && name in global && typeof global[name] === "function") {
                        Constructor = global[name];
                    }
                    var error = new Constructor(message);
                    error.stack = stack;
                    return {
                        value: error
                    };
                }
                if (type === "ErrorResponse") {
                    var [data2, status, statusText] = rest;
                    return {
                        value: new ErrorResponseImpl(status, statusText, data2)
                    };
                }
                if (type === "SingleFetchRedirect") {
                    return {
                        value: {
                            [SingleFetchRedirectSymbol]: rest[0]
                        }
                    };
                }
                if (type === "SingleFetchClassInstance") {
                    return {
                        value: rest[0]
                    };
                }
                if (type === "SingleFetchFallback") {
                    return {
                        value: void 0
                    };
                }
            }
        ]
    });
}
function unwrapSingleFetchResult(result, routeId) {
    if ("redirect" in result) {
        var { redirect: location, revalidate, reload, replace: replace2, status } = result.redirect;
        throw redirect(location, {
            status,
            headers: (0,_swc_helpers_object_spread__rspack_import_2._)({}, revalidate ? {
                "X-Remix-Revalidate": "yes"
            } : null, reload ? {
                "X-Remix-Reload-Document": "yes"
            } : null, replace2 ? {
                "X-Remix-Replace": "yes"
            } : null)
        });
    }
    var routeResult = result.routes[routeId];
    if (routeResult == null) {
        throw new SingleFetchNoResultError('No result found for routeId "'.concat(routeId, '"'));
    } else if ("error" in routeResult) {
        throw routeResult.error;
    } else if ("data" in routeResult) {
        return routeResult.data;
    } else {
        throw new Error('Invalid response found for routeId "'.concat(routeId, '"'));
    }
}
function createDeferred2() {
    var resolve;
    var reject;
    var promise = new Promise((res, rej)=>{
        resolve = (val)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                res(val);
                try {
                    yield promise;
                } catch (e) {}
            })();
        reject = (error)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                rej(error);
                try {
                    yield promise;
                } catch (e) {}
            })();
    });
    return {
        promise,
        //@ts-ignore
        resolve,
        //@ts-ignore
        reject
    };
}
// lib/dom/ssr/errorBoundaries.tsx

// lib/dom/ssr/components.tsx

// lib/dom/ssr/routeModules.ts
function loadRouteModule(route, routeModulesCache) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        if (route.id in routeModulesCache) {
            return routeModulesCache[route.id];
        }
        try {
            var routeModule = yield import(/* @vite-ignore */ /* webpackIgnore: true */ route.module);
            routeModulesCache[route.id] = routeModule;
            return routeModule;
        } catch (error) {
            console.error("Error loading route module `".concat(route.module, "`, reloading page..."));
            console.error(error);
            if (window.__reactRouterContext && window.__reactRouterContext.isSpaMode && // @ts-expect-error
            /* unsupported import.meta.hot */ undefined) {}
            window.location.reload();
            return new Promise(()=>{});
        }
    })();
}
// lib/dom/ssr/links.ts
function getKeyedLinksForMatches(matches, routeModules, manifest) {
    var descriptors = matches.map((match)=>{
        var _module_links;
        var module = routeModules[match.route.id];
        var route = manifest.routes[match.route.id];
        return [
            route && route.css ? route.css.map((href)=>({
                    rel: "stylesheet",
                    href
                })) : [],
            (module === null || module === void 0 ? void 0 : (_module_links = module.links) === null || _module_links === void 0 ? void 0 : _module_links.call(module)) || []
        ];
    }).flat(2);
    var preloads = getModuleLinkHrefs(matches, manifest);
    return dedupeLinkDescriptors(descriptors, preloads);
}
function getRouteCssDescriptors(route) {
    if (!route.css) return [];
    return route.css.map((href)=>({
            rel: "stylesheet",
            href
        }));
}
function prefetchRouteCss(route) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        if (!route.css) return;
        var descriptors = getRouteCssDescriptors(route);
        yield Promise.all(descriptors.map(prefetchStyleLink));
    })();
}
function prefetchStyleLinks(route, routeModule) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        if (!route.css && !routeModule.links || !isPreloadSupported()) return;
        var descriptors = [];
        if (route.css) {
            descriptors.push(...getRouteCssDescriptors(route));
        }
        if (routeModule.links) {
            descriptors.push(...routeModule.links());
        }
        if (descriptors.length === 0) return;
        var styleLinks = [];
        for (var descriptor of descriptors){
            if (!isPageLinkDescriptor(descriptor) && descriptor.rel === "stylesheet") {
                styleLinks.push((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, descriptor), {
                    rel: "preload",
                    as: "style"
                }));
            }
        }
        yield Promise.all(styleLinks.map(prefetchStyleLink));
    })();
}
function prefetchStyleLink(descriptor) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        return new Promise((resolve)=>{
            if (descriptor.media && !window.matchMedia(descriptor.media).matches || document.querySelector('link[rel="stylesheet"][href="'.concat(descriptor.href, '"]'))) {
                return resolve();
            }
            var link = document.createElement("link");
            Object.assign(link, descriptor);
            function removeLink() {
                if (document.head.contains(link)) {
                    document.head.removeChild(link);
                }
            }
            link.onload = ()=>{
                removeLink();
                resolve();
            };
            link.onerror = ()=>{
                removeLink();
                resolve();
            };
            document.head.appendChild(link);
        });
    })();
}
function isPageLinkDescriptor(object) {
    return object != null && typeof object.page === "string";
}
function isHtmlLinkDescriptor(object) {
    if (object == null) {
        return false;
    }
    if (object.href == null) {
        return object.rel === "preload" && typeof object.imageSrcSet === "string" && typeof object.imageSizes === "string";
    }
    return typeof object.rel === "string" && typeof object.href === "string";
}
function getKeyedPrefetchLinks(matches, manifest, routeModules) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var links = yield Promise.all(matches.map((match)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                var route = manifest.routes[match.route.id];
                if (route) {
                    var mod = yield loadRouteModule(route, routeModules);
                    return mod.links ? mod.links() : [];
                }
                return [];
            })()));
        return dedupeLinkDescriptors(links.flat(1).filter(isHtmlLinkDescriptor).filter((link)=>link.rel === "stylesheet" || link.rel === "preload").map((link)=>link.rel === "stylesheet" ? (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, link), {
                rel: "prefetch",
                as: "style"
            }) : (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, link), {
                rel: "prefetch"
            })));
    })();
}
function getNewMatchesForLinks(page, nextMatches, currentMatches, manifest, location, mode) {
    var isNew = (match, index)=>{
        if (!currentMatches[index]) return true;
        return match.route.id !== currentMatches[index].route.id;
    };
    var matchPathChanged = (match, index)=>{
        var // splat param changed, which is not present in match.path
        // e.g. /files/images/avatar.jpg -> files/finances.xls
        _currentMatches_index_route_path;
        return(// param change, /users/123 -> /users/456
        currentMatches[index].pathname !== match.pathname || ((_currentMatches_index_route_path = currentMatches[index].route.path) === null || _currentMatches_index_route_path === void 0 ? void 0 : _currentMatches_index_route_path.endsWith("*")) && currentMatches[index].params["*"] !== match.params["*"]);
    };
    if (mode === "assets") {
        return nextMatches.filter((match, index)=>isNew(match, index) || matchPathChanged(match, index));
    }
    if (mode === "data") {
        return nextMatches.filter((match, index)=>{
            var manifestRoute = manifest.routes[match.route.id];
            if (!manifestRoute || !manifestRoute.hasLoader) {
                return false;
            }
            if (isNew(match, index) || matchPathChanged(match, index)) {
                return true;
            }
            if (match.route.shouldRevalidate) {
                var _currentMatches_;
                var routeChoice = match.route.shouldRevalidate({
                    currentUrl: new URL(location.pathname + location.search + location.hash, window.origin),
                    currentParams: ((_currentMatches_ = currentMatches[0]) === null || _currentMatches_ === void 0 ? void 0 : _currentMatches_.params) || {},
                    nextUrl: new URL(page, window.origin),
                    nextParams: match.params,
                    defaultShouldRevalidate: true
                });
                if (typeof routeChoice === "boolean") {
                    return routeChoice;
                }
            }
            return true;
        });
    }
    return [];
}
function getModuleLinkHrefs(matches, manifest) {
    var { includeHydrateFallback } = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    return dedupeHrefs(matches.map((match)=>{
        var route = manifest.routes[match.route.id];
        if (!route) return [];
        var hrefs = [
            route.module
        ];
        if (route.clientActionModule) {
            hrefs = hrefs.concat(route.clientActionModule);
        }
        if (route.clientLoaderModule) {
            hrefs = hrefs.concat(route.clientLoaderModule);
        }
        if (includeHydrateFallback && route.hydrateFallbackModule) {
            hrefs = hrefs.concat(route.hydrateFallbackModule);
        }
        if (route.imports) {
            hrefs = hrefs.concat(route.imports);
        }
        return hrefs;
    }).flat(1));
}
function dedupeHrefs(hrefs) {
    return [
        ...new Set(hrefs)
    ];
}
function sortKeys(obj) {
    var sorted = {};
    var keys = Object.keys(obj).sort();
    for (var key of keys){
        sorted[key] = obj[key];
    }
    return sorted;
}
function dedupeLinkDescriptors(descriptors, preloads) {
    var set = /* @__PURE__ */ new Set();
    var preloadsSet = new Set(preloads);
    return descriptors.reduce((deduped, descriptor)=>{
        var alreadyModulePreload = preloads && !isPageLinkDescriptor(descriptor) && descriptor.as === "script" && descriptor.href && preloadsSet.has(descriptor.href);
        if (alreadyModulePreload) {
            return deduped;
        }
        var key = JSON.stringify(sortKeys(descriptor));
        if (!set.has(key)) {
            set.add(key);
            deduped.push({
                key,
                link: descriptor
            });
        }
        return deduped;
    }, []);
}
var _isPreloadSupported;
function isPreloadSupported() {
    if (_isPreloadSupported !== void 0) {
        return _isPreloadSupported;
    }
    var el = document.createElement("link");
    _isPreloadSupported = el.relList.supports("preload");
    el = null;
    return _isPreloadSupported;
}
// lib/dom/ssr/fog-of-war.ts

// lib/dom/ssr/routes.tsx

// lib/dom/ssr/fallback.tsx

function RemixRootDefaultHydrateFallback() {
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(BoundaryShell, {
        title: "Loading...",
        renderScripts: true
    }, ENABLE_DEV_WARNINGS ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", {
        dangerouslySetInnerHTML: {
            __html: '\n              console.log(\n                "\uD83D\uDCBF Hey developer \uD83D\uDC4B. You can provide a way better UX than this " +\n                "when your app is loading JS modules and/or running `clientLoader` " +\n                "functions. Check out https://reactrouter.com/start/framework/route-module#hydratefallback " +\n                "for more information."\n              );\n            '
        }
    }) : null);
}
_c19 = RemixRootDefaultHydrateFallback;
// lib/dom/ssr/routes.tsx
function groupRoutesByParentId(manifest) {
    var routes = {};
    Object.values(manifest).forEach((route)=>{
        if (route) {
            var parentId = route.parentId || "";
            if (!routes[parentId]) {
                routes[parentId] = [];
            }
            routes[parentId].push(route);
        }
    });
    return routes;
}
function getRouteComponents(route, routeModule, isSpaMode) {
    var _s = $RefreshSig$();
    var Component4 = getRouteModuleComponent(routeModule);
    var HydrateFallback = routeModule.HydrateFallback && (!isSpaMode || route.id === "root") ? routeModule.HydrateFallback : route.id === "root" ? RemixRootDefaultHydrateFallback : void 0;
    var ErrorBoundary = routeModule.ErrorBoundary ? routeModule.ErrorBoundary : route.id === "root" ? _s(()=>/* @__PURE__ */ /*#__PURE__*/ {
        _s();
        return react__rspack_import_0.createElement(RemixRootDefaultErrorBoundary, {
            error: useRouteError()
        });
    }, "YDkf/bojC730qvJxOiv5VT1rhKY=", false, function() {
        return [
            useRouteError
        ];
    }) : void 0;
    if (route.id === "root" && routeModule.Layout) {
        return (0,_swc_helpers_object_spread__rspack_import_2._)({}, Component4 ? {
            element: /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(routeModule.Layout, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Component4, null))
        } : {
            Component: Component4
        }, ErrorBoundary ? {
            errorElement: /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(routeModule.Layout, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(ErrorBoundary, null))
        } : {
            ErrorBoundary
        }, HydrateFallback ? {
            hydrateFallbackElement: /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(routeModule.Layout, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(HydrateFallback, null))
        } : {
            HydrateFallback
        });
    }
    return {
        Component: Component4,
        ErrorBoundary,
        HydrateFallback
    };
}
function createServerRoutes(manifest, routeModules, future, isSpaMode) {
    var parentId = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : "", routesByParentId = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : groupRoutesByParentId(manifest), spaModeLazyPromise = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : Promise.resolve({
        Component: ()=>null
    });
    return (routesByParentId[parentId] || []).map((route)=>{
        var routeModule = routeModules[route.id];
        invariant2(routeModule, "No `routeModule` available to create server routes");
        var dataRoute = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, getRouteComponents(route, routeModule, isSpaMode)), {
            caseSensitive: route.caseSensitive,
            id: route.id,
            index: route.index,
            path: route.path,
            handle: routeModule.handle,
            // For SPA Mode, all routes are lazy except root.  However we tell the
            // router root is also lazy here too since we don't need a full
            // implementation - we just need a `lazy` prop to tell the RR rendering
            // where to stop which is always at the root route in SPA mode
            lazy: isSpaMode ? ()=>spaModeLazyPromise : void 0,
            // For partial hydration rendering, we need to indicate when the route
            // has a loader/clientLoader, but it won't ever be called during the static
            // render, so just give it a no-op function so we can render down to the
            // proper fallback
            loader: route.hasLoader || route.hasClientLoader ? ()=>null : void 0
        });
        var children = createServerRoutes(manifest, routeModules, future, isSpaMode, route.id, routesByParentId, spaModeLazyPromise);
        if (children.length > 0) dataRoute.children = children;
        return dataRoute;
    });
}
function createClientRoutesWithHMRRevalidationOptOut(needsRevalidation, manifest, routeModulesCache, initialState, ssr, isSpaMode) {
    return createClientRoutes(manifest, routeModulesCache, initialState, ssr, isSpaMode, "", groupRoutesByParentId(manifest), needsRevalidation);
}
function preventInvalidServerHandlerCall(type, route) {
    if (type === "loader" && !route.hasLoader || type === "action" && !route.hasAction) {
        var fn = type === "action" ? "serverAction()" : "serverLoader()";
        var msg = "You are trying to call ".concat(fn, " on a route that does not have a server ").concat(type, ' (routeId: "').concat(route.id, '")');
        console.error(msg);
        throw new ErrorResponseImpl(400, "Bad Request", new Error(msg), true);
    }
}
function noActionDefinedError(type, routeId) {
    var article = type === "clientAction" ? "a" : "an";
    var msg = 'Route "'.concat(routeId, '" does not have ').concat(article, " ").concat(type, ", but you are trying to submit to it. To fix this, please add ").concat(article, " `").concat(type, "` function to the route");
    console.error(msg);
    throw new ErrorResponseImpl(405, "Method Not Allowed", new Error(msg), true);
}
function createClientRoutes(manifest, routeModulesCache, initialState, ssr, isSpaMode) {
    var parentId = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : "", routesByParentId = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : groupRoutesByParentId(manifest), needsRevalidation = arguments.length > 7 ? arguments[7] : void 0;
    return (routesByParentId[parentId] || []).map((route)=>{
        var routeModule = routeModulesCache[route.id];
        function fetchServerHandler(singleFetch) {
            invariant2(typeof singleFetch === "function", "No single fetch function available for route handler");
            return singleFetch();
        }
        function fetchServerLoader(singleFetch) {
            if (!route.hasLoader) return Promise.resolve(null);
            return fetchServerHandler(singleFetch);
        }
        function fetchServerAction(singleFetch) {
            if (!route.hasAction) {
                throw noActionDefinedError("action", route.id);
            }
            return fetchServerHandler(singleFetch);
        }
        function prefetchModule(modulePath) {
            import(/* @vite-ignore */ /* webpackIgnore: true */ modulePath);
        }
        function prefetchRouteModuleChunks(route2) {
            if (route2.clientActionModule) {
                prefetchModule(route2.clientActionModule);
            }
            if (route2.clientLoaderModule) {
                prefetchModule(route2.clientLoaderModule);
            }
        }
        function prefetchStylesAndCallHandler(handler) {
            return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                var cachedModule = routeModulesCache[route.id];
                var linkPrefetchPromise = cachedModule ? prefetchStyleLinks(route, cachedModule) : Promise.resolve();
                try {
                    return handler();
                } finally{
                    yield linkPrefetchPromise;
                }
            })();
        }
        var dataRoute = {
            id: route.id,
            index: route.index,
            path: route.path
        };
        if (routeModule) {
            var _initialState_loaderData, _initialState_errors, _routeModule_clientLoader;
            Object.assign(dataRoute, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, dataRoute, getRouteComponents(route, routeModule, isSpaMode)), {
                middleware: routeModule.clientMiddleware,
                handle: routeModule.handle,
                shouldRevalidate: getShouldRevalidateFunction(dataRoute.path, routeModule, route, ssr, needsRevalidation)
            }));
            var hasInitialData = initialState && initialState.loaderData && route.id in initialState.loaderData;
            var initialData = hasInitialData ? initialState === null || initialState === void 0 ? void 0 : (_initialState_loaderData = initialState.loaderData) === null || _initialState_loaderData === void 0 ? void 0 : _initialState_loaderData[route.id] : void 0;
            var hasInitialError = initialState && initialState.errors && route.id in initialState.errors;
            var initialError = hasInitialError ? initialState === null || initialState === void 0 ? void 0 : (_initialState_errors = initialState.errors) === null || _initialState_errors === void 0 ? void 0 : _initialState_errors[route.id] : void 0;
            var isHydrationRequest = needsRevalidation == null && (((_routeModule_clientLoader = routeModule.clientLoader) === null || _routeModule_clientLoader === void 0 ? void 0 : _routeModule_clientLoader.hydrate) === true || !route.hasLoader);
            dataRoute.loader = (param, singleFetch)=>{
                var { request, params, context, unstable_pattern } = param;
                return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                    try {
                        var result = yield prefetchStylesAndCallHandler(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                invariant2(routeModule, "No `routeModule` available for critical-route loader");
                                if (!routeModule.clientLoader) {
                                    return fetchServerLoader(singleFetch);
                                }
                                return routeModule.clientLoader({
                                    request,
                                    params,
                                    context,
                                    unstable_pattern,
                                    serverLoader () {
                                        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                            preventInvalidServerHandlerCall("loader", route);
                                            if (isHydrationRequest) {
                                                if (hasInitialData) {
                                                    return initialData;
                                                }
                                                if (hasInitialError) {
                                                    throw initialError;
                                                }
                                            }
                                            return fetchServerLoader(singleFetch);
                                        })();
                                    }
                                });
                            })());
                        return result;
                    } finally{
                        isHydrationRequest = false;
                    }
                })();
            };
            dataRoute.loader.hydrate = shouldHydrateRouteLoader(route.id, routeModule.clientLoader, route.hasLoader, isSpaMode);
            dataRoute.action = (param, singleFetch)=>{
                var { request, params, context, unstable_pattern } = param;
                return prefetchStylesAndCallHandler(()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        invariant2(routeModule, "No `routeModule` available for critical-route action");
                        if (!routeModule.clientAction) {
                            if (isSpaMode) {
                                throw noActionDefinedError("clientAction", route.id);
                            }
                            return fetchServerAction(singleFetch);
                        }
                        return routeModule.clientAction({
                            request,
                            params,
                            context,
                            unstable_pattern,
                            serverAction () {
                                return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                    preventInvalidServerHandlerCall("action", route);
                                    return fetchServerAction(singleFetch);
                                })();
                            }
                        });
                    })());
            };
        } else {
            if (!route.hasClientLoader) {
                dataRoute.loader = (_, singleFetch)=>prefetchStylesAndCallHandler(()=>{
                        return fetchServerLoader(singleFetch);
                    });
            }
            if (!route.hasClientAction) {
                dataRoute.action = (_, singleFetch)=>prefetchStylesAndCallHandler(()=>{
                        if (isSpaMode) {
                            throw noActionDefinedError("clientAction", route.id);
                        }
                        return fetchServerAction(singleFetch);
                    });
            }
            var lazyRoutePromise;
            function getLazyRoute() {
                return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                    if (lazyRoutePromise) {
                        return yield lazyRoutePromise;
                    }
                    lazyRoutePromise = (()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                            if (route.clientLoaderModule || route.clientActionModule) {
                                yield new Promise((resolve)=>setTimeout(resolve, 0));
                            }
                            var routeModulePromise = loadRouteModuleWithBlockingLinks(route, routeModulesCache);
                            prefetchRouteModuleChunks(route);
                            return yield routeModulePromise;
                        })())();
                    return yield lazyRoutePromise;
                })();
            }
            dataRoute.lazy = {
                loader: route.hasClientLoader ? ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        var { clientLoader } = route.clientLoaderModule ? yield import(/* @vite-ignore */ /* webpackIgnore: true */ route.clientLoaderModule) : yield getLazyRoute();
                        invariant2(clientLoader, "No `clientLoader` export found");
                        return (args, singleFetch)=>clientLoader((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, args), {
                                serverLoader () {
                                    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                        preventInvalidServerHandlerCall("loader", route);
                                        return fetchServerLoader(singleFetch);
                                    })();
                                }
                            }));
                    })() : void 0,
                action: route.hasClientAction ? ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        var clientActionPromise = route.clientActionModule ? import(/* @vite-ignore */ /* webpackIgnore: true */ route.clientActionModule) : getLazyRoute();
                        prefetchRouteModuleChunks(route);
                        var { clientAction } = yield clientActionPromise;
                        invariant2(clientAction, "No `clientAction` export found");
                        return (args, singleFetch)=>clientAction((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, args), {
                                serverAction () {
                                    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                                        preventInvalidServerHandlerCall("action", route);
                                        return fetchServerAction(singleFetch);
                                    })();
                                }
                            }));
                    })() : void 0,
                middleware: route.hasClientMiddleware ? ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        var { clientMiddleware } = route.clientMiddlewareModule ? yield import(/* @vite-ignore */ /* webpackIgnore: true */ route.clientMiddlewareModule) : yield getLazyRoute();
                        invariant2(clientMiddleware, "No `clientMiddleware` export found");
                        return clientMiddleware;
                    })() : void 0,
                shouldRevalidate: ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        var lazyRoute = yield getLazyRoute();
                        return getShouldRevalidateFunction(dataRoute.path, lazyRoute, route, ssr, needsRevalidation);
                    })(),
                handle: ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        return (yield getLazyRoute()).handle;
                    })(),
                // No need to wrap these in layout since the root route is never
                // loaded via route.lazy()
                Component: ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        return (yield getLazyRoute()).Component;
                    })(),
                ErrorBoundary: route.hasErrorBoundary ? ()=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                        return (yield getLazyRoute()).ErrorBoundary;
                    })() : void 0
            };
        }
        var children = createClientRoutes(manifest, routeModulesCache, initialState, ssr, isSpaMode, route.id, routesByParentId, needsRevalidation);
        if (children.length > 0) dataRoute.children = children;
        return dataRoute;
    });
}
function getShouldRevalidateFunction(path, route, manifestRoute, ssr, needsRevalidation) {
    if (needsRevalidation) {
        return wrapShouldRevalidateForHdr(manifestRoute.id, route.shouldRevalidate, needsRevalidation);
    }
    if (!ssr && manifestRoute.hasLoader && !manifestRoute.hasClientLoader) {
        var myParams = path ? compilePath(path)[1].map((p)=>p.paramName) : [];
        var didParamsChange = (opts)=>myParams.some((p)=>opts.currentParams[p] !== opts.nextParams[p]);
        if (route.shouldRevalidate) {
            var fn = route.shouldRevalidate;
            return (opts)=>fn((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, opts), {
                    defaultShouldRevalidate: didParamsChange(opts)
                }));
        } else {
            return (opts)=>didParamsChange(opts);
        }
    }
    return route.shouldRevalidate;
}
function wrapShouldRevalidateForHdr(routeId, routeShouldRevalidate, needsRevalidation) {
    var handledRevalidation = false;
    return (arg)=>{
        if (!handledRevalidation) {
            handledRevalidation = true;
            return needsRevalidation.has(routeId);
        }
        return routeShouldRevalidate ? routeShouldRevalidate(arg) : arg.defaultShouldRevalidate;
    };
}
function loadRouteModuleWithBlockingLinks(route, routeModules) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var routeModulePromise = loadRouteModule(route, routeModules);
        var prefetchRouteCssPromise = prefetchRouteCss(route);
        var routeModule = yield routeModulePromise;
        yield Promise.all([
            prefetchRouteCssPromise,
            prefetchStyleLinks(route, routeModule)
        ]);
        return {
            Component: getRouteModuleComponent(routeModule),
            ErrorBoundary: routeModule.ErrorBoundary,
            clientMiddleware: routeModule.clientMiddleware,
            clientAction: routeModule.clientAction,
            clientLoader: routeModule.clientLoader,
            handle: routeModule.handle,
            links: routeModule.links,
            meta: routeModule.meta,
            shouldRevalidate: routeModule.shouldRevalidate
        };
    })();
}
function getRouteModuleComponent(routeModule) {
    if (routeModule.default == null) return void 0;
    var isEmptyObject = typeof routeModule.default === "object" && Object.keys(routeModule.default).length === 0;
    if (!isEmptyObject) {
        return routeModule.default;
    }
}
function shouldHydrateRouteLoader(routeId, clientLoader, hasLoader, isSpaMode) {
    return isSpaMode && routeId !== "root" || clientLoader != null && (clientLoader.hydrate === true || hasLoader !== true);
}
// lib/dom/ssr/fog-of-war.ts
var nextPaths = /* @__PURE__ */ new Set();
var discoveredPathsMaxSize = 1e3;
var discoveredPaths = /* @__PURE__ */ new Set();
var URL_LIMIT = 7680;
function isFogOfWarEnabled(routeDiscovery, ssr) {
    return routeDiscovery.mode === "lazy" && ssr === true;
}
function getPartialManifest(_0, _1) {
    var _ref = [
        _0,
        _1
    ], [_ref1, ..._rest] = _ref, { sri } = _ref1, manifest = (0,_swc_helpers_object_without_properties__rspack_import_5._)(_ref1, [
        "sri"
    ]), [router] = _rest;
    var routeIds = new Set(router.state.matches.map((m)=>m.route.id));
    var segments = router.state.location.pathname.split("/").filter(Boolean);
    var paths = [
        "/"
    ];
    segments.pop();
    while(segments.length > 0){
        paths.push("/".concat(segments.join("/")));
        segments.pop();
    }
    paths.forEach((path)=>{
        var matches = matchRoutes(router.routes, path, router.basename);
        if (matches) {
            matches.forEach((m)=>routeIds.add(m.route.id));
        }
    });
    var initialRoutes = [
        ...routeIds
    ].reduce((acc, id)=>Object.assign(acc, {
            [id]: manifest.routes[id]
        }), {});
    return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, manifest), {
        routes: initialRoutes,
        sri: sri ? true : void 0
    });
}
function getPatchRoutesOnNavigationFunction(manifest, routeModules, ssr, routeDiscovery, isSpaMode, basename) {
    if (!isFogOfWarEnabled(routeDiscovery, ssr)) {
        return void 0;
    }
    return (param)=>{
        var { path, patch, signal, fetcherKey } = param;
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            if (discoveredPaths.has(path)) {
                return;
            }
            yield fetchAndApplyManifestPatches([
                path
            ], fetcherKey ? window.location.href : path, manifest, routeModules, ssr, isSpaMode, basename, routeDiscovery.manifestPath, patch, signal);
        })();
    };
}
function useFogOFWarDiscovery(router, manifest, routeModules, ssr, routeDiscovery, isSpaMode) {
    _s50();
    react__rspack_import_0.useEffect(()=>{
        var // @ts-expect-error - TS doesn't know about this yet
        _window_navigator_connection, _window_navigator;
        if (!isFogOfWarEnabled(routeDiscovery, ssr) || ((_window_navigator = window.navigator) === null || _window_navigator === void 0 ? void 0 : (_window_navigator_connection = _window_navigator.connection) === null || _window_navigator_connection === void 0 ? void 0 : _window_navigator_connection.saveData) === true) {
            return;
        }
        function registerElement(el) {
            var path = el.tagName === "FORM" ? el.getAttribute("action") : el.getAttribute("href");
            if (!path) {
                return;
            }
            var pathname = el.tagName === "A" ? el.pathname : new URL(path, window.location.origin).pathname;
            if (!discoveredPaths.has(pathname)) {
                nextPaths.add(pathname);
            }
        }
        function fetchPatches() {
            return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
                document.querySelectorAll("a[data-discover], form[data-discover]").forEach(registerElement);
                var lazyPaths = Array.from(nextPaths.keys()).filter((path)=>{
                    if (discoveredPaths.has(path)) {
                        nextPaths.delete(path);
                        return false;
                    }
                    return true;
                });
                if (lazyPaths.length === 0) {
                    return;
                }
                try {
                    yield fetchAndApplyManifestPatches(lazyPaths, null, manifest, routeModules, ssr, isSpaMode, router.basename, routeDiscovery.manifestPath, router.patchRoutes);
                } catch (e) {
                    console.error("Failed to fetch manifest patches", e);
                }
            })();
        }
        var debouncedFetchPatches = debounce(fetchPatches, 100);
        fetchPatches();
        var observer = new MutationObserver(()=>debouncedFetchPatches());
        observer.observe(document.documentElement, {
            subtree: true,
            childList: true,
            attributes: true,
            attributeFilter: [
                "data-discover",
                "href",
                "action"
            ]
        });
        return ()=>observer.disconnect();
    }, [
        ssr,
        isSpaMode,
        manifest,
        routeModules,
        router,
        routeDiscovery
    ]);
}
_s50(useFogOFWarDiscovery, "OD7bBpZva5O2jO+Puf00hKivP7c=");
function getManifestPath(_manifestPath, basename) {
    var manifestPath = _manifestPath || "/__manifest";
    if (basename == null) {
        return manifestPath;
    }
    return "".concat(basename).concat(manifestPath).replace(/\/+/g, "/");
}
var MANIFEST_VERSION_STORAGE_KEY = "react-router-manifest-version";
function fetchAndApplyManifestPatches(paths, errorReloadPath, manifest, routeModules, ssr, isSpaMode, basename, manifestPath, patchRoutes, signal) {
    return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
        var searchParams = new URLSearchParams();
        searchParams.set("paths", paths.sort().join(","));
        searchParams.set("version", manifest.version);
        var url = new URL(getManifestPath(manifestPath, basename), window.location.origin);
        url.search = searchParams.toString();
        if (url.toString().length > URL_LIMIT) {
            nextPaths.clear();
            return;
        }
        var serverPatches;
        try {
            var res = yield fetch(url, {
                signal
            });
            if (!res.ok) {
                throw new Error("".concat(res.status, " ").concat(res.statusText));
            } else if (res.status === 204 && res.headers.has("X-Remix-Reload-Document")) {
                if (!errorReloadPath) {
                    console.warn("Detected a manifest version mismatch during eager route discovery. The next navigation/fetch to an undiscovered route will result in a new document navigation to sync up with the latest manifest.");
                    return;
                }
                try {
                    if (sessionStorage.getItem(MANIFEST_VERSION_STORAGE_KEY) === manifest.version) {
                        console.error("Unable to discover routes due to manifest version mismatch.");
                        return;
                    }
                    sessionStorage.setItem(MANIFEST_VERSION_STORAGE_KEY, manifest.version);
                } catch (unused) {}
                window.location.href = errorReloadPath;
                console.warn("Detected manifest version mismatch, reloading...");
                yield new Promise(()=>{});
            } else if (res.status >= 400) {
                throw new Error((yield res.text()));
            }
            try {
                sessionStorage.removeItem(MANIFEST_VERSION_STORAGE_KEY);
            } catch (unused) {}
            serverPatches = yield res.json();
        } catch (e) {
            if (signal === null || signal === void 0 ? void 0 : signal.aborted) return;
            throw e;
        }
        var knownRoutes = new Set(Object.keys(manifest.routes));
        var patches = Object.values(serverPatches).reduce((acc, route)=>{
            if (route && !knownRoutes.has(route.id)) {
                acc[route.id] = route;
            }
            return acc;
        }, {});
        Object.assign(manifest.routes, patches);
        paths.forEach((p)=>addToFifoQueue(p, discoveredPaths));
        var parentIds = /* @__PURE__ */ new Set();
        Object.values(patches).forEach((patch)=>{
            if (patch && (!patch.parentId || !patches[patch.parentId])) {
                parentIds.add(patch.parentId);
            }
        });
        parentIds.forEach((parentId)=>patchRoutes(parentId || null, createClientRoutes(patches, routeModules, null, ssr, isSpaMode, parentId)));
    })();
}
function addToFifoQueue(path, queue) {
    if (queue.size >= discoveredPathsMaxSize) {
        var first = queue.values().next().value;
        queue.delete(first);
    }
    queue.add(path);
}
function debounce(callback, wait) {
    var timeoutId;
    return function() {
        for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
            args[_key] = arguments[_key];
        }
        window.clearTimeout(timeoutId);
        timeoutId = window.setTimeout(()=>callback(...args), wait);
    };
}
// lib/dom/ssr/components.tsx
function useDataRouterContext2() {
    _s51();
    var context = react__rspack_import_0.useContext(DataRouterContext);
    invariant2(context, "You must render this element inside a <DataRouterContext.Provider> element");
    return context;
}
_s51(useDataRouterContext2, "b9L3QQ+jgeyIrH0NfHrJ8nn7VMU=");
function useDataRouterStateContext() {
    _s52();
    var context = react__rspack_import_0.useContext(DataRouterStateContext);
    invariant2(context, "You must render this element inside a <DataRouterStateContext.Provider> element");
    return context;
}
_s52(useDataRouterStateContext, "b9L3QQ+jgeyIrH0NfHrJ8nn7VMU=");
var FrameworkContext = /*#__PURE__*/ react__rspack_import_0.createContext(void 0);
FrameworkContext.displayName = "FrameworkContext";
function useFrameworkContext() {
    _s53();
    var context = react__rspack_import_0.useContext(FrameworkContext);
    invariant2(context, "You must render this element inside a <HydratedRouter> element");
    return context;
}
_s53(useFrameworkContext, "b9L3QQ+jgeyIrH0NfHrJ8nn7VMU=");
function usePrefetchBehavior(prefetch, theirElementProps) {
    _s54();
    var frameworkContext = react__rspack_import_0.useContext(FrameworkContext);
    var [maybePrefetch, setMaybePrefetch] = react__rspack_import_0.useState(false);
    var [shouldPrefetch, setShouldPrefetch] = react__rspack_import_0.useState(false);
    var { onFocus, onBlur, onMouseEnter, onMouseLeave, onTouchStart } = theirElementProps;
    var ref = react__rspack_import_0.useRef(null);
    react__rspack_import_0.useEffect(()=>{
        if (prefetch === "render") {
            setShouldPrefetch(true);
        }
        if (prefetch === "viewport") {
            var callback = (entries)=>{
                entries.forEach((entry)=>{
                    setShouldPrefetch(entry.isIntersecting);
                });
            };
            var observer = new IntersectionObserver(callback, {
                threshold: 0.5
            });
            if (ref.current) observer.observe(ref.current);
            return ()=>{
                observer.disconnect();
            };
        }
    }, [
        prefetch
    ]);
    react__rspack_import_0.useEffect(()=>{
        if (maybePrefetch) {
            var id = setTimeout(()=>{
                setShouldPrefetch(true);
            }, 100);
            return ()=>{
                clearTimeout(id);
            };
        }
    }, [
        maybePrefetch
    ]);
    var setIntent = ()=>{
        setMaybePrefetch(true);
    };
    var cancelIntent = ()=>{
        setMaybePrefetch(false);
        setShouldPrefetch(false);
    };
    if (!frameworkContext) {
        return [
            false,
            ref,
            {}
        ];
    }
    if (prefetch !== "intent") {
        return [
            shouldPrefetch,
            ref,
            {}
        ];
    }
    return [
        shouldPrefetch,
        ref,
        {
            onFocus: composeEventHandlers(onFocus, setIntent),
            onBlur: composeEventHandlers(onBlur, cancelIntent),
            onMouseEnter: composeEventHandlers(onMouseEnter, setIntent),
            onMouseLeave: composeEventHandlers(onMouseLeave, cancelIntent),
            onTouchStart: composeEventHandlers(onTouchStart, setIntent)
        }
    ];
}
_s54(usePrefetchBehavior, "+/vhFzzdrq26roTceeGYHO+rmmA=");
function composeEventHandlers(theirHandler, ourHandler) {
    return (event)=>{
        theirHandler && theirHandler(event);
        if (!event.defaultPrevented) {
            ourHandler(event);
        }
    };
}
function getActiveMatches(matches, errors, isSpaMode) {
    if (isSpaMode && !isHydrated) {
        return [
            matches[0]
        ];
    }
    if (errors) {
        var errorIdx = matches.findIndex((m)=>errors[m.route.id] !== void 0);
        return matches.slice(0, errorIdx + 1);
    }
    return matches;
}
var CRITICAL_CSS_DATA_ATTRIBUTE = "data-react-router-critical-css";
function Links(param) {
    var { nonce, crossOrigin } = param;
    _s55();
    var { isSpaMode, manifest, routeModules, criticalCss } = useFrameworkContext();
    var { errors, matches: routerMatches } = useDataRouterStateContext();
    var matches = getActiveMatches(routerMatches, errors, isSpaMode);
    var keyedLinks = react__rspack_import_0.useMemo(()=>getKeyedLinksForMatches(matches, routeModules, manifest), [
        matches,
        routeModules,
        manifest
    ]);
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, typeof criticalCss === "string" ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("style", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, {
        [CRITICAL_CSS_DATA_ATTRIBUTE]: ""
    }), {
        nonce,
        dangerouslySetInnerHTML: {
            __html: criticalCss
        }
    })) : null, typeof criticalCss === "object" ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("link", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, {
        [CRITICAL_CSS_DATA_ATTRIBUTE]: ""
    }), {
        rel: "stylesheet",
        href: criticalCss.href,
        nonce,
        crossOrigin
    })) : null, keyedLinks.map((param)=>{
        var { key, link } = param;
        var _link_crossOrigin, _link_crossOrigin1;
        return isPageLinkDescriptor(link) ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(PrefetchPageLinks, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({
            key,
            nonce
        }, link), {
            crossOrigin: (_link_crossOrigin = link.crossOrigin) !== null && _link_crossOrigin !== void 0 ? _link_crossOrigin : crossOrigin
        })) : /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("link", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({
            key,
            nonce
        }, link), {
            crossOrigin: (_link_crossOrigin1 = link.crossOrigin) !== null && _link_crossOrigin1 !== void 0 ? _link_crossOrigin1 : crossOrigin
        }));
    }));
}
_s55(Links, "Ocm5oaH/es+Td7/xVNenUsxSg/E=", false, function() {
    return [
        useFrameworkContext,
        useDataRouterStateContext
    ];
});
_c20 = Links;
function PrefetchPageLinks(_0) {
    var { page } = _0, linkProps = (0,_swc_helpers_object_without_properties__rspack_import_5._)(_0, [
        "page"
    ]);
    _s56();
    var { router } = useDataRouterContext2();
    var matches = react__rspack_import_0.useMemo(()=>matchRoutes(router.routes, page, router.basename), [
        router.routes,
        page,
        router.basename
    ]);
    if (!matches) {
        return null;
    }
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(PrefetchPageLinksImpl, (0,_swc_helpers_object_spread__rspack_import_2._)({
        page,
        matches
    }, linkProps));
}
_s56(PrefetchPageLinks, "Ju1avLqF8pkmE+gxElEwFRGedho=", false, function() {
    return [
        useDataRouterContext2
    ];
});
_c21 = PrefetchPageLinks;
function useKeyedPrefetchLinks(matches) {
    _s57();
    var { manifest, routeModules } = useFrameworkContext();
    var [keyedPrefetchLinks, setKeyedPrefetchLinks] = react__rspack_import_0.useState([]);
    react__rspack_import_0.useEffect(()=>{
        var interrupted = false;
        void getKeyedPrefetchLinks(matches, manifest, routeModules).then((links)=>{
            if (!interrupted) {
                setKeyedPrefetchLinks(links);
            }
        });
        return ()=>{
            interrupted = true;
        };
    }, [
        matches,
        manifest,
        routeModules
    ]);
    return keyedPrefetchLinks;
}
_s57(useKeyedPrefetchLinks, "sWHEtt5QMED1uFo3cmfzVZ3FRTU=", false, function() {
    return [
        useFrameworkContext
    ];
});
function PrefetchPageLinksImpl(_0) {
    var { page, matches: nextMatches } = _0, linkProps = (0,_swc_helpers_object_without_properties__rspack_import_5._)(_0, [
        "page",
        "matches"
    ]);
    _s58();
    var location = useLocation();
    var { future, manifest, routeModules } = useFrameworkContext();
    var { basename } = useDataRouterContext2();
    var { loaderData, matches } = useDataRouterStateContext();
    var newMatchesForData = react__rspack_import_0.useMemo(()=>getNewMatchesForLinks(page, nextMatches, matches, manifest, location, "data"), [
        page,
        nextMatches,
        matches,
        manifest,
        location
    ]);
    var newMatchesForAssets = react__rspack_import_0.useMemo(()=>getNewMatchesForLinks(page, nextMatches, matches, manifest, location, "assets"), [
        page,
        nextMatches,
        matches,
        manifest,
        location
    ]);
    var dataHrefs = react__rspack_import_0.useMemo(()=>{
        if (page === location.pathname + location.search + location.hash) {
            return [];
        }
        var routesParams = /* @__PURE__ */ new Set();
        var foundOptOutRoute = false;
        nextMatches.forEach((m)=>{
            var _routeModules_m_route_id;
            var manifestRoute = manifest.routes[m.route.id];
            if (!manifestRoute || !manifestRoute.hasLoader) {
                return;
            }
            if (!newMatchesForData.some((m2)=>m2.route.id === m.route.id) && m.route.id in loaderData && ((_routeModules_m_route_id = routeModules[m.route.id]) === null || _routeModules_m_route_id === void 0 ? void 0 : _routeModules_m_route_id.shouldRevalidate)) {
                foundOptOutRoute = true;
            } else if (manifestRoute.hasClientLoader) {
                foundOptOutRoute = true;
            } else {
                routesParams.add(m.route.id);
            }
        });
        if (routesParams.size === 0) {
            return [];
        }
        var url = singleFetchUrl(page, basename, future.unstable_trailingSlashAwareDataRequests, "data");
        if (foundOptOutRoute && routesParams.size > 0) {
            url.searchParams.set("_routes", nextMatches.filter((m)=>routesParams.has(m.route.id)).map((m)=>m.route.id).join(","));
        }
        return [
            url.pathname + url.search
        ];
    }, [
        basename,
        future.unstable_trailingSlashAwareDataRequests,
        loaderData,
        location,
        manifest,
        newMatchesForData,
        nextMatches,
        page,
        routeModules
    ]);
    var moduleHrefs = react__rspack_import_0.useMemo(()=>getModuleLinkHrefs(newMatchesForAssets, manifest), [
        newMatchesForAssets,
        manifest
    ]);
    var keyedPrefetchLinks = useKeyedPrefetchLinks(newMatchesForAssets);
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, dataHrefs.map((href)=>/* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("link", (0,_swc_helpers_object_spread__rspack_import_2._)({
            key: href,
            rel: "prefetch",
            as: "fetch",
            href
        }, linkProps))), moduleHrefs.map((href)=>/* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("link", (0,_swc_helpers_object_spread__rspack_import_2._)({
            key: href,
            rel: "modulepreload",
            href
        }, linkProps))), keyedPrefetchLinks.map((param)=>{
        var { key, link } = param;
        var _link_crossOrigin;
        return(// these don't spread `linkProps` because they are full link descriptors
        // already with their own props
        /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("link", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({
            key,
            nonce: linkProps.nonce
        }, link), {
            crossOrigin: (_link_crossOrigin = link.crossOrigin) !== null && _link_crossOrigin !== void 0 ? _link_crossOrigin : linkProps.crossOrigin
        })));
    }));
}
_s58(PrefetchPageLinksImpl, "t7PJjDFK5FauQ5AqEZWmU67nlhE=", false, function() {
    return [
        useLocation,
        useFrameworkContext,
        useDataRouterContext2,
        useDataRouterStateContext,
        useKeyedPrefetchLinks
    ];
});
_c22 = PrefetchPageLinksImpl;
function Meta() {
    _s59();
    var { isSpaMode, routeModules } = useFrameworkContext();
    var { errors, matches: routerMatches, loaderData } = useDataRouterStateContext();
    var location = useLocation();
    var _matches = getActiveMatches(routerMatches, errors, isSpaMode);
    var error = null;
    if (errors) {
        error = errors[_matches[_matches.length - 1].route.id];
    }
    var meta = [];
    var leafMeta = null;
    var matches = [];
    for(var i = 0; i < _matches.length; i++){
        var _match = _matches[i];
        var routeId = _match.route.id;
        var data2 = loaderData[routeId];
        var params = _match.params;
        var routeModule = routeModules[routeId];
        var routeMeta = [];
        var match = {
            id: routeId,
            data: data2,
            loaderData: data2,
            meta: [],
            params: _match.params,
            pathname: _match.pathname,
            handle: _match.route.handle,
            error
        };
        matches[i] = match;
        if (routeModule === null || routeModule === void 0 ? void 0 : routeModule.meta) {
            routeMeta = typeof routeModule.meta === "function" ? routeModule.meta({
                data: data2,
                loaderData: data2,
                params,
                location,
                matches,
                error
            }) : Array.isArray(routeModule.meta) ? [
                ...routeModule.meta
            ] : routeModule.meta;
        } else if (leafMeta) {
            routeMeta = [
                ...leafMeta
            ];
        }
        routeMeta = routeMeta || [];
        if (!Array.isArray(routeMeta)) {
            throw new Error("The route at " + _match.route.path + " returns an invalid value. All route meta functions must return an array of meta objects.\n\nTo reference the meta function API, see https://remix.run/route/meta");
        }
        match.meta = routeMeta;
        matches[i] = match;
        meta = [
            ...routeMeta
        ];
        leafMeta = meta;
    }
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, meta.flat().map((metaProps)=>{
        if (!metaProps) {
            return null;
        }
        if ("tagName" in metaProps) {
            var { tagName } = metaProps, rest = (0,_swc_helpers_object_without_properties__rspack_import_5._)(metaProps, [
                "tagName"
            ]);
            if (!isValidMetaTag(tagName)) {
                console.warn("A meta object uses an invalid tagName: ".concat(tagName, ". Expected either 'link' or 'meta'"));
                return null;
            }
            var Comp = tagName;
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Comp, (0,_swc_helpers_object_spread__rspack_import_2._)({
                key: JSON.stringify(rest)
            }, rest));
        }
        if ("title" in metaProps) {
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("title", {
                key: "title"
            }, String(metaProps.title));
        }
        if ("charset" in metaProps) {
            var _metaProps_charSet;
            (_metaProps_charSet = metaProps.charSet) !== null && _metaProps_charSet !== void 0 ? _metaProps_charSet : metaProps.charSet = metaProps.charset;
            delete metaProps.charset;
        }
        if ("charSet" in metaProps && metaProps.charSet != null) {
            return typeof metaProps.charSet === "string" ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("meta", {
                key: "charSet",
                charSet: metaProps.charSet
            }) : null;
        }
        if ("script:ld+json" in metaProps) {
            try {
                var json = JSON.stringify(metaProps["script:ld+json"]);
                return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", {
                    key: "script:ld+json:".concat(json),
                    type: "application/ld+json",
                    dangerouslySetInnerHTML: {
                        __html: escapeHtml(json)
                    }
                });
            } catch (err) {
                return null;
            }
        }
        return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("meta", (0,_swc_helpers_object_spread__rspack_import_2._)({
            key: JSON.stringify(metaProps)
        }, metaProps));
    }));
}
_s59(Meta, "w17N/ChjgqGYfQOaYWf4yD46LhE=", false, function() {
    return [
        useFrameworkContext,
        useDataRouterStateContext,
        useLocation
    ];
});
_c23 = Meta;
function isValidMetaTag(tagName) {
    return typeof tagName === "string" && /^(meta|link)$/.test(tagName);
}
var isHydrated = false;
function setIsHydrated() {
    isHydrated = true;
}
function Scripts(scriptProps) {
    _s60();
    var { manifest, serverHandoffString, isSpaMode, renderMeta, routeDiscovery, ssr } = useFrameworkContext();
    var { router, static: isStatic, staticContext } = useDataRouterContext2();
    var { matches: routerMatches } = useDataRouterStateContext();
    var isRSCRouterContext = useIsRSCRouterContext();
    var enableFogOfWar = isFogOfWarEnabled(routeDiscovery, ssr);
    if (renderMeta) {
        renderMeta.didRenderScripts = true;
    }
    var matches = getActiveMatches(routerMatches, null, isSpaMode);
    react__rspack_import_0.useEffect(()=>{
        setIsHydrated();
    }, []);
    var initialScripts = react__rspack_import_0.useMemo(()=>{
        var _manifest_hmr;
        if (isRSCRouterContext) {
            return null;
        }
        var streamScript = "window.__reactRouterContext.stream = new ReadableStream({start(controller){window.__reactRouterContext.streamController = controller;}}).pipeThrough(new TextEncoderStream());";
        var contextScript = staticContext ? "window.__reactRouterContext = ".concat(serverHandoffString, ";").concat(streamScript) : " ";
        var routeModulesScript = !isStatic ? " " : "".concat(((_manifest_hmr = manifest.hmr) === null || _manifest_hmr === void 0 ? void 0 : _manifest_hmr.runtime) ? "import ".concat(JSON.stringify(manifest.hmr.runtime), ";") : "").concat(!enableFogOfWar ? "import ".concat(JSON.stringify(manifest.url)) : "", ";\n").concat(matches.map((match, routeIndex)=>{
            var routeVarName = "route".concat(routeIndex);
            var manifestEntry = manifest.routes[match.route.id];
            invariant2(manifestEntry, "Route ".concat(match.route.id, " not found in manifest"));
            var { clientActionModule, clientLoaderModule, clientMiddlewareModule, hydrateFallbackModule, module } = manifestEntry;
            var chunks = [
                ...clientActionModule ? [
                    {
                        module: clientActionModule,
                        varName: "".concat(routeVarName, "_clientAction")
                    }
                ] : [],
                ...clientLoaderModule ? [
                    {
                        module: clientLoaderModule,
                        varName: "".concat(routeVarName, "_clientLoader")
                    }
                ] : [],
                ...clientMiddlewareModule ? [
                    {
                        module: clientMiddlewareModule,
                        varName: "".concat(routeVarName, "_clientMiddleware")
                    }
                ] : [],
                ...hydrateFallbackModule ? [
                    {
                        module: hydrateFallbackModule,
                        varName: "".concat(routeVarName, "_HydrateFallback")
                    }
                ] : [],
                {
                    module,
                    varName: "".concat(routeVarName, "_main")
                }
            ];
            if (chunks.length === 1) {
                return "import * as ".concat(routeVarName, " from ").concat(JSON.stringify(module), ";");
            }
            var chunkImportsSnippet = chunks.map((chunk)=>"import * as ".concat(chunk.varName, ' from "').concat(chunk.module, '";')).join("\n");
            var mergedChunksSnippet = "const ".concat(routeVarName, " = {").concat(chunks.map((chunk)=>"...".concat(chunk.varName)).join(","), "};");
            return [
                chunkImportsSnippet,
                mergedChunksSnippet
            ].join("\n");
        }).join("\n"), "\n  ").concat(enableFogOfWar ? // Inline a minimal manifest with the SSR matches
        "window.__reactRouterManifest = ".concat(JSON.stringify(getPartialManifest(manifest, router), null, 2), ";") : "", "\n  window.__reactRouterRouteModules = {").concat(matches.map((match, index)=>"".concat(JSON.stringify(match.route.id), ":route").concat(index)).join(","), "};\n\nimport(").concat(JSON.stringify(manifest.entry.module), ");");
        return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, scriptProps), {
            suppressHydrationWarning: true,
            dangerouslySetInnerHTML: {
                __html: contextScript
            },
            type: void 0
        })), /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, scriptProps), {
            suppressHydrationWarning: true,
            dangerouslySetInnerHTML: {
                __html: routeModulesScript
            },
            type: "module",
            async: true
        })));
    }, []);
    var preloads = isHydrated || isRSCRouterContext ? [] : dedupe(manifest.entry.imports.concat(getModuleLinkHrefs(matches, manifest, {
        includeHydrateFallback: true
    })));
    var sri = typeof manifest.sri === "object" ? manifest.sri : {};
    warnOnce(!isRSCRouterContext, "The <Scripts /> element is a no-op when using RSC and can be safely removed.");
    return isHydrated || isRSCRouterContext ? null : /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, typeof manifest.sri === "object" ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, scriptProps), {
        "rr-importmap": "",
        type: "importmap",
        suppressHydrationWarning: true,
        dangerouslySetInnerHTML: {
            __html: JSON.stringify({
                integrity: sri
            })
        }
    })) : null, !enableFogOfWar ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("link", {
        rel: "modulepreload",
        href: manifest.url,
        crossOrigin: scriptProps.crossOrigin,
        integrity: sri[manifest.url],
        suppressHydrationWarning: true
    }) : null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("link", {
        rel: "modulepreload",
        href: manifest.entry.module,
        crossOrigin: scriptProps.crossOrigin,
        integrity: sri[manifest.entry.module],
        suppressHydrationWarning: true
    }), preloads.map((path)=>/* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("link", {
            key: path,
            rel: "modulepreload",
            href: path,
            crossOrigin: scriptProps.crossOrigin,
            integrity: sri[path],
            suppressHydrationWarning: true
        })), initialScripts);
}
_s60(Scripts, "EbapkRrGWXHpxsMAC4NnJTT4ZYw=", false, function() {
    return [
        useFrameworkContext,
        useDataRouterContext2,
        useDataRouterStateContext,
        useIsRSCRouterContext
    ];
});
_c24 = Scripts;
function dedupe(array) {
    return [
        ...new Set(array)
    ];
}
function mergeRefs() {
    for(var _len = arguments.length, refs = new Array(_len), _key = 0; _key < _len; _key++){
        refs[_key] = arguments[_key];
    }
    return (value)=>{
        refs.forEach((ref)=>{
            if (typeof ref === "function") {
                ref(value);
            } else if (ref != null) {
                ref.current = value;
            }
        });
    };
}
// lib/dom/ssr/errorBoundaries.tsx
var RemixErrorBoundary = class RemixErrorBoundary extends react__rspack_import_0.Component {
    static getDerivedStateFromError(error) {
        return {
            error
        };
    }
    static getDerivedStateFromProps(props, state) {
        if (state.location !== props.location) {
            return {
                error: props.error || null,
                location: props.location
            };
        }
        return {
            error: props.error || state.error,
            location: state.location
        };
    }
    render() {
        if (this.state.error) {
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(RemixRootDefaultErrorBoundary, {
                error: this.state.error,
                isOutsideRemixApp: true
            });
        } else {
            return this.props.children;
        }
    }
    constructor(props){
        super(props);
        this.state = {
            error: props.error || null,
            location: props.location
        };
    }
};
function RemixRootDefaultErrorBoundary(param) {
    var { error, isOutsideRemixApp } = param;
    console.error(error);
    var heyDeveloper = /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", {
        dangerouslySetInnerHTML: {
            __html: '\n        console.log(\n          "\uD83D\uDCBF Hey developer \uD83D\uDC4B. You can provide a way better UX than this when your app throws errors. Check out https://reactrouter.com/how-to/error-boundary for more information."\n        );\n      '
        }
    });
    if (isRouteErrorResponse(error)) {
        return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(BoundaryShell, {
            title: "Unhandled Thrown Response!"
        }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("h1", {
            style: {
                fontSize: "24px"
            }
        }, error.status, " ", error.statusText), ENABLE_DEV_WARNINGS ? heyDeveloper : null);
    }
    var errorInstance;
    if (error instanceof Error) {
        errorInstance = error;
    } else {
        var errorString = error == null ? "Unknown Error" : typeof error === "object" && "toString" in error ? error.toString() : JSON.stringify(error);
        errorInstance = new Error(errorString);
    }
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(BoundaryShell, {
        title: "Application Error!",
        isOutsideRemixApp
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("h1", {
        style: {
            fontSize: "24px"
        }
    }, "Application Error"), /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("pre", {
        style: {
            padding: "2rem",
            background: "hsla(10, 50%, 50%, 0.1)",
            color: "red",
            overflow: "auto"
        }
    }, errorInstance.stack), heyDeveloper);
}
_c25 = RemixRootDefaultErrorBoundary;
function BoundaryShell(param) {
    var { title, renderScripts, isOutsideRemixApp, children } = param;
    var _routeModules_root;
    _s61();
    var { routeModules } = useFrameworkContext();
    if (((_routeModules_root = routeModules.root) === null || _routeModules_root === void 0 ? void 0 : _routeModules_root.Layout) && !isOutsideRemixApp) {
        return children;
    }
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("html", {
        lang: "en"
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("head", null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("meta", {
        charSet: "utf-8"
    }), /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("meta", {
        name: "viewport",
        content: "width=device-width,initial-scale=1,viewport-fit=cover"
    }), /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("title", null, title)), /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("body", null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("main", {
        style: {
            fontFamily: "system-ui, sans-serif",
            padding: "2rem"
        }
    }, children, renderScripts ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Scripts, null) : null)));
}
_s61(BoundaryShell, "tZEWUlq3XQ3GoTndP1oB/43iz9A=", false, function() {
    return [
        useFrameworkContext
    ];
});
_c26 = BoundaryShell;
// lib/dom/lib.tsx

var isBrowser2 = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
try {
    if (isBrowser2) {
        window.__reactRouterVersion = "7.13.0";
    }
} catch (e) {}
function createBrowserRouter(routes, opts) {
    return createRouter({
        basename: opts === null || opts === void 0 ? void 0 : opts.basename,
        getContext: opts === null || opts === void 0 ? void 0 : opts.getContext,
        future: opts === null || opts === void 0 ? void 0 : opts.future,
        history: createBrowserHistory({
            window: opts === null || opts === void 0 ? void 0 : opts.window
        }),
        hydrationData: (opts === null || opts === void 0 ? void 0 : opts.hydrationData) || parseHydrationData(),
        routes,
        mapRouteProperties,
        hydrationRouteProperties,
        dataStrategy: opts === null || opts === void 0 ? void 0 : opts.dataStrategy,
        patchRoutesOnNavigation: opts === null || opts === void 0 ? void 0 : opts.patchRoutesOnNavigation,
        window: opts === null || opts === void 0 ? void 0 : opts.window,
        unstable_instrumentations: opts === null || opts === void 0 ? void 0 : opts.unstable_instrumentations
    }).initialize();
}
function createHashRouter(routes, opts) {
    return createRouter({
        basename: opts === null || opts === void 0 ? void 0 : opts.basename,
        getContext: opts === null || opts === void 0 ? void 0 : opts.getContext,
        future: opts === null || opts === void 0 ? void 0 : opts.future,
        history: createHashHistory({
            window: opts === null || opts === void 0 ? void 0 : opts.window
        }),
        hydrationData: (opts === null || opts === void 0 ? void 0 : opts.hydrationData) || parseHydrationData(),
        routes,
        mapRouteProperties,
        hydrationRouteProperties,
        dataStrategy: opts === null || opts === void 0 ? void 0 : opts.dataStrategy,
        patchRoutesOnNavigation: opts === null || opts === void 0 ? void 0 : opts.patchRoutesOnNavigation,
        window: opts === null || opts === void 0 ? void 0 : opts.window,
        unstable_instrumentations: opts === null || opts === void 0 ? void 0 : opts.unstable_instrumentations
    }).initialize();
}
function parseHydrationData() {
    var _window;
    var state = (_window = window) === null || _window === void 0 ? void 0 : _window.__staticRouterHydrationData;
    if (state && state.errors) {
        state = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, state), {
            errors: deserializeErrors(state.errors)
        });
    }
    return state;
}
function deserializeErrors(errors) {
    if (!errors) return null;
    var entries = Object.entries(errors);
    var serialized = {};
    for (var [key, val] of entries){
        if (val && val.__type === "RouteErrorResponse") {
            serialized[key] = new ErrorResponseImpl(val.status, val.statusText, val.data, val.internal === true);
        } else if (val && val.__type === "Error") {
            if (val.__subType) {
                var ErrorConstructor = window[val.__subType];
                if (typeof ErrorConstructor === "function") {
                    try {
                        var error = new ErrorConstructor(val.message);
                        error.stack = "";
                        serialized[key] = error;
                    } catch (e) {}
                }
            }
            if (serialized[key] == null) {
                var error1 = new Error(val.message);
                error1.stack = "";
                serialized[key] = error1;
            }
        } else {
            serialized[key] = val;
        }
    }
    return serialized;
}
function BrowserRouter(param) {
    var { basename, children, unstable_useTransitions, window: window2 } = param;
    _s62();
    var historyRef = react__rspack_import_0.useRef();
    if (historyRef.current == null) {
        historyRef.current = createBrowserHistory({
            window: window2,
            v5Compat: true
        });
    }
    var history = historyRef.current;
    var [state, setStateImpl] = react__rspack_import_0.useState({
        action: history.action,
        location: history.location
    });
    var setState = react__rspack_import_0.useCallback((newState)=>{
        if (unstable_useTransitions === false) {
            setStateImpl(newState);
        } else {
            react__rspack_import_0.startTransition(()=>setStateImpl(newState));
        }
    }, [
        unstable_useTransitions
    ]);
    react__rspack_import_0.useLayoutEffect(()=>history.listen(setState), [
        history,
        setState
    ]);
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Router, {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        unstable_useTransitions
    });
}
_s62(BrowserRouter, "4hUJUqHydkgNqll0yBdcphm8IwU=");
_c27 = BrowserRouter;
function HashRouter(param) {
    var { basename, children, unstable_useTransitions, window: window2 } = param;
    _s63();
    var historyRef = react__rspack_import_0.useRef();
    if (historyRef.current == null) {
        historyRef.current = createHashHistory({
            window: window2,
            v5Compat: true
        });
    }
    var history = historyRef.current;
    var [state, setStateImpl] = react__rspack_import_0.useState({
        action: history.action,
        location: history.location
    });
    var setState = react__rspack_import_0.useCallback((newState)=>{
        if (unstable_useTransitions === false) {
            setStateImpl(newState);
        } else {
            react__rspack_import_0.startTransition(()=>setStateImpl(newState));
        }
    }, [
        unstable_useTransitions
    ]);
    react__rspack_import_0.useLayoutEffect(()=>history.listen(setState), [
        history,
        setState
    ]);
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Router, {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        unstable_useTransitions
    });
}
_s63(HashRouter, "4hUJUqHydkgNqll0yBdcphm8IwU=");
_c28 = HashRouter;
function HistoryRouter(param) {
    var { basename, children, history, unstable_useTransitions } = param;
    _s64();
    var [state, setStateImpl] = react__rspack_import_0.useState({
        action: history.action,
        location: history.location
    });
    var setState = react__rspack_import_0.useCallback((newState)=>{
        if (unstable_useTransitions === false) {
            setStateImpl(newState);
        } else {
            react__rspack_import_0.startTransition(()=>setStateImpl(newState));
        }
    }, [
        unstable_useTransitions
    ]);
    react__rspack_import_0.useLayoutEffect(()=>history.listen(setState), [
        history,
        setState
    ]);
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Router, {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        unstable_useTransitions
    });
}
_s64(HistoryRouter, "qZQfXZolk6/V9qSWbR9WFN9lmtM=");
_c29 = HistoryRouter;
HistoryRouter.displayName = "unstable_HistoryRouter";
var ABSOLUTE_URL_REGEX2 = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
var Link = /*#__PURE__*/ react__rspack_import_0.forwardRef(_s65(function LinkWithRef(_0, _1) {
    var _ref = [
        _0,
        _1
    ], [_ref1, ..._rest] = _ref, { onClick, discover = "render", prefetch = "none", relative, reloadDocument, replace: replace2, state, target, to, preventScrollReset, viewTransition, unstable_defaultShouldRevalidate } = _ref1, rest = (0,_swc_helpers_object_without_properties__rspack_import_5._)(_ref1, [
        "onClick",
        "discover",
        "prefetch",
        "relative",
        "reloadDocument",
        "replace",
        "state",
        "target",
        "to",
        "preventScrollReset",
        "viewTransition",
        "unstable_defaultShouldRevalidate"
    ]), [forwardedRef] = _rest;
    _s65();
    var { basename, unstable_useTransitions } = react__rspack_import_0.useContext(NavigationContext);
    var isAbsolute = typeof to === "string" && ABSOLUTE_URL_REGEX2.test(to);
    var parsed = parseToInfo(to, basename);
    to = parsed.to;
    var href = useHref(to, {
        relative
    });
    var [shouldPrefetch, prefetchRef, prefetchHandlers] = usePrefetchBehavior(prefetch, rest);
    var internalOnClick = useLinkClickHandler(to, {
        replace: replace2,
        state,
        target,
        preventScrollReset,
        relative,
        viewTransition,
        unstable_defaultShouldRevalidate,
        unstable_useTransitions
    });
    function handleClick(event) {
        if (onClick) onClick(event);
        if (!event.defaultPrevented) {
            internalOnClick(event);
        }
    }
    var link = // eslint-disable-next-line jsx-a11y/anchor-has-content
    /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("a", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, rest, prefetchHandlers), {
        href: parsed.absoluteURL || href,
        onClick: parsed.isExternal || reloadDocument ? onClick : handleClick,
        ref: mergeRefs(forwardedRef, prefetchRef),
        target,
        "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
    }));
    return shouldPrefetch && !isAbsolute ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, link, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(PrefetchPageLinks, {
        page: href
    })) : link;
}, "KkcychyqoXJJxLDOIZekq9PwDLk=", false, function() {
    return [
        useHref,
        usePrefetchBehavior,
        useLinkClickHandler
    ];
}));
_c30 = Link;
Link.displayName = "Link";
var NavLink = /*#__PURE__*/ _s66(react__rspack_import_0.forwardRef(_c31 = _s66(function NavLinkWithRef(_0, _1) {
    var _ref = [
        _0,
        _1
    ], [_ref1, ..._rest] = _ref, { "aria-current": ariaCurrentProp = "page", caseSensitive = false, className: classNameProp = "", end = false, style: styleProp, to, viewTransition, children } = _ref1, rest = (0,_swc_helpers_object_without_properties__rspack_import_5._)(_ref1, [
        "aria-current",
        "caseSensitive",
        "className",
        "end",
        "style",
        "to",
        "viewTransition",
        "children"
    ]), [ref] = _rest;
    _s66();
    var path = useResolvedPath(to, {
        relative: rest.relative
    });
    var location = useLocation();
    var routerState = react__rspack_import_0.useContext(DataRouterStateContext);
    var { navigator, basename } = react__rspack_import_0.useContext(NavigationContext);
    var isTransitioning = routerState != null && // Conditional usage is OK here because the usage of a data router is static
    // eslint-disable-next-line react-hooks/rules-of-hooks
    useViewTransitionState(path) && viewTransition === true;
    var toPathname = navigator.encodeLocation ? navigator.encodeLocation(path).pathname : path.pathname;
    var locationPathname = location.pathname;
    var nextLocationPathname = routerState && routerState.navigation && routerState.navigation.location ? routerState.navigation.location.pathname : null;
    if (!caseSensitive) {
        locationPathname = locationPathname.toLowerCase();
        nextLocationPathname = nextLocationPathname ? nextLocationPathname.toLowerCase() : null;
        toPathname = toPathname.toLowerCase();
    }
    if (nextLocationPathname && basename) {
        nextLocationPathname = stripBasename(nextLocationPathname, basename) || nextLocationPathname;
    }
    var endSlashPosition = toPathname !== "/" && toPathname.endsWith("/") ? toPathname.length - 1 : toPathname.length;
    var isActive = locationPathname === toPathname || !end && locationPathname.startsWith(toPathname) && locationPathname.charAt(endSlashPosition) === "/";
    var isPending = nextLocationPathname != null && (nextLocationPathname === toPathname || !end && nextLocationPathname.startsWith(toPathname) && nextLocationPathname.charAt(toPathname.length) === "/");
    var renderProps = {
        isActive,
        isPending,
        isTransitioning
    };
    var ariaCurrent = isActive ? ariaCurrentProp : void 0;
    var className;
    if (typeof classNameProp === "function") {
        className = classNameProp(renderProps);
    } else {
        className = [
            classNameProp,
            isActive ? "active" : null,
            isPending ? "pending" : null,
            isTransitioning ? "transitioning" : null
        ].filter(Boolean).join(" ");
    }
    var style = typeof styleProp === "function" ? styleProp(renderProps) : styleProp;
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Link, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, rest), {
        "aria-current": ariaCurrent,
        className,
        ref,
        style,
        to,
        viewTransition
    }), typeof children === "function" ? children(renderProps) : children);
}, "j92BubfA9kIE/RrzAN67mw66YWU=", false, function() {
    return [
        useResolvedPath,
        useLocation,
        useViewTransitionState
    ];
})), "j92BubfA9kIE/RrzAN67mw66YWU=", false, function() {
    return [
        useResolvedPath,
        useLocation,
        useViewTransitionState
    ];
});
_c32 = NavLink;
NavLink.displayName = "NavLink";
var Form = /*#__PURE__*/ react__rspack_import_0.forwardRef(_s67((_0, _1)=>{
    var _ref = [
        _0,
        _1
    ], [_ref1, ..._rest] = _ref, { discover = "render", fetcherKey, navigate, reloadDocument, replace: replace2, state, method = defaultMethod, action, onSubmit, relative, preventScrollReset, viewTransition, unstable_defaultShouldRevalidate } = _ref1, props = (0,_swc_helpers_object_without_properties__rspack_import_5._)(_ref1, [
        "discover",
        "fetcherKey",
        "navigate",
        "reloadDocument",
        "replace",
        "state",
        "method",
        "action",
        "onSubmit",
        "relative",
        "preventScrollReset",
        "viewTransition",
        "unstable_defaultShouldRevalidate"
    ]), [forwardedRef] = _rest;
    _s67();
    var { unstable_useTransitions } = react__rspack_import_0.useContext(NavigationContext);
    var submit = useSubmit();
    var formAction = useFormAction(action, {
        relative
    });
    var formMethod = method.toLowerCase() === "get" ? "get" : "post";
    var isAbsolute = typeof action === "string" && ABSOLUTE_URL_REGEX2.test(action);
    var submitHandler = (event)=>{
        onSubmit && onSubmit(event);
        if (event.defaultPrevented) return;
        event.preventDefault();
        var submitter = event.nativeEvent.submitter;
        var submitMethod = (submitter === null || submitter === void 0 ? void 0 : submitter.getAttribute("formmethod")) || method;
        var doSubmit = ()=>submit(submitter || event.currentTarget, {
                fetcherKey,
                method: submitMethod,
                navigate,
                replace: replace2,
                state,
                relative,
                preventScrollReset,
                viewTransition,
                unstable_defaultShouldRevalidate
            });
        if (unstable_useTransitions && navigate !== false) {
            react__rspack_import_0.startTransition(()=>doSubmit());
        } else {
            doSubmit();
        }
    };
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("form", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({
        ref: forwardedRef,
        method: formMethod,
        action: formAction,
        onSubmit: reloadDocument ? onSubmit : submitHandler
    }, props), {
        "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
    }));
}, "QHlj3J0PWgoG1kOKge5w0dsJ4j0=", false, function() {
    return [
        useSubmit,
        useFormAction
    ];
}));
_c33 = Form;
Form.displayName = "Form";
function ScrollRestoration(_0) {
    var { getKey, storageKey } = _0, props = (0,_swc_helpers_object_without_properties__rspack_import_5._)(_0, [
        "getKey",
        "storageKey"
    ]);
    _s68();
    var remixContext = react__rspack_import_0.useContext(FrameworkContext);
    var { basename } = react__rspack_import_0.useContext(NavigationContext);
    var location = useLocation();
    var matches = useMatches();
    useScrollRestoration({
        getKey,
        storageKey
    });
    var ssrKey = react__rspack_import_0.useMemo(()=>{
        if (!remixContext || !getKey) return null;
        var userKey = getScrollRestorationKey(location, matches, basename, getKey);
        return userKey !== location.key ? userKey : null;
    }, // Nah, we only need this the first time for the SSR render
    // eslint-disable-next-line react-hooks/exhaustive-deps
    []);
    if (!remixContext || remixContext.isSpaMode) {
        return null;
    }
    var restoreScroll = ((storageKey2, restoreKey)=>{
        if (!window.history.state || !window.history.state.key) {
            var key = Math.random().toString(32).slice(2);
            window.history.replaceState({
                key
            }, "");
        }
        try {
            var positions = JSON.parse(sessionStorage.getItem(storageKey2) || "{}");
            var storedY = positions[restoreKey || window.history.state.key];
            if (typeof storedY === "number") {
                window.scrollTo(0, storedY);
            }
        } catch (error) {
            console.error(error);
            sessionStorage.removeItem(storageKey2);
        }
    }).toString();
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, props), {
        suppressHydrationWarning: true,
        dangerouslySetInnerHTML: {
            __html: "(".concat(restoreScroll, ")(").concat(escapeHtml(JSON.stringify(storageKey || SCROLL_RESTORATION_STORAGE_KEY)), ", ").concat(escapeHtml(JSON.stringify(ssrKey)), ")")
        }
    }));
}
_s68(ScrollRestoration, "eh0fdV4DvL8E1fzzu6+A6p2cBTs=", false, function() {
    return [
        useLocation,
        useMatches,
        useScrollRestoration
    ];
});
_c34 = ScrollRestoration;
ScrollRestoration.displayName = "ScrollRestoration";
function getDataRouterConsoleError2(hookName) {
    return "".concat(hookName, " must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.");
}
function useDataRouterContext3(hookName) {
    _s69();
    var ctx = react__rspack_import_0.useContext(DataRouterContext);
    invariant(ctx, getDataRouterConsoleError2(hookName));
    return ctx;
}
_s69(useDataRouterContext3, "/dMy7t63NXD4eYACoT93CePwGrg=");
function useDataRouterState2(hookName) {
    _s70();
    var state = react__rspack_import_0.useContext(DataRouterStateContext);
    invariant(state, getDataRouterConsoleError2(hookName));
    return state;
}
_s70(useDataRouterState2, "kdLImxeHm3898p7tqgy8Fclg5s8=");
function useLinkClickHandler(to) {
    var { target, replace: replaceProp, state, preventScrollReset, relative, viewTransition, unstable_defaultShouldRevalidate, unstable_useTransitions } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    _s71();
    var navigate = useNavigate();
    var location = useLocation();
    var path = useResolvedPath(to, {
        relative
    });
    return react__rspack_import_0.useCallback((event)=>{
        if (shouldProcessLinkClick(event, target)) {
            event.preventDefault();
            var replace2 = replaceProp !== void 0 ? replaceProp : createPath(location) === createPath(path);
            var doNavigate = ()=>navigate(to, {
                    replace: replace2,
                    state,
                    preventScrollReset,
                    relative,
                    viewTransition,
                    unstable_defaultShouldRevalidate
                });
            if (unstable_useTransitions) {
                react__rspack_import_0.startTransition(()=>doNavigate());
            } else {
                doNavigate();
            }
        }
    }, [
        location,
        navigate,
        path,
        replaceProp,
        state,
        target,
        to,
        preventScrollReset,
        relative,
        viewTransition,
        unstable_defaultShouldRevalidate,
        unstable_useTransitions
    ]);
}
_s71(useLinkClickHandler, "BZYjyGAa1ZxLRntdbX23tSdFTkI=", false, function() {
    return [
        useNavigate,
        useLocation,
        useResolvedPath
    ];
});
function useSearchParams(defaultInit) {
    _s72();
    warning(typeof URLSearchParams !== "undefined", "You cannot use the `useSearchParams` hook in a browser that does not support the URLSearchParams API. If you need to support Internet Explorer 11, we recommend you load a polyfill such as https://github.com/ungap/url-search-params.");
    var defaultSearchParamsRef = react__rspack_import_0.useRef(createSearchParams(defaultInit));
    var hasSetSearchParamsRef = react__rspack_import_0.useRef(false);
    var location = useLocation();
    var searchParams = react__rspack_import_0.useMemo(()=>// Only merge in the defaults if we haven't yet called setSearchParams.
        // Once we call that we want those to take precedence, otherwise you can't
        // remove a param with setSearchParams({}) if it has an initial value
        getSearchParamsForLocation(location.search, hasSetSearchParamsRef.current ? null : defaultSearchParamsRef.current), [
        location.search
    ]);
    var navigate = useNavigate();
    var setSearchParams = react__rspack_import_0.useCallback((nextInit, navigateOptions)=>{
        var newSearchParams = createSearchParams(typeof nextInit === "function" ? nextInit(new URLSearchParams(searchParams)) : nextInit);
        hasSetSearchParamsRef.current = true;
        navigate("?" + newSearchParams, navigateOptions);
    }, [
        navigate,
        searchParams
    ]);
    return [
        searchParams,
        setSearchParams
    ];
}
_s72(useSearchParams, "OI4Q5UoLuAWNlbh84tDqEBFb7zQ=", false, function() {
    return [
        useLocation,
        useNavigate
    ];
});
var fetcherId = 0;
var getUniqueFetcherId = ()=>"__".concat(String(++fetcherId), "__");
function useSubmit() {
    _s73();
    var { router } = useDataRouterContext3("useSubmit" /* UseSubmit */ );
    var { basename } = react__rspack_import_0.useContext(NavigationContext);
    var currentRouteId = useRouteId();
    var routerFetch = router.fetch;
    var routerNavigate = router.navigate;
    return react__rspack_import_0.useCallback(function(target) {
        var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        return (0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            var { action, method, encType, formData, body } = getFormSubmissionInfo(target, basename);
            if (options.navigate === false) {
                var key = options.fetcherKey || getUniqueFetcherId();
                yield routerFetch(key, currentRouteId, options.action || action, {
                    unstable_defaultShouldRevalidate: options.unstable_defaultShouldRevalidate,
                    preventScrollReset: options.preventScrollReset,
                    formData,
                    body,
                    formMethod: options.method || method,
                    formEncType: options.encType || encType,
                    flushSync: options.flushSync
                });
            } else {
                yield routerNavigate(options.action || action, {
                    unstable_defaultShouldRevalidate: options.unstable_defaultShouldRevalidate,
                    preventScrollReset: options.preventScrollReset,
                    formData,
                    body,
                    formMethod: options.method || method,
                    formEncType: options.encType || encType,
                    replace: options.replace,
                    state: options.state,
                    fromRouteId: currentRouteId,
                    flushSync: options.flushSync,
                    viewTransition: options.viewTransition
                });
            }
        })();
    }, [
        routerFetch,
        routerNavigate,
        basename,
        currentRouteId
    ]);
}
_s73(useSubmit, "WxdB39klEKxykuqGKy4EC+CxZek=", false, function() {
    return [
        useDataRouterContext3,
        useRouteId
    ];
});
function useFormAction(action) {
    var { relative } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    _s74();
    var { basename } = react__rspack_import_0.useContext(NavigationContext);
    var routeContext = react__rspack_import_0.useContext(RouteContext);
    invariant(routeContext, "useFormAction must be used inside a RouteContext");
    var [match] = routeContext.matches.slice(-1);
    var path = (0,_swc_helpers_object_spread__rspack_import_2._)({}, useResolvedPath(action ? action : ".", {
        relative
    }));
    var location = useLocation();
    if (action == null) {
        path.search = location.search;
        var params = new URLSearchParams(path.search);
        var indexValues = params.getAll("index");
        var hasNakedIndexParam = indexValues.some((v)=>v === "");
        if (hasNakedIndexParam) {
            params.delete("index");
            indexValues.filter((v)=>v).forEach((v)=>params.append("index", v));
            var qs = params.toString();
            path.search = qs ? "?".concat(qs) : "";
        }
    }
    if ((!action || action === ".") && match.route.index) {
        path.search = path.search ? path.search.replace(/^\?/, "?index&") : "?index";
    }
    if (basename !== "/") {
        path.pathname = path.pathname === "/" ? basename : joinPaths([
            basename,
            path.pathname
        ]);
    }
    return createPath(path);
}
_s74(useFormAction, "BNf8cMtkAItz6ocH7ApgwxvQgy8=", false, function() {
    return [
        useResolvedPath,
        useLocation
    ];
});
function useFetcher() {
    var { key } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    var _route_matches_;
    _s75();
    var { router } = useDataRouterContext3("useFetcher" /* UseFetcher */ );
    var state = useDataRouterState2("useFetcher" /* UseFetcher */ );
    var fetcherData = react__rspack_import_0.useContext(FetchersContext);
    var route = react__rspack_import_0.useContext(RouteContext);
    var routeId = (_route_matches_ = route.matches[route.matches.length - 1]) === null || _route_matches_ === void 0 ? void 0 : _route_matches_.route.id;
    invariant(fetcherData, "useFetcher must be used inside a FetchersContext");
    invariant(route, "useFetcher must be used inside a RouteContext");
    invariant(routeId != null, 'useFetcher can only be used on routes that contain a unique "id"');
    var defaultKey = react__rspack_import_0.useId();
    var [fetcherKey, setFetcherKey] = react__rspack_import_0.useState(key || defaultKey);
    if (key && key !== fetcherKey) {
        setFetcherKey(key);
    }
    var { deleteFetcher, getFetcher, resetFetcher, fetch: routerFetch } = router;
    react__rspack_import_0.useEffect(()=>{
        getFetcher(fetcherKey);
        return ()=>deleteFetcher(fetcherKey);
    }, [
        deleteFetcher,
        getFetcher,
        fetcherKey
    ]);
    var load = react__rspack_import_0.useCallback((href, opts)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            invariant(routeId, "No routeId available for fetcher.load()");
            yield routerFetch(fetcherKey, routeId, href, opts);
        })(), [
        fetcherKey,
        routeId,
        routerFetch
    ]);
    var submitImpl = useSubmit();
    var submit = react__rspack_import_0.useCallback((target, opts)=>(0,_swc_helpers_async_to_generator__rspack_import_3._)(function*() {
            yield submitImpl(target, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, opts), {
                navigate: false,
                fetcherKey
            }));
        })(), [
        fetcherKey,
        submitImpl
    ]);
    var reset = react__rspack_import_0.useCallback((opts)=>resetFetcher(fetcherKey, opts), [
        resetFetcher,
        fetcherKey
    ]);
    var FetcherForm = react__rspack_import_0.useMemo(()=>{
        var FetcherForm2 = /*#__PURE__*/ react__rspack_import_0.forwardRef((props, ref)=>{
            return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Form, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, props), {
                navigate: false,
                fetcherKey,
                ref
            }));
        });
        FetcherForm2.displayName = "fetcher.Form";
        return FetcherForm2;
    }, [
        fetcherKey
    ]);
    var fetcher = state.fetchers.get(fetcherKey) || IDLE_FETCHER;
    var data2 = fetcherData.get(fetcherKey);
    var fetcherWithComponents = react__rspack_import_0.useMemo(()=>(0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({
            Form: FetcherForm,
            submit,
            load,
            reset
        }, fetcher), {
            data: data2
        }), [
        FetcherForm,
        submit,
        load,
        reset,
        fetcher,
        data2
    ]);
    return fetcherWithComponents;
}
_s75(useFetcher, "+1W59CNXFL1D9ifKnaACQrymOcI=", false, function() {
    return [
        useDataRouterContext3,
        useDataRouterState2,
        react__rspack_import_0.useId,
        useSubmit
    ];
});
function useFetchers() {
    _s76();
    var state = useDataRouterState2("useFetchers" /* UseFetchers */ );
    return Array.from(state.fetchers.entries()).map((param)=>{
        var [key, fetcher] = param;
        return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, fetcher), {
            key
        });
    });
}
_s76(useFetchers, "Q4faTkQvrbJ0PEGf7Wr3qX49SIc=", false, function() {
    return [
        useDataRouterState2
    ];
});
var SCROLL_RESTORATION_STORAGE_KEY = "react-router-scroll-positions";
var savedScrollPositions = {};
function getScrollRestorationKey(location, matches, basename, getKey) {
    var key = null;
    if (getKey) {
        if (basename !== "/") {
            key = getKey((0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, location), {
                pathname: stripBasename(location.pathname, basename) || location.pathname
            }), matches);
        } else {
            key = getKey(location, matches);
        }
    }
    if (key == null) {
        key = location.key;
    }
    return key;
}
function useScrollRestoration() {
    var { getKey, storageKey } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    _s77();
    var { router } = useDataRouterContext3("useScrollRestoration" /* UseScrollRestoration */ );
    var { restoreScrollPosition, preventScrollReset } = useDataRouterState2("useScrollRestoration" /* UseScrollRestoration */ );
    var { basename } = react__rspack_import_0.useContext(NavigationContext);
    var location = useLocation();
    var matches = useMatches();
    var navigation = useNavigation();
    react__rspack_import_0.useEffect(()=>{
        window.history.scrollRestoration = "manual";
        return ()=>{
            window.history.scrollRestoration = "auto";
        };
    }, []);
    usePageHide(react__rspack_import_0.useCallback(()=>{
        if (navigation.state === "idle") {
            var key = getScrollRestorationKey(location, matches, basename, getKey);
            savedScrollPositions[key] = window.scrollY;
        }
        try {
            sessionStorage.setItem(storageKey || SCROLL_RESTORATION_STORAGE_KEY, JSON.stringify(savedScrollPositions));
        } catch (error) {
            warning(false, "Failed to save scroll positions in sessionStorage, <ScrollRestoration /> will not work properly (".concat(error, ")."));
        }
        window.history.scrollRestoration = "auto";
    }, [
        navigation.state,
        getKey,
        basename,
        location,
        matches,
        storageKey
    ]));
    if (typeof document !== "undefined") {
        react__rspack_import_0.useLayoutEffect(()=>{
            try {
                var sessionPositions = sessionStorage.getItem(storageKey || SCROLL_RESTORATION_STORAGE_KEY);
                if (sessionPositions) {
                    savedScrollPositions = JSON.parse(sessionPositions);
                }
            } catch (e) {}
        }, [
            storageKey
        ]);
        react__rspack_import_0.useLayoutEffect(()=>{
            var disableScrollRestoration = router === null || router === void 0 ? void 0 : router.enableScrollRestoration(savedScrollPositions, ()=>window.scrollY, getKey ? (location2, matches2)=>getScrollRestorationKey(location2, matches2, basename, getKey) : void 0);
            return ()=>disableScrollRestoration && disableScrollRestoration();
        }, [
            router,
            basename,
            getKey
        ]);
        react__rspack_import_0.useLayoutEffect(()=>{
            if (restoreScrollPosition === false) {
                return;
            }
            if (typeof restoreScrollPosition === "number") {
                window.scrollTo(0, restoreScrollPosition);
                return;
            }
            try {
                if (location.hash) {
                    var el = document.getElementById(decodeURIComponent(location.hash.slice(1)));
                    if (el) {
                        el.scrollIntoView();
                        return;
                    }
                }
            } catch (unused) {
                warning(false, '"'.concat(location.hash.slice(1), '" is not a decodable element ID. The view will not scroll to it.'));
            }
            if (preventScrollReset === true) {
                return;
            }
            window.scrollTo(0, 0);
        }, [
            location,
            restoreScrollPosition,
            preventScrollReset
        ]);
    }
}
_s77(useScrollRestoration, "gdUr+Ili0TAG9qOSpDz9SqVFekE=", false, function() {
    return [
        useDataRouterContext3,
        useDataRouterState2,
        useLocation,
        useMatches,
        useNavigation,
        usePageHide
    ];
});
function useBeforeUnload(callback, options) {
    _s78();
    var { capture } = options || {};
    react__rspack_import_0.useEffect(()=>{
        var opts = capture != null ? {
            capture
        } : void 0;
        window.addEventListener("beforeunload", callback, opts);
        return ()=>{
            window.removeEventListener("beforeunload", callback, opts);
        };
    }, [
        callback,
        capture
    ]);
}
_s78(useBeforeUnload, "OD7bBpZva5O2jO+Puf00hKivP7c=");
function usePageHide(callback, options) {
    _s79();
    var { capture } = options || {};
    react__rspack_import_0.useEffect(()=>{
        var opts = capture != null ? {
            capture
        } : void 0;
        window.addEventListener("pagehide", callback, opts);
        return ()=>{
            window.removeEventListener("pagehide", callback, opts);
        };
    }, [
        callback,
        capture
    ]);
}
_s79(usePageHide, "OD7bBpZva5O2jO+Puf00hKivP7c=");
function usePrompt(param) {
    var { when, message } = param;
    _s80();
    var blocker = useBlocker(when);
    react__rspack_import_0.useEffect(()=>{
        if (blocker.state === "blocked") {
            var proceed = window.confirm(message);
            if (proceed) {
                setTimeout(blocker.proceed, 0);
            } else {
                blocker.reset();
            }
        }
    }, [
        blocker,
        message
    ]);
    react__rspack_import_0.useEffect(()=>{
        if (blocker.state === "blocked" && !when) {
            blocker.reset();
        }
    }, [
        blocker,
        when
    ]);
}
_s80(usePrompt, "jYtya6TSjG38CYszy4JXllb2X4U=", false, function() {
    return [
        useBlocker
    ];
});
function useViewTransitionState(to) {
    var { relative } = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    _s81();
    var vtContext = react__rspack_import_0.useContext(ViewTransitionContext);
    invariant(vtContext != null, "`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?");
    var { basename } = useDataRouterContext3("useViewTransitionState" /* useViewTransitionState */ );
    var path = useResolvedPath(to, {
        relative
    });
    if (!vtContext.isTransitioning) {
        return false;
    }
    var currentPath = stripBasename(vtContext.currentLocation.pathname, basename) || vtContext.currentLocation.pathname;
    var nextPath = stripBasename(vtContext.nextLocation.pathname, basename) || vtContext.nextLocation.pathname;
    return matchPath(path.pathname, nextPath) != null || matchPath(path.pathname, currentPath) != null;
}
_s81(useViewTransitionState, "smAGDSHJ3mfLBIjaUgd9peUJl4E=", false, function() {
    return [
        useDataRouterContext3,
        useResolvedPath
    ];
});
// lib/dom/server.tsx

function StaticRouter(param) {
    var { basename, children, location: locationProp = "/" } = param;
    if (typeof locationProp === "string") {
        locationProp = parsePath(locationProp);
    }
    var action = "POP" /* Pop */ ;
    var location = {
        pathname: locationProp.pathname || "/",
        search: locationProp.search || "",
        hash: locationProp.hash || "",
        state: locationProp.state != null ? locationProp.state : null,
        key: locationProp.key || "default"
    };
    var staticNavigator = getStatelessNavigator();
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Router, {
        basename,
        children,
        location,
        navigationType: action,
        navigator: staticNavigator,
        static: true,
        unstable_useTransitions: false
    });
}
_c35 = StaticRouter;
function StaticRouterProvider(param) {
    var { context, router, hydrate: hydrate2 = true, nonce } = param;
    invariant(router && context, "You must provide `router` and `context` to <StaticRouterProvider>");
    var dataRouterContext = {
        router,
        navigator: getStatelessNavigator(),
        static: true,
        staticContext: context,
        basename: context.basename || "/"
    };
    var fetchersContext = /* @__PURE__ */ new Map();
    var hydrateScript = "";
    if (hydrate2 !== false) {
        var data2 = {
            loaderData: context.loaderData,
            actionData: context.actionData,
            errors: serializeErrors(context.errors)
        };
        var json = escapeHtml(JSON.stringify(JSON.stringify(data2)));
        hydrateScript = "window.__staticRouterHydrationData = JSON.parse(".concat(json, ");");
    }
    var { state } = dataRouterContext.router;
    return /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(react__rspack_import_0.Fragment, null, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(DataRouterContext.Provider, {
        value: dataRouterContext
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(DataRouterStateContext.Provider, {
        value: state
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(FetchersContext.Provider, {
        value: fetchersContext
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(ViewTransitionContext.Provider, {
        value: {
            isTransitioning: false
        }
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(Router, {
        basename: dataRouterContext.basename,
        location: state.location,
        navigationType: state.historyAction,
        navigator: dataRouterContext.navigator,
        static: dataRouterContext.static,
        unstable_useTransitions: false
    }, /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement(DataRoutes2, {
        routes: router.routes,
        future: router.future,
        state
    })))))), hydrateScript ? /* @__PURE__ */ /*#__PURE__*/ react__rspack_import_0.createElement("script", {
        suppressHydrationWarning: true,
        nonce,
        dangerouslySetInnerHTML: {
            __html: hydrateScript
        }
    }) : null);
}
_c36 = StaticRouterProvider;
function DataRoutes2(param) {
    var { routes, future, state } = param;
    _s82();
    return useRoutesImpl(routes, void 0, state, void 0, future);
}
_s82(DataRoutes2, "bxXQ1PjoUitFHQG/730mCYNdT00=", false, function() {
    return [
        useRoutesImpl
    ];
});
_c37 = DataRoutes2;
function serializeErrors(errors) {
    if (!errors) return null;
    var entries = Object.entries(errors);
    var serialized = {};
    for (var [key, val] of entries){
        if (isRouteErrorResponse(val)) {
            serialized[key] = (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, val), {
                __type: "RouteErrorResponse"
            });
        } else if (val instanceof Error) {
            serialized[key] = (0,_swc_helpers_object_spread__rspack_import_2._)({
                message: val.message,
                __type: "Error"
            }, val.name !== "Error" ? {
                __subType: val.name
            } : {});
        } else {
            serialized[key] = val;
        }
    }
    return serialized;
}
function getStatelessNavigator() {
    return {
        createHref,
        encodeLocation,
        push (to) {
            throw new Error("You cannot use navigator.push() on the server because it is a stateless environment. This error was probably triggered when you did a `navigate(".concat(JSON.stringify(to), ")` somewhere in your app."));
        },
        replace (to) {
            throw new Error("You cannot use navigator.replace() on the server because it is a stateless environment. This error was probably triggered when you did a `navigate(".concat(JSON.stringify(to), ", { replace: true })` somewhere in your app."));
        },
        go (delta) {
            throw new Error("You cannot use navigator.go() on the server because it is a stateless environment. This error was probably triggered when you did a `navigate(".concat(delta, ")` somewhere in your app."));
        },
        back () {
            throw new Error("You cannot use navigator.back() on the server because it is a stateless environment.");
        },
        forward () {
            throw new Error("You cannot use navigator.forward() on the server because it is a stateless environment.");
        }
    };
}
function createStaticHandler2(routes, opts) {
    return createStaticHandler(routes, (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, opts), {
        mapRouteProperties
    }));
}
function createStaticRouter(routes, context) {
    var opts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    var manifest = {};
    var dataRoutes = convertRoutesToDataRoutes(routes, mapRouteProperties, void 0, manifest);
    var matches = context.matches.map((match)=>{
        var route = manifest[match.route.id] || match.route;
        return (0,_swc_helpers_object_spread_props__rspack_import_1._)((0,_swc_helpers_object_spread__rspack_import_2._)({}, match), {
            route
        });
    });
    var msg = (method)=>"You cannot use router.".concat(method, "() on the server because it is a stateless environment");
    return {
        get basename () {
            return context.basename;
        },
        get future () {
            return (0,_swc_helpers_object_spread__rspack_import_2._)({
                v8_middleware: false
            }, opts === null || opts === void 0 ? void 0 : opts.future);
        },
        get state () {
            return {
                historyAction: "POP" /* Pop */ ,
                location: context.location,
                matches,
                loaderData: context.loaderData,
                actionData: context.actionData,
                errors: context.errors,
                initialized: true,
                navigation: IDLE_NAVIGATION,
                restoreScrollPosition: null,
                preventScrollReset: false,
                revalidation: "idle",
                fetchers: /* @__PURE__ */ new Map(),
                blockers: /* @__PURE__ */ new Map()
            };
        },
        get routes () {
            return dataRoutes;
        },
        get window () {
            return void 0;
        },
        initialize () {
            throw msg("initialize");
        },
        subscribe () {
            throw msg("subscribe");
        },
        enableScrollRestoration () {
            throw msg("enableScrollRestoration");
        },
        navigate () {
            throw msg("navigate");
        },
        fetch () {
            throw msg("fetch");
        },
        revalidate () {
            throw msg("revalidate");
        },
        createHref,
        encodeLocation,
        getFetcher () {
            return IDLE_FETCHER;
        },
        deleteFetcher () {
            throw msg("deleteFetcher");
        },
        resetFetcher () {
            throw msg("resetFetcher");
        },
        dispose () {
            throw msg("dispose");
        },
        getBlocker () {
            return IDLE_BLOCKER;
        },
        deleteBlocker () {
            throw msg("deleteBlocker");
        },
        patchRoutes () {
            throw msg("patchRoutes");
        },
        _internalFetchControllers: /* @__PURE__ */ new Map(),
        _internalSetRoutes () {
            throw msg("_internalSetRoutes");
        },
        _internalSetStateDoNotUseOrYouWillBreakYourApp () {
            throw msg("_internalSetStateDoNotUseOrYouWillBreakYourApp");
        }
    };
}
function createHref(to) {
    return typeof to === "string" ? to : createPath(to);
}
function encodeLocation(to) {
    var href = typeof to === "string" ? to : createPath(to);
    href = href.replace(/ $/, "%20");
    var encoded = ABSOLUTE_URL_REGEX3.test(href) ? new URL(href) : new URL(href, "http://localhost");
    return {
        pathname: encoded.pathname,
        search: encoded.search,
        hash: encoded.hash
    };
}
var ABSOLUTE_URL_REGEX3 = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;

var _c, _c1, _c2, _c3, _c4, _c5, _c6, _c7, _c8, _c9, _c10, _c11, _c12, _c13, _c14, _c15, _c16, _c17, _c18, _c19, _c20, _c21, _c22, _c23, _c24, _c25, _c26, _c27, _c28, _c29, _c30, _c31, _c32, _c33, _c34, _c35, _c36, _c37;
$RefreshReg$(_c, "AwaitContextProvider");
$RefreshReg$(_c1, "DefaultErrorComponent");
$RefreshReg$(_c2, "RSCErrorHandler");
$RefreshReg$(_c3, "RenderedRoute");
$RefreshReg$(_c4, "RouterProvider");
$RefreshReg$(_c5, "MemoizedDataRoutes");
$RefreshReg$(_c6, "DataRoutes");
$RefreshReg$(_c7, "MemoryRouter");
$RefreshReg$(_c8, "Navigate");
$RefreshReg$(_c9, "Outlet");
$RefreshReg$(_c10, "Route");
$RefreshReg$(_c11, "Router");
$RefreshReg$(_c12, "Routes");
$RefreshReg$(_c13, "Await");
$RefreshReg$(_c14, "ResolveAwait");
$RefreshReg$(_c15, "WithComponentProps");
$RefreshReg$(_c16, "WithHydrateFallbackProps");
$RefreshReg$(_c17, "WithErrorBoundaryProps");
$RefreshReg$(_c18, "StreamTransfer");
$RefreshReg$(_c19, "RemixRootDefaultHydrateFallback");
$RefreshReg$(_c20, "Links");
$RefreshReg$(_c21, "PrefetchPageLinks");
$RefreshReg$(_c22, "PrefetchPageLinksImpl");
$RefreshReg$(_c23, "Meta");
$RefreshReg$(_c24, "Scripts");
$RefreshReg$(_c25, "RemixRootDefaultErrorBoundary");
$RefreshReg$(_c26, "BoundaryShell");
$RefreshReg$(_c27, "BrowserRouter");
$RefreshReg$(_c28, "HashRouter");
$RefreshReg$(_c29, "HistoryRouter");
$RefreshReg$(_c30, "Link");
$RefreshReg$(_c31, "NavLink$React10.forwardRef");
$RefreshReg$(_c32, "NavLink");
$RefreshReg$(_c33, "Form");
$RefreshReg$(_c34, "ScrollRestoration");
$RefreshReg$(_c35, "StaticRouter");
$RefreshReg$(_c36, "StaticRouterProvider");
$RefreshReg$(_c37, "DataRoutes2");

function $RefreshSig$() { return $ReactRefreshRuntime$.createSignatureFunctionForTransform() }
function $RefreshReg$(type, id) { $ReactRefreshRuntime$.register(type, module.id + "_" + id) }
Promise.resolve().then(() => { $ReactRefreshRuntime$.refresh(module.id, module.hot) });


},

}]);
//# sourceMappingURL=lib-router.js.map